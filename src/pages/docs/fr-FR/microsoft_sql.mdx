---
title: Microsoft SQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/365?languages=fr&filter=basic&value=0
metaTitle: Doc d'intégration Microsoft SQL | ILLA Cloud
desc: Apprenez comment utiliser ILLA Cloud pour accéder et travailler avec les données SQL de Microsoft. Construisez des applications, visualisez des données et automatisez des workflows avec ILLA Cloud.
tagCategory: doc_menu_microsoft_sql_click
categoryName: 🔨 Intégrations
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Qu'est-ce que Microsoft SQL ?</h2>

Microsoft SQL dans Illa est une intégration de base de données qui permet aux utilisateurs de se connecter et d'interagir avec une base de données Microsoft SQL (MS SQL). MS SQL est un système de gestion de base de données relationnelle populaire reconnu pour sa rapidité, sa fiabilité et sa facilité d'utilisation.

Avec l'intégration MS SQL dans Illa, vous pouvez interrompre, insérer, mettre à jour et supprimer des données d'une base de données MS SQL à l'aide de commandes SQL. Vous pouvez également créer des requêtes SQL personnalisées et les exécuter dans Illa. Cela facilite la construction d'applications et de tableaux de bord basés sur des données qui affichent des données en temps réel à partir d'une base de données MS SQL.

### Créer MS SQL

Il y a deux façons de créer une ressource dans Illa après la connexion à votre compte Illa.

-   Créer dans les ressources

Connectez-vous à votre compte Illa, sélectionnez `**Ressources**` en haut de la page, et cliquez sur le bouton `**Créer nouveau**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Sélectionnez **`Microsoft SQL`** dans la liste des bases de données.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connectez-vous à la base de données avec les paramètres requis décrits dans `Paramètres de connexion` ci-dessous.

Cliquez sur `**Tester la connexion**` pour voir si nous pouvons nous connecter avec succès à la base de données. Si oui, cliquez sur **`Enregistrer les ressources`**, sinon vérifiez que le nom d'hôte, le port, le nom d'utilisateur et le mot de passe sont corrects.

Après avoir créé une ressource, le SQL MS prêt s'affichera comme indiqué.

![ms_ex_ready](https://cdn.illacloud.com/official-website/img/docs/ms_ex_ready.png)

-   Créer dans le constructeur

Connectez-vous à votre compte Illa, créez un projet dans Illa Builder dans la page `**App**` et accédez à la **`Liste d'actions`** en bas de la page. Cliquez sur `**new**`, puis sélectionnez **`Microsoft SQL`** dans la liste des bases de données. Ensuite, connectez-vous à la base de données avec les paramètres requis décrits dans `Paramètres de connexion` ci-dessous.

Cliquez sur `**Tester la connexion**` pour voir si nous pouvons nous connecter avec succès à la base de données. Si oui, cliquez sur **`Enregistrer les ressources`**, sinon vérifiez que le nom d'hôte, le port, le nom d'utilisateur et le mot de passe sont corrects.

### Paramètres de connexion

Ici, nous avons besoin de fournir des informations pour se connecter à la base de données MS SQL.

![ms_config](https://cdn.illacloud.com/official-website/img/docs/ms_config.png)

| Propriétés                 | Requis      | Libellé                                                                                                                                     |
| -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Nom                        | Obligatoire | Le nom de la ressource lors de la création d'actions dans l'ILLA.                                                                           |
| Hostname                   | Obligatoire | L'URL ou l'adresse IP de votre base de données                                                                                              |
| Port                       | Obligatoire | Le numéro de port de l'hôte que vous devez utiliser pour vous connecter. Si vous ne spécifiez pas de port, le port par défaut est '1433'.   |
| Base de données            | Obligatoire | Le nom de la base de données                                                                                                                |
| Nom d'utilisateur          | Obligatoire | le nom d'utilisateur que vous souhaitez utiliser lors de votre connexion au serveur MS SQL.                                                 |
| Mot de passe               | Obligatoire | Utilisez ce mot de passe pour l'authentification.                                                                                           |
| Option de connexion        | optionnel   | paire clé-valeur pour spécifier l'option de connexion                                                                                       |
| Options SSL                | optionnel   | décide à quel point une connexion sécurisée TCP/IP est priorisée lors de la négociation avec le serveur.                                    |

### Créer des actions

Nous avons créé une ressource MS SQL, nous pouvons ajouter l'action en sélectionnant MS SQL dans la liste des actions et en choisissant le bouton **`Créer l'action`**.

![ms_resource_list](https://cdn.illacloud.com/official-website/img/docs/ms_resource_list.png)

Maintenant, nous avons ajouté le serveur MS SQL comme une action à notre page de construction.

![ms](https://cdn.illacloud.com/official-website/img/docs/ms.png)

### Configurer MS SQL

| Propriétés            | Libellé                                                                                                                                                                                                                                                                                                                       |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Type de configuration | le type de configuration que vous utilisez pour vous connecter à votre base de données MS SQL Server : insertion en bloc et SQL Server. Pour le mode SQL, vous pouvez utiliser la requête SQL. Pour le mode d'insertion en vrac, vous pouvez insérer des données dans une table sélectionnée au format {{ [{x:a, y:b}, … ] }} |
| Requête SQL           | commandes sql pour récupérer (sélectionner), inserer, mettre à jour, supprimer des données de la base de données.                                                                                                                                                                                                             |
| Transformateur        | transformation des données dans le style que vous aimez en utilisant JavaScript                                                                                                                                                                                                                                               |

Exemple d'utilisation :

-   Sélectionner

```sql
SELECT * FROM users ORDER BY lastname;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   Mise à jour

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   Supprimez

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
