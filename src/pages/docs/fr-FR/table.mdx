---
title: Tableau
metaTitle: Doc de composant de table | ILLA Cloud
desc: Apprenez à utiliser le composant de table pour personnaliser votre application dans ILLA Cloud.
tagCategory: doc_menu_table_click
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/267?languages=fr&filter=basic&value=0
categoryName: 🧬 Assembler des composants
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>Qu'est-ce que la date ?</h2>

Le composant Tableau dans ILLA Cloud est une fonctionnalité puissante qui permet aux utilisateurs d'afficher, d'organiser et de manipuler des données tabulaires dans l'application. Il fournit une mise en page structurée avec des lignes et des colonnes, permettant aux utilisateurs de visualiser et d'interagir avec les données de manière pratique et structurée.

### Propriétés

| Propriétés                                    | Libellé                                                                                                                                                                |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source de données                             | source de données qui remplit la table                                                                                                                                 |
| Etat vide                                     | le contenu ou le message affiché lorsque la table n'a pas de données à afficher                                                                                        |
| En cours de chargement                        | si le composant de table est actuellement dans un état de chargement                                                                                                   |
| Colonnes                                      | structure et configuration des colonnes de la table. (Voir la section sur les colonnes ci-dessous pour plus de détails)                                                |
| Colonne                                       | le nombre de colonnes                                                                                                                                                  |
| Sélection multi-ligne                         | Utilisez {{table.selectedRow[n].columnName}} pour accéder aux données.                                                                                                 |
| Sélection de cellule                          | Permettre aux utilisateurs de sélectionner une cellule                                                                                                                 |
| Cliquez à l'extérieur pour désélectionner     | Prise en charge dans le mode de sélection unique en ligne. Après l'avoir activé, cliquez sur une zone autre que la table pour désélectionner la ligne.                 |
| Dépassement                                   | comment la table gère le contenu qui dépasse l'espace disponible dans le conteneur de la table (pagination ou défilement)                                              |
| Activer la pagination côté serveur            | Activer ou non la pagination du serveur                                                                                                                                |
| Taille de la page                             | le nombre de lignes à afficher par page lorsque la pagination côté serveur est activée                                                                                 |
| Rafraîchir                                    | Afficher ou non l'icône de rafraîchissement.                                                                                                                           |
| Télécharger                                   | Afficher le bouton de téléchargement dans la barre d'outils.                                                                                                           |
| Télécharger les données brutes                | Afficher/Masquer l'icône de téléchargement des données brutes.                                                                                                         |
| Filtre                                        | Afficher le bouton de filtre dans la barre d'outils                                                                                                                    |
| Gestionnaire d'événements                     | la détection et la réponse aux actions ou événements spécifiques de l'utilisateur tels que les clics, les touches pressées ou les soumissions de formulaires.          |
| Désactivé                                     | non interactif et ne peut pas être modifié ou déclenché par l'utilisateur.                                                                                             |

#### **Vue d'ensemble des propriétés des colonnes**

Pour chaque colonne, nous pouvons configurer ses propriétés pour personnaliser les données affichées pour chaque colonne.

| Propriétés                                           | Libellé                                                                                                                                                                                                                                      |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Titre de la colonne                                  | spécifie le nom ou le libellé de la colonne, qui sert d'en-tête pour identifier les données qu'il représente                                                                                                                                 |
| Type de colonne                                      | définit le type de données que la colonne contient, y compris l'auto, le texte, la date, la balise, l'heure, la dateTime, nombre, pourcentage, lien, bouton, groupe de boutons, booléen, image, groupe d'icônes, notation, Markdown, devise  |
| Valeur Mappée                                        | Utiliser {{ currentRow }} pour accéder aux données de la ligne courante. Par exemple, pour calculer "coût par clic" en fonction des colonnes "coût" et "temps de clic":                                                                      |
| {{ currentRow.cost / currentRow.clickTimes }}        |                                                                                                                                                                                                                                              |
| Activer le tri                                       | si la colonne permet aux utilisateurs de trier les données de la table en fonction des valeurs de cette colonne spécifique                                                                                                                   |
| Arrière-plan                                         | Définir l'arrière-plan de la colonne. Utilisez {{ écrire une instruction conditionnelle pour définir la couleur affichée par chaque cellule sous des conditions différentes.                                                                 |
| Alignement                                           | contrôle l'alignement horizontal du contenu dans les cellules de colonne                                                                                                                                                                     |

#### Types de colonnes :

-   Automatique

un type dynamique qui détermine automatiquement le type de données basé sur les valeurs de la colonne

-   Texte du texte

représente une colonne qui contient des données textuelles

-   Date

représente les valeurs de données qui correspondent à des dates spécifiques

| Propriétés   | Libellé                                                                              |
| ------------ | ------------------------------------------------------------------------------------ |
| Formater     | accepte différents codes de formatage ou motifs, la valeur par défaut est AAAA-MM-JJ |

-   Étiquette

un format spécialisé pour afficher les balises ou les étiquettes associées aux entrées de données

| Propriétés             | Libellé                                                                      |
| ---------------------- | ---------------------------------------------------------------------------- |
| Étiquette d'étiquette  | représente le texte ou le libellé assigné à une balise dans la colonne "tag" |
| Couleur du tag         | définit la couleur assignée à la balise dans la colonne "tag"                |

-   Date et heure

représenter et manipuler avec précision les valeurs de temps dans le tableau

| Propriétés   | Libellé                                                                                            |
| ------------ | -------------------------------------------------------------------------------------------------- |
| Formater     | la représentation visuelle des données de temps dans la colonne, la valeur par défaut est HH:mm:ss |

-   DateHeure

stocke et représente les valeurs de date et d'heure

| Propriétés   | Libellé                                                                                                                              |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| Formater     | spécifie comment les valeurs de date et d'heure doivent être affichées dans la colonne, la valeur par défaut est AAAA-MM-JJ HH:mm:ss |

-   Numéros

gérer les données numériques

| Propriétés                         | Libellé                                                                                                                                    |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Emplacements décimaux              | spécifiez le nombre de décimales à afficher pour les valeurs numériques dans la colonne                                                    |
| Afficher le séparateur de milliers | si la colonne doit afficher un séparateur de milliers pour améliorer la lisibilité et la facilité d'interprétation pour les grands nombres |

-   Pourcentage

gérer les valeurs en pourcentage d'une manière concise et visuellement attrayante

| Propriétés                         | Libellé                                                                                     |
| ---------------------------------- | ------------------------------------------------------------------------------------------- |
| Emplacements décimaux              | spécifiez le nombre de décimales à afficher pour les valeurs de pourcentage dans la colonne |
| Afficher le séparateur de milliers | si un séparateur de milliers doit être inclus dans les valeurs de pourcentage affichées     |

-   Lier

inclure les liens cliquables dans une colonne spécifique

-   Bouton

Inclure les boutons interactifs dans une colonne spécifique

| Propriétés                | Libellé                                                                                      |
| ------------------------- | -------------------------------------------------------------------------------------------- |
| Gestionnaire d'événements | définit l'action ou l'événement qui se produit lorsque le bouton de la colonne est cliqué    |
| Désactivé                 | si le bouton de la colonne est activé ou désactivé                                           |
| Couleur du thème          | choisir une couleur de thème pour le bouton                                                  |
| Variante                  | style visuel ou variante du bouton (Compléter ou contourner)                                 |

-   Groupe de boutons

permet aux utilisateurs de créer des groupes de boutons interactifs dans une colonne

Pour chaque bouton dans les colonnes, nous pouvons configurer ses propriétés.

| Propriétés                | Libellé                                                                                                                                                 |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Valeur Mappée             | associer chaque bouton du groupe de boutons avec une valeur ou un champ spécifique de la source de données                                              |
| Variante                  | style visuel ou apparence des boutons dans le groupe de boutons                                                                                         |
| Gestionnaire d'événements | l'action ou la fonction déclenchée lorsqu'un bouton du groupe est cliqué ou interagit avec                                                              |
| Désactivé                 | si les boutons du groupe de boutons sont activés ou désactivés                                                                                          |
| Couleur du thème          | personnaliser la couleur ou le style des boutons dans le groupe pour s'aligner sur le thème global ou l'image de marque de leur application ou site web |

-   Boolean

représente des données qui ont deux états possibles : true ou false

-   Image

remplir une colonne dans la table avec les données d'image

| Propriétés     | Libellé                                                                           |
| -------------- | --------------------------------------------------------------------------------- |
| Type d'échelle | comment l'image est mise à l'échelle ou redimensionnée dans la cellule du tableau |

-   Groupe d'icônes

une colonne spécialisée qui permet aux utilisateurs d'afficher des icônes dans un groupe ou une collection

Pour chaque bouton dans les colonnes, nous pouvons configurer ses propriétés.

| Propriétés                | Libellé                                                                                                                                                 |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Icône                     | permet aux utilisateurs de sélectionner ou de spécifier l'icône à afficher dans la colonne IconGroup                                                    |
| Couleur du thème          | détermine le modèle de couleur ou le thème appliqué à l'icône dans la colonne IconGroup                                                                 |
| Gestionnaire d'événements | permet aux utilisateurs de définir un événement ou une action déclenchée lorsque l'icône de la colonne IconGroup est interagissant par les utilisateurs |
| Désactivé                 | état de la colonne IconGroup, en déterminant si elle est désactivée ou activée pour l'interaction utilisateur                                           |

-   Évaluation

représente les notes ou commentaires donnés aux articles ou aux entités

-   Markdown

possibilité de rendre le texte formaté en utilisant la syntaxe Markdown dans les cellules de la colonne

-   Devise

gérer et formater les valeurs de la devise.

| Propriétés                         | Libellé                                                                                  |
| ---------------------------------- | ---------------------------------------------------------------------------------------- |
| Emplacements décimaux              | le nombre de décimales à afficher pour les valeurs de la devise dans la colonne          |
| Code devise                        | spécifie le code ou l'abréviation représentant la devise utilisée dans la colonne        |
| Afficher le séparateur de milliers | si un séparateur de milliers est affiché pour les grandes valeurs de devise              |

### Méthode

Vous pouvez utiliser d'autres composants pour contrôler le composant. Nous soutenons les deux méthodes suivantes:

-   **selectPage**

sélectionner toutes les lignes d'une page spécifique dans une table paginée

| Propriétés     | Libellé                                                |
| -------------- | ------------------------------------------------------ |
| Valeur de fin  | le numéro de page ou l'index de la page à sélectionner |

-   **sélectionnerLigne**

sélectionnez une ligne spécifique dans la table

| Propriétés                    | Libellé                                                                                                                                                                            |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ligne sélectionnée par défaut | la valeur par défaut de la ligne sélectionnée, peut être l'index de ligne, un identifiant unique, ou toute autre propriété qui identifie de manière unique la ligne à sélectionner |

-   **clearSelection**

effacer la sélection courante dans la table

-   **setFilters**

appliquer des filtres aux données de la table

| Propriétés  | Libellé                                      |
| ----------- | -------------------------------------------- |
| Filtres     | conditions ou critères de filtrage           |
| Mode filtre | détermine comment les filtres sont appliqués |

-   **ClearFilters**

efface tous les filtres appliqués dans la table

-   **setSort**

définir le tri pour la table

| Propriétés   | Libellé                                                         |
| ------------ | --------------------------------------------------------------- |
| Colonne      | la colonne sur laquelle le tri doit être appliqué               |
| Orientation  | direction du genre, qui peut être "ascendant" ou "descendant"   |

### Donnée

Le composant a des données couramment utilisées, qui peuvent être appelées via **`{{componentName.propertyName}}`** dans l'application.

| Nom de la propriété                               | Libellé                                                                                                                                  |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| cliquez en dehors pour réinitialiser la sélection | si vous cliquez en dehors du composant Tableau devrait réinitialiser la sélection actuelle                                               |
| columnMapper                                      | un objet qui fait correspondre les champs de données ou les clés de la source de données de la table aux noms de colonnes correspondants |
| Index de noms de colonnes                         | un objet qui stocke les indices des noms de colonnes                                                                                     |
| colonnes                                          | un tableau qui définit la configuration et les propriétés de chaque colonne dans la table                                                |
| Visibilité de colonne                             | un objet qui stocke le statut de visibilité de chaque colonne dans la table                                                              |
| Indexations de colonnes personnalisées            | un objet qui stocke les indices des colonnes personnalisées dans la table                                                                |
| Source de données                                 | la source de données de la table                                                                                                         |
| dataSourceJS                                      | une représentation JavaScript de la source de données                                                                                    |
| Mode source de données                            | le mode ou le type de la source de données.                                                                                              |
| format@@0 defaultSortKey                          | clé de colonne par défaut à utiliser pour trier la table initialement                                                                    |
| defaultSortOrder                                  | l'ordre de tri par défaut de la table                                                                                                    |
| désactivée                                        | si le composant Tableau est désactivé ou non                                                                                             |
| Données affichées                                 | données actuellement affichées dans le tableau                                                                                           |
| Indexations des données affichées                 | les indices des lignes affichées dans la table                                                                                           |
| nomdeaffichage                                    | spécifie le nom ou le libellé du composant de table                                                                                      |
| Télécharger                                       | permet de télécharger les données de la table                                                                                            |
| Télécharger RawData                               | télécharger les données brutes ou les données affichées du tableau                                                                       |
| videState                                         | le contenu ou le message à afficher lorsque la table est vide                                                                            |
| Activer la pagination latérale du serveur         | Quand activé, la propriété enableServerSidePagination active la pagination côté serveur pour la table                                    |
| Activer la sélection simple de cellule            | permet la sélection à une seule cellule dans la table                                                                                    |
| Filtre                                            | les conditions de filtre ou de filtrage actuellement appliquées dans le tableau                                                          |
| hasNextPage                                       | s'il y a une page suivante disponible dans la table paginée                                                                              |
| en cours de chargement                            | si le composant de la table est dans un état de chargement                                                                               |
| Sélection multiligne                              | active la sélection multi-ligne dans la table                                                                                            |
| nextAfterCursor                                   | le curseur ou l'identifiant pour récupérer le prochain ensemble de données dans la pagination côté serveur                               |
| format@@0 nextBeforeCursor                        | le curseur pour récupérer les données précédentes dans la pagination côté serveur                                                        |
| débordement                                       | le comportement de la table lorsque le contenu dépasse l'espace disponible                                                               |
| pageIndex                                         | l'index de la page courante dans une table paginée                                                                                       |
| Taille de la page                                 | détermine le nombre de lignes à afficher par page dans une table paginée                                                                 |
| Décalage de la page                               | le nombre de pages à décaler dans la pagination                                                                                          |
| Actualiser                                        | déclenche une actualisation des données de la table                                                                                      |
| Noms de colonnes renommés                         | stocke les noms de colonnes renommés dans la table                                                                                       |
| Evénements en ligne                               | permet d'associer des événements ou des actions personnalisés à des lignes spécifiques dans la table                                     |
| Nombre total de lignes                            | le nombre total de lignes dans la table                                                                                                  |

Exemple: {{table1.disabled}}

## Cas d'utilisation

Ensuite, nous allons montrer comment mapper les données de `**Upstash**` vers le composant `**table**`.

### Étape 1 Ajouter une action

Créons une table dans Upstash appelée **`employee_profile`,** incluant 4 colonnes `name`, `level`, `age` et `maried`.

Ensuite, nous pouvons créer une nouvelle action pour Upstash à partir de la liste d'action dans ILLA Cloud et nommé upstash2.

Pour lister toutes les données dans employee_profile mettez le code snippet ci-dessous dans la section Requête Redis.

```sql
OBTENIR le profil de l'employé
```

Puisque les données de sortie sont une liste de paires de dictionnaire, nous voulons les transformer en tableau. Dans Transformer, activez-le et mettez le code ci-dessous.

```jsx
const jsonString = data[0].result;

// Parse the JSON string into a JavaScript object
const jsonData = JSON.parse(jsonString);

// Access the "employees" property and map it to a new array
const mappedArray = jsonData.employees.map((employee) => ({
  name: employee.name,
  level: employee.level,
  age: employee.age,
  married: employee.married,
}));

return mappedArray;
```

![up_code](https://cdn.illacloud.com/official-website/img/docs/up_code.jpg)

Cliquez sur **`Save`** et **`Run`** pour activer cette action.

![up_data](https://cdn.illacloud.com/official-website/img/docs/up_data.jpg)

### Étape 2 Ajouter des composants

Ensuite, nous pouvons ajouter un composant **`table`** à la toile.

Dans le champ **`Source de données`** dans la page d'Inspect après avoir cliqué sur le composant, mettez `{{upstash2.data}}` pour récupérer les données depuis Upstash.

### Test de l'étape 3

Le regard final devrait être comme indiqué.

![up_test](https://cdn.illacloud.com/official-website/img/docs/up_test.jpg)
