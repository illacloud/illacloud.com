---
title: API REST
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/385?languages=fr&filter=basic&value=0
metaTitle: Doc d'intégration d'API REST | ILLA Cloud
desc: Apprenez comment utiliser ILLA Cloud pour accéder et travailler avec les données de l'API REST. Construisez des applications, visualisez des données et automatisez des workflows avec ILLA Cloud.
tagCategory: doc_menu_rest_api_click
categoryName: 🔨 Intégrations
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Qu'est-ce que REST API ?</h2>

Dans ILLA Cloud, la fonctionnalité d'API REST permet aux développeurs d'interagir avec des systèmes externes, les services et les sources de données en utilisant le style architectural du Transfert d'Etat Représentant (REST). Avec la REST API dans ILLA Cloud, les développeurs peuvent intégrer de façon transparente leurs applications avec d'autres applications, bases de données ou services.

L'API REST dans ILLA Cloud permet de créer des terminaux personnalisés qui peuvent gérer les requêtes HTTP, telles que GET, POST, PUT et DELETE. Cela permet aux développeurs d'effectuer diverses opérations, y compris la récupération de données, l'envoi de données, la mise à jour des enregistrements et la suppression d'informations.

### Créer une API REST

Il y a deux façons de créer une ressource dans Illa après la connexion à votre compte Illa.

-   Créer dans les ressources

Connectez-vous à votre compte Illa, sélectionnez `**Ressources**` en haut de la page, et cliquez sur le bouton `**Créer nouveau**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Sélectionnez **`REST API`** dans la liste des bases de données.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connectez-vous à la base de données avec les paramètres requis décrits dans `Paramètres de connexion` ci-dessous.

Cliquez sur `**Tester la connexion**` pour voir si nous pouvons nous connecter avec succès à la base de données. Si oui, cliquez sur **`Enregistrer les ressources`**, sinon vérifiez que le nom d'hôte, le port, le nom d'utilisateur et le mot de passe sont corrects.

Après avoir créé une ressource, l'API REST prête s'affichera comme indiqué.

![rest_ex_ready](https://cdn.illacloud.com/official-website/img/official-site/components/rest_ex_ready.jpg)

-   Créer dans le constructeur

Connectez-vous à votre compte Illa, créez un projet dans Illa Builder dans la page `**App**` et accédez à la **`Liste d'actions`** en bas de la page. Cliquez sur `**new**`, puis sélectionnez **`REST API`** dans la liste des bases de données. Ensuite, connectez-vous à la base de données avec les paramètres requis décrits dans `Paramètres de connexion` ci-dessous.

Cliquez sur `**Tester la connexion**` pour voir si nous pouvons nous connecter avec succès à la base de données. Si oui, cliquez sur **`Enregistrer les ressources`**, sinon vérifiez que le nom d'hôte, le port, le nom d'utilisateur et le mot de passe sont corrects.

### Paramètres de connexion

Ici, nous devons fournir des informations pour nous connecter à la base de données de l'API REST.

![rest_config](https://cdn.illacloud.com/official-website/img/official-site/components/rest_config.jpg)

| Propriétés        | Requis      | Libellé                                                                                                       |
| ----------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| Nom               | Obligatoire | Le nom de la ressource lors de la création d'actions dans l'ILLA.                                             |
| URL de base       | Obligatoire | L'URL ou l'adresse IP de votre base de données                                                                |
| Paramètres d'URL  | optionnel   | fournir des informations supplémentaires ou modifier le comportement des requêtes API                         |
| En-têtes          | optionnel   | métadonnées supplémentaires à propos de la requête ou de la réponse de l'API                                  |
| Cookies           | optionnel   | de petites parties de données qui sont stockées sur le côté client et envoyées avec chaque requête API        |
| Authentification  | optionnel   | vérification de l'identité de l'utilisateur ou de l'application faisant la demande de l'API                   |

### Créer des actions

Nous avons créé une ressource REST API ; nous pouvons ajouter l'action en sélectionnant REST API dans la liste d'actions et en choisissant le bouton **`Créer l'action`**.

![rest_resource_list](https://cdn.illacloud.com/official-website/img/official-site/components/rest_resource_list.jpg)

Maintenant nous avons ajouté le serveur REST API comme une action à notre page de construction.

![rest](https://cdn.illacloud.com/official-website/img/official-site/components/rest.jpg)

### Configurer l'API REST

| Propriétés       | Libellé                                                                                                                                              |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Type d'action    | GET, POST, PUT, HEAD, PATCH, DELETE, OPTIONS                                                                                                         |
| Paramètre d'URL  | fournir des informations supplémentaires ou modifier le comportement des requêtes API si non spécifié dans les paramètres de connexion               |
| En-têtes         | métadonnées supplémentaires à propos de la requête ou de la réponse de l'API si non spécifiées dans la configuration de la connexion                 |
| Transformateur   | transformation des données dans le style que vous aimez en utilisant JavaScript                                                                      |

#### OBTENIR

récupère ou récupère les données depuis un serveur. Lorsque vous faites une requête GET, le client spécifie l'URL de la ressource à laquelle il veut accéder, et le serveur répond avec les données demandées.

#### POSTER

créer une nouvelle ressource sur le serveur. Il permet aux clients de soumettre des données au serveur à être traitées et stockées. Lors de l'envoi d'une requête POST, le client inclut les données dans le corps de la requête.

#### SUPPRIMER

mettre à jour ou remplacer une ressource existante. Il est généralement utilisé pour modifier l'état ou le contenu d'une ressource identifiée par une URL spécifique.

#### TÊTE

similaire à la méthode GET, mais avec une différence clé : la méthode HEAD ne récupère que les en-têtes de la réponse, sans le corps de la réponse.

#### PATCH

mettre à jour une ressource spécifique. Il est généralement utilisé pour apporter des modifications partielles à une ressource existante sans remplacer la ressource entière.

#### EFFACER

demander la suppression d'une ressource ou de données spécifiques identifiées par une URL donnée

### Cas d'utilisation

Nous vous montrerons comment remplir automatiquement les informations du formulaire à partir des données de l'API REST.

#### Étape 1 Ajouter une action

Créons une table dans l'API REST avec les colonnes `id`, `name` et `age`. Le document id 1 est ce que nous voulons afficher dans le composant de formulaire.

![rest_get_all](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_all.jpg)

Ensuite, nous pouvons créer une nouvelle action pour l'API REST à partir de la liste d'actions dans l'application que nous voulons construire, listant toutes les données du segment et nommé restapi1.

Sélectionnez `GET` pour `**Type d'action**`. Dans les paramètres d'URL, mettez `id` pour la clé et `1` pour la valeur**.**

Cliquez sur **`Save`** et **`Run`** pour activer cette action.

![rest_get_1](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_1.jpg)

#### Étape 2 Ajouter des composants

Ajoute un composant **`form`** avec le texte par défaut "Form" et un bouton "Soumettre" à canvas.

Ajoute un composant **`input`** , un composant **`number input`** et un composant **`button`** de `**Insert**` dans le formulaire.

Étiquetez le composant d'entrée qui sera "Nom", le composant d'entrée de nombre qui sera "Âge" et le composant de bouton qui sera "Remplissage Automatique".

Définissez l'espace réservé du composant d'entrée à "Veuillez mettre votre nom ici" et la valeur par défaut du nombre d'entrée à 0 comme indiqué ci-dessous.

![rest_layout](https://cdn.illacloud.com/official-website/img/official-site/components/rest_layout.jpg)

#### Étape 3 Configurer le composant

Pour les composants "Remplissage automatique" **`button`**, nous pouvons le configurer pour définir la valeur dans le composant **`input`**.

1.  Cliquez sur le composant "Remplissage Automatique" **`button`** pour ouvrir sa page d'inspection. Sous **`Gestionnaire d'événement`**, cliquez sur `**+ Nouveau**`.
2.  Dans le gestionnaire d'événements, sélectionnez **`Control component`** en action, et sélectionnez le composant **`input`** que vous voulez mettre à jour comme cible de l'événement.
3.  Choisissez l'action **`setValue`** et définissez la valeur comme `{{restapi1.data[0]["name"]}}`

![rest_get_input](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_input.jpg)

De même, pour que les composants "Remplissage Automatique" **`button`** définissent la valeur dans le composant **`nombre input`**.

1.  Sous **`Gestionnaire d'événement`**, cliquez sur `**+ Nouveau**`.
2.  Dans le gestionnaire d'événements, sélectionnez **`Control component`** en action, et sélectionnez le composant **`number input`** que vous voulez mettre à jour comme cible de l'événement.
3.  Choisissez l'action **`setValue`** et définissez la valeur comme `{{restapi1.data[0]["age"]}}`

![rest_get_num](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_num.jpg)

Pour le composant "Soumettre" **`button`**, nous pouvons le configurer pour soumettre le formulaire si le formulaire est rempli

1.  Cliquez sur le composant **`button`** pour ouvrir sa page d'inspection. Sous **`Gestionnaire d'événement`**, cliquez sur `**+ Nouveau**`.
2.  Dans le gestionnaire d'événements, sélectionnez **`Control component`** en action, et sélectionnez le composant **`form`** que vous voulez mettre à jour comme cible de l'événement.
3.  Choisissez la méthode **`submit`**

![rest_submit_config](https://cdn.illacloud.com/official-website/img/official-site/components/rest_submit_config.jpg)

#### Test de l'étape 4

![rest_test](https://cdn.illacloud.com/official-website/img/official-site/components/rest_test.gif)
