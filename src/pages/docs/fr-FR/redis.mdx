---
title: Redis
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/347?languages=fr&filter=basic&value=0
metaTitle: Doc d'intégration Redis | ILLA Cloud
desc: Découvrez comment vous connecter et interagir avec votre base de données Redis en utilisant la fonctionnalité d'intégration de la base de données d'ILLA Cloud. Consultez ce guide pour plus de détails.
tagCategory: doc_menu_redis_click
categoryName: 🔨 Intégrations
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Qu'est-ce que Redis ?</h2>

Redis dans Illa est une intégration de base de données qui vous permet de vous connecter et d'interagir avec une base de données Redis. Redis est un système de gestion de base de données relationnelle populaire reconnu pour sa rapidité, sa fiabilité et sa facilité d'utilisation.

Avec l'intégration de Redis dans Illa, vous pouvez interroger, insérer, mettre à jour et supprimer des données d'une base de données Redis en utilisant des commandes SQL. Vous pouvez également créer des requêtes SQL personnalisées et les exécuter dans Illa. Cela facilite la construction d'applications et de tableaux de bord basés sur des données qui affichent des données en temps réel à partir d'une base de données Redis.

### Créer un Redis

Il y a deux façons de créer une ressource dans Illa après la connexion à votre compte Illa.

-   Créer dans les ressources

Connectez-vous à votre compte Illa, sélectionnez `**Ressources**` en haut de la page, et cliquez sur le bouton `**Créer nouveau**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Sélectionnez **`Redis`** dans la liste des bases de données.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connectez-vous à la base de données avec les paramètres requis décrits dans `Paramètres de connexion` ci-dessous.

Cliquez sur `**Tester la connexion**` pour voir si nous pouvons nous connecter avec succès à la base de données. Si oui, cliquez sur **`Enregistrer les ressources`**, sinon vérifiez que le nom d'hôte, le port, le nom d'utilisateur et le mot de passe sont corrects.

Après avoir créé une ressource, le Redis prêt s'affichera comme indiqué.

![redis_ex_ready](https://cdn.illacloud.com/official-website/img/docs/redis_ex_ready.png)

-   Créer dans le constructeur

Connectez-vous à votre compte Illa, créez un projet dans Illa Builder dans la page `**App**` et accédez à la **`Liste d'actions`** en bas de la page. Cliquez sur `**new**`, puis sélectionnez **`Redis`** dans la liste des bases de données. Ensuite, connectez-vous à la base de données avec les paramètres requis décrits dans `Paramètres de connexion` ci-dessous.

Cliquez sur `**Tester la connexion**` pour voir si nous pouvons nous connecter avec succès à la base de données. Si oui, cliquez sur **`Enregistrer les ressources`**, sinon vérifiez que le nom d'hôte, le port, le nom d'utilisateur et le mot de passe sont corrects.

### Paramètres de connexion

Ici, nous avons besoin de fournir des informations pour se connecter à la base de données Redis.

![redis_config](https://cdn.illacloud.com/official-website/img/docs/redis_config.png)

| Propriétés                  | Requis      | Libellé                                                                                                                                                |
| --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Nom                         | Obligatoire | Le nom de la ressource lors de la création d'actions dans l'ILLA.                                                                                      |
| Hostname                    | Obligatoire | L'URL ou l'adresse IP de votre base de données                                                                                                         |
| Port                        | Obligatoire | Le numéro de port de l'hôte que vous devez utiliser pour vous connecter. Si vous ne spécifiez pas de port, le port par défaut est '6379'.              |
| Index de la base de données | optionnel   | une structure de données qui est utilisée pour optimiser les performances des requêtes qui récupèrent les données d'une base de données Redis          |
| Base de données             | Obligatoire | Le nom de la base de données                                                                                                                           |
| Nom d'utilisateur           | Obligatoire | le nom d'utilisateur que vous souhaitez utiliser lors de votre connexion au serveur Redis.                                                             |
| Mot de passe                | Obligatoire | Utilisez ce mot de passe pour l'authentification.                                                                                                      |
| Options SSL                 | optionnel   | décide à quel point une connexion sécurisée TCP/IP est priorisée lors de la négociation avec le serveur.                                               |

### Créer des actions

Nous avons créé une ressource Redis, nous pouvons ajouter l'action en sélectionnant Redis dans la liste des actions et en choisissant le bouton **`Créer l'action`**.

![redis_resource_list](https://cdn.illacloud.com/official-website/img/docs/redis_resource_list.png)

Maintenant nous avons ajouté le serveur Redis comme une action à notre page de construction.

![redis](https://cdn.illacloud.com/official-website/img/docs/redis.png)

### Configurer Redis

| Propriétés     | Libellé                                                                         |
| -------------- | ------------------------------------------------------------------------------- |
| Requête        | commandes pour récupérer les données de la base de données                      |
| Transformateur | transformation des données dans le style que vous aimez en utilisant JavaScript |

Exemple d'utilisation :

-   CLÉS

Cette commande récupère une liste de toutes les clés de la base de données Redis. Vous pouvez utiliser des caractères génériques pour filtrer les clés par motif.

```sql
KEYS *
KEYS user:*
```

-   OBTENIR

Cette commande récupère la valeur associée à une clé donnée.

```sql
GET user:12345
```

-   HGETALL

Cette commande récupère tous les champs et leurs valeurs à partir d'une structure de données de hachage Redis.

```sql
HGETALL user:12345
```

-   ZRANGE

Cette commande récupère une plage de valeurs à partir d'une structure de données Redis triée par ensemble en fonction de leurs scores.

```sql
ZRANGE high_scores 0 9 WITHSCORES
```

-   RANGER

Cette commande récupère une plage de valeurs à partir d'une structure de données de la liste Redis.

```sql
LRANGE my_list 0 4
```

-   EXISTES

Cette commande vérifie si une clé donnée existe dans la base de données Redis.

```sql
EXISTS user:12345
```

-   TTC

Cette commande récupère la valeur du temps de vie (TTL) associée à une clé, qui spécifie la durée de vie de la clé avant qu'elle n'expire.

```sql
TTL user:12345
```
