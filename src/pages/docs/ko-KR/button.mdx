---
title: Button
metaTitle: Button Component Doc | ILLA Cloud
desc: Learn how to use the button component in ILLA Cloud.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/303?languages=en&filter=basic&value=0
tagCategory: doc_menu_button_click
categoryName: 🧬 Assemble components
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>What is Button?</h2>

The Button component allows users to trigger an action or perform a function by clicking on the button. 

### 속성

| 속성          | 설명                                                                                                                        |
| ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| Text        | The text displayed for the button                                                                                         |
| 이벤트 핸들러     | 구성 요소 이벤트에 대한 응답으로 쿼리를 트리거하거나 구성 요소를 제어하거나 다른 API를 호출합니다.                                                                 |
| 로드 중        | 구성 요소가 로드되는지 여부를 제어합니다.                                                                                                   |
| 장애가 있는      | Control the status of whether the component is disabled. The component cannot be modified or focused when it is disabled. |
| 힌트          | User can enter component tooltip here. The tooltip will be shown when it is focused. Markdown format is supported.        |
| 숨다          | Dynamically control whether the component is hidden. You can change the hidden status through dynamical boolean value.    |
| 스타일         | Allow users to choose between two different styles for the button: fill or outline.                                       |
| Theme Color | Allows users to specify the button's background color and opacity                                                         |

### 이벤트 핸들러

| 이벤트      | 설명                                                                          |
| -------- | --------------------------------------------------------------------------- |
| 딸깍 하는 소리 | When a user click this button, perform specific action customized by users. |

### 응답 데이터

The component has some commonly used data, which can be called via **`{{componentName.propertyName}}`** in the app.

| Property name | 설명                                                           |
| ------------- | ------------------------------------------------------------ |
| colorScheme   | background color of the button                               |
| 장애가 있는        | a Boolean value indicate the disabled status                 |
| displayName   | the name of this component (ie button1)                      |
| 숨다            | a Boolean value indicate the hidden status of button         |
| hiddenDynamic | a Boolean value indicate the hidden dynamic status of button |
| 로드 중          | loading value                                                |
| text          | text value displayed on the button                           |
| tooltipText   | value of tooltip text                                        |
| 스타일           | variant style of the button (fill or outlined)               |

### Use case

Next, we will demonstrate how to use button to control the **`form`** component dynamically. 

#### Step 1 Add an action

We have created a table named **`countries`**, including 5 columns:  **`num_code`**,  **`alpha_2_code`**, **`alpha_3_code`**, **`en_short_name`**, **`nationality`**. The countries’ names are what we want to use as options value, which are stored the **`en_short_name`** column.

Then we can create a new action for Supabase from the action list, listing all data in **`countries`** and named supabasedb1 

```sql
select distinct en_short_name as value from "Countries" order by value;
```

This is what we have as output:

<img src='https://cdn.illacloud.com/official-website/img/docs/countries.png' alt='' />

Click **`Save`** and **`Run`** to activate this action.

#### Step 2 Add a Component

Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** Then we drag a Input component, a select component and a number input component into the form, labeling them as **`Name`, `Nationality`,** and **`Age`.** First we add the **`Form`** component and label it as 'Survey'.

We then map the **`countries`** values to select component by 

1.  in mapped mode, set data sources to be `{{supabasedb1.data}}` 
2.  in mapped option, set value to be `{{item.value}}`

<img src='https://cdn.illacloud.com/official-website/img/docs/button1.png' alt='' />

Then we can configure the **`Submit`** button to control the **`Send`** method of **`Form`** component. 

1.  In the Edit event handler, select **`Control component`** in action, select the **`form`** component that you want to update as the target of the event.
2.  Choose the **`Submit`** action. 

<img src='https://cdn.illacloud.com/official-website/img/docs/countries2.png' alt='' />
