---
title: Microsoft SQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/365?languages=en&filter=basic&value=0
metaTitle: Microsoft SQL Integration Doc | ILLA Cloud
desc: Learn how to use ILLA Cloud to access and work with Microsoft SQL data. Build apps, visualize data, and automate workflows with ILLA Cloud.
tagCategory: doc_menu_microsoft_sql_click
categoryName: 🔨 통합
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>What is Microsoft SQL?</h2>

Microsoft SQL in Illa is a database integration that allows users to connect and interact with a Microsoft SQL (MS SQL) database. MS SQL is a popular open-source relational database management system that is known for its speed, reliability, and ease of use.

With the MS SQL integration in Illa, you can query, insert, update, and delete data from a MS SQL database using SQL commands. With the Appwrite integration in Illa, you can query, insert, update, and delete data from a Appwrite database using SQL commands. This makes it easy to build data-driven applications and dashboards that display real-time data from a MS SQL database.

### Create MS SQL

There are two ways to create a resource in Illa after signing into your Illa account.

-   Create in Resources

Sign into your Illa account, select `**Resources**` on the top of the page, and click `**Create New**` button.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Select **`Microsoft SQL`** from the database list.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connect to the database with the required parameters described in `Connection Settings` below.

Click `**Test Connection**` to see if we can successfully connect to the database. If yes, click **`Save Resources`**, else, double check the hostname, port, username, and password is correct.

After creating a resource, the ready MS SQL will display as shown.

![ms_ex_ready]\(https://cdn.illacloud.com/official-website/img/docs/ms_ex_ready.png)

-   Create in Builder

Sign into your Illa account, create a project in Illa Builder in the `**App**` page, and navigate to the **`Action List`** at the bottom of the page. Click `**new**`, then select **`Microsoft SQL`** from the database list. Then, connect to the database with required parameters described in `Connection Settings` below.

Click `**Test Connection**` to see if we can successfully connect to the database. If yes, click **`Save Resources`**, else, double check the hostname, port, username, and password is correct.

### Connection Settings

Here we need to provide information for connecting to MS SQL database.

![ms_config]\(https://cdn.illacloud.com/official-website/img/docs/ms_config.png)

| 속성                | 필수    | 설명                                                                                                                 |
| ----------------- | ----- | ------------------------------------------------------------------------------------------------------------------ |
| 이름                | 필수    | ILLA에서 이 자원의 이름을 지정하십시오.                                                                                           |
| 호스트 이름            | 필수    | The URL or IP address for your database                                                                            |
| 포트                | 필수    | The server host's port number that you should use to connect. If you don't specify a port, default port is '1433'. |
| 데이터 베이스           | 필수    | The name of the database                                                                                           |
| 사용자 이름            | 필수    | the username you wish to use when logging in to the MS SQL server.                                                 |
| 비밀번호              | 필수    | Use this password for authentication.                                                                              |
| Connection option | 선택 과목 | key-value pair to specify the connection option                                                                    |
| SSL 옵션            | 선택 과목 | decides how high a secure SSL TCP/IP connection is prioritized while negotiating with the server.                  |

### Create Actions

We have created a MS SQL resource; we can add the action by selecting MS SQL from action list and choosing the **`Create action`** button.

![ms_resource_list]\(https://cdn.illacloud.com/official-website/img/docs/ms_resource_list.png)

Now we have added the MS SQL server as an action to our building page.

![ms]\(https://cdn.illacloud.com/official-website/img/docs/ms.png)

### Configure MS SQL

| 속성          | 설명                                                                                                                                                                                                                                                 |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 구성 유형       | the type of configuration that you use to connect to your MS SQL Server database: SQL and Bulk insert. For SQL mode, you can use SQL query. For Bulk insert mode, you can insert data into a selected table with the format {{ [{x:a, y:b}, … ] }} |
| SQL query   | sql commands to fetch (select), insert, update, delete data from database.                                                                                                                                                                         |
| 변신 로봇       | transforming data into the style you like using JavaScript                                                                                                                                                                                         |

Example usage:

-   Select

```sql
SELECT * FROM users ORDER BY lastname;
```

-   구성 요소

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   업데이트

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   삭제

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
