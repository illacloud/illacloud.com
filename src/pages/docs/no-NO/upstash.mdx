---
title: Upstash
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/427?languages=no&filter=basic&value=0
metaTitle: Upstash Integration Doc ILLA Cloud
desc: Lær hvordan du kan koble Oppstash Redis databasen til ILLA Cloud og lage apper som kan spørre, manipulere og vise data
tagCategory: doc_menu_upstash_click
categoryName: 🔨 Integrasjoner
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Hva er Upstash?</h2>

Upstash er en skytjeneste med Redis kompatibel som gir en enkel måte å integrere Redis funksjonalitet i applikasjoner. I konteksten av ILLA Cloud, lar Upstash-integrasjonen brukerne bruke funksjoner og muligheter til Upstash i ILLA-skyen.

Denne veiledningen beskriver prosessen med å lage et Admin-panel ved hjelp av ILLA Builder og Upstash i noen få enkle steg. ILLA Cloud er en lavkode-plattform for utviklere som muliggjør rask utvikling og utplassering av interne verktøy. Det åpner for å opprette sider ved å dra og slippe UI komponenter, koble til database eller API, og skrive JavaScript. For å lære mer om Upstash, besøk nettsiden deres på https://upstash.com/(https://upstash.com/) La oss begynne!

### Sett opp en backend ved Oppstash

Opprett en konto ved Upstash. Det kan hende du vil logge på å bruke Amazon, Github eller Google kontoer.

Etter å ha logget inn på kontoen din på `Upstash`, klikk på `Opprett database` knappen i Redis.

![up_home](https://cdn.illacloud.com/official-website/img/docs/up_home.jpg)

På dashbordet `Upstash`, klikk `Opprett database`.

![up_create_db](https://cdn.illacloud.com/official-website/img/docs/up_create_db.jpg)

Merk: du kan kun ha ETT database med gratis avgift.

For å konfigurere databasen. Du kan navngi databasen din slik du ønsker. Her heter vi den `db1`. Velg den `primære regionen` og `les region` basert på din skriv og les brukerplasseringer. Aktiver `TSL/SSL`. Det er valgfritt å aktivere Eviction.

![up_db_config](https://cdn.illacloud.com/official-website/img/docs/up_db_config.jpg)

Klikk på \*\*`opprette`-knappen, vi har nå en database som er klar til bruk.

### Opprett upstash i ILLA Sky

Det er to måter å lage en ressurs i Illa etter å ha logget på din Illa-konto.

-   Opprett i ressurser

Logg inn på din Illa konto, velg `**Ressurser**` på toppen av siden, og klikk `**Opprett nye**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Velg **`Upstash`** fra databaselisten.

![action_list_0](https://cdn.illacloud.com/official-website/img/docs/action_list_0.jpg)

Koble til databasen med de nødvendige parametrene beskrevet i "Tilkoblingsinnstillinger" nedenfor.

Klikk `**Test oppkobling**` for å se om vi har lykkes i å koble til databasen. Hvis ja, klikk **`Lagre ressurser`**, eller dobbeltsjekk vertsnavn, port, brukernavn og passord er riktig.

Etter oppretting av en ressurs, den klare `Upstash` vil vises som vist.

![up_ex_ready](https://cdn.illacloud.com/official-website/img/docs/up_ex_ready.jpg)

-   Opprett i bygger

Logg på din Illa konto, opprett et prosjekt i Illa Builder på `**` siden, og naviger til **`Handlingslisten`** nederst på siden. Klikk `**new**`, og velg **`Upstash`** fra databaselisten. Deretter kan du koble til databasen med de nødvendige parametrene beskrevet i "Tilkoblingsinnstillinger" nedenfor.

Klikk `**Test oppkobling**` for å se om vi har lykkes i å koble til databasen. Hvis ja, klikk **`Lagre ressurser`**, eller dobbeltsjekk vertsnavn, port, brukernavn og passord er riktig.

### Tilkobling innstillinger

Her må vi gi informasjon for å koble til Upstash-databasen.

![up_config](https://cdn.illacloud.com/official-website/img/docs/up_config_rev.jpg)

| Egenskaper | Påkrevd      | Beskrivelse                                                                                                                 |
| ---------- | ------------ | --------------------------------------------------------------------------------------------------------------------------- |
| Navn       | obligatorisk | Navnet på ressursen når det skapes handlinger i ILLA.                                                                       |
| Hostname   | obligatorisk | URL-adressen eller IP-adressen for databasen din                                                                            |
| Port       | obligatorisk | Tjenerens portnummer som du bør bruke til å koble til. Hvis du ikke angir en port, er standard port '6379'.                 |
| Passord    | obligatorisk | Bruk dette passordet for godkjenning.                                                                                       |
| SSL valg   | obligatorisk | om å aktivere SSL (anbefale å aktivere)                                                                                     |

For å fylle ut påkrevde opplysninger gå til oversiktssiden for databasen og klikk på databasen vi har nettopp opprettet i Upstash.

![up_info](https://cdn.illacloud.com/official-website/img/docs/up_info.jpg)

Du kan kopiere den ved å hovre musen over endepunktet, passord og port og klikke på tekstikonet for kopier bak det.

I Upstash-integrasjonskonfigurasjonen i ILLA Cloud, setter du navnet du vil ha for `navn`, `Endepunkt` for `vertsnavn`, gitt `Port` for `Port`, og kopier det krypterte `passord` for `Password`.

![up_config_done](https://cdn.illacloud.com/official-website/img/docs/up_config_rev_done.jpg)

### Opprett handlinger

Vi har laget en Upstash-ressurs, vi kan legge til handlingen ved å velge Upstash fra handlingslisten og velge **`Create action`** knappen.

![up_resource_list](https://cdn.illacloud.com/official-website/img/docs/up_resource_list.jpg)

Nå har vi lagt til Upstash serveren som en handling på vår byggeside.

![upstash](https://cdn.illacloud.com/official-website/img/docs/upstash.jpg)

### Konfigurer oppslag

| Egenskaper     | Beskrivelse                                                             |
| -------------- | ----------------------------------------------------------------------- |
| Redis spørring | bruk spørringen for å hente og manipulere data lagret i Redis databasen |
| Transformator  | transformerer data til stilen du ønsker å bruke JavaScript              |

Eksempel bruk:

1.  Få verdier:
    -   Få en verdi: Bruk kommandoen **`GET`** for å hente verdien knyttet til en bestemt nøkkel. For eksempel: **`GET key`**.
2.  Angi verdier:
    -   Angi en verdi: Bruk kommandoen **`SET`** for å sette verdien til en nøkkel. For eksempel **`SET nøkkelverdi`**.
3.  Hash operasjoner:
    -   Angi feltverdi-par i en hash: Bruk **`HSET`** kommandoen for å sette et feltverdi-par i en hash. For eksempel: **`HSET hashKey feltverdi`**.
    -   Få en verdi fra en hash: Bruk **`HGET`** kommandoen for å hente verdien av et spesifikt felt i en hash. For eksempel: **`HGET hashKey felt`**.
4.  Liste operasjoner:
    -   Legg til et element i en liste: Bruk **`LPUSH`** eller **`RPUSH`** kommandoen for å legge til et element i begynnelsen eller slutten av en liste. For eksempel: **`LPUSH listenKey verdi `**.
    -   Henter elementer fra en liste: Bruk **`LRANGE`** kommandoen for å hente en rekke elementer fra en liste. For eksempel: **`LRANGE listenKey starter end`**.
5.  Angi operasjoner:
    -   Legg til elementer til et sett: Bruk **`SADD`"-kommandoen for å legge til ett eller flere elementer i et sett. For eksempel: **`SADD setKey verdi 1 verdi2`\*\*.
    -   Hent elementer fra et sett: Bruk kommandoen **`SMEMBERS`** for å få alle medlemmene i et sett. For eksempel: **`SMEMBERS setKey`**.
6.  Sorterte handlinger:
    -   Legg til elementer til et sortert sett: Bruk kommandoen **`ZADD`** for å legge til elementer til et sortert sett med sine poengsummer. For eksempel: **`ZADD sortedSetKey score1 member1 score2 member2`**.
    -   Hente elementer fra et sortert sett: Bruk **`ZRANGE`** kommandoen for å hente en rekke elementer fra et sortert sett basert på sine poengsummer. For eksempel: **`ZRANGE sortedSetKey start end`**.

Merk: Du kan referere til Redis dokumentasjon for en fullstendig liste over kommandoer og hvordan de brukes.

## Bruk kasse

Deretter vil vi vise hvordan man kartlegger dataene fra `**Upstash**` til komponenten `table`.

#### Trinn 1 Legg til en handling

La oss lage en tabell i Upstash kalt **`ansatte-profile`, ** inkludert 4 kolonner `navn`, `level`, `age`, og `married`.

Da kan vi opprette en ny handling for Upstash fra handlingslisten i ILLA Cloud og navnet upstash2.

For å liste opp alle data i ansatte-profilen har du satt koden under i delen Redis spørringer.

```sql
FÅ ansatte_profil
```

Siden utdataen er en liste over ordlister, ønsker vi å omdanne den til en liste. I Transformator, aktiver det og sett ned koden nedenfor.

```jsx
const jsonString = data[0].result;

// Parse JSON-strengen til et JavaScript-objekt
konst jsonData = JSON. arse(jsonString);

/ Få tilgang til egenskapen og kartlegge den til ny array
const mappedArray = jsonData. mployees.map(ansatte) => ({
  navn: ansatte.navn,
  nivå: ansatt. fjellferdighet
  alder: ansattefekteskap,
  ekteskap,
}),

retur mappedArray;
```

![up_code](https://cdn.illacloud.com/official-website/img/docs/up_code.jpg)

Klikk **`Lagre`** og **`Kjør`** for å aktivere denne handlingen.

![up_data](https://cdn.illacloud.com/official-website/img/docs/up_data.jpg)

#### Trinn 2 Legg til komponenter

Deretter kan vi legge til en \*\*`tabell`komponent til lerretet.

I feltet **`Datakild`** i Inspeksjonssiden etter å ha klikket på komponenten, satt `{{upstash2.data}}` for å hente dataene fra upstash.

#### Trinn 3 Prøving

Det endelige utseendet skal være som vist.

![up_test](https://cdn.illacloud.com/official-website/img/docs/up_test.jpg)
