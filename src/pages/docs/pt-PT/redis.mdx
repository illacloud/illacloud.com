---
title: Redis
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/347?languages=pt&filter=basic&value=0
metaTitle: Cód de Integração Redis ku ILLA Cloud
desc: Descubra como conectar e interagir com o banco de dados Redis usando o recurso de integração do banco de dados ILLA Cloud. Confira este guia para mais detalhes.
tagCategory: doc_menu_redis_click
categoryName: 🔨 Integrações
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>O que é Redis?</h2>

Redis in Illa é uma integração de banco de dados que lhe permite conectar e interagir com um banco de dados Redis. Redis é um popular sistema de gerenciamento de banco de dados de código aberto que é conhecido por sua velocidade, confiabilidade e facilidade de uso.

Com a integração do Redis no Illa, você pode consultar, inserir, atualizar e excluir dados de um banco de dados Redis usando comandos SQL. Você também pode criar consultas SQL personalizadas e executá-las dentro do Illa. Isso facilita a criação de aplicativos e painéis orientados a dados que exibem dados em tempo real de um banco de dados Redis.

### Criar Redis

Há duas maneiras de criar um recurso no Illa depois de entrar em sua conta do Illa.

-   Criar em recursos

Entre na sua conta Illa, selecione `**Recursos**` no topo da página, e clique no botão `**Criar Novo**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Selecione **`Redis`** da lista de banco de dados.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Conectar ao banco de dados com os parâmetros necessários descritos em `Configurações de Conexão` abaixo.

Clique em `**Test Connection**` para ver se conseguimos nos conectar com sucesso ao banco de dados. Se sim, clique **`Salvar Recursos`**, outra coisa, verifique se o nome do host, porta, nome de usuário e senha está correto.

Depois de criar um recurso, o Redis pronto será exibido como mostrado.

![redis_ex_ready](https://cdn.illacloud.com/official-website/img/docs/redis_ex_ready.png)

-   Criar no Construtor

Entre na sua conta do Illa, crie um projeto no Illa Builder na página `**App**`, e navegue para a **Lista de Ações\`** na parte inferior da página. Clique em `**novo**`, e então selecione **`Redis`** na lista de dados. Em seguida, conecte ao banco de dados com os parâmetros necessários descritos em `Configurações de Conexão` abaixo.

Clique em `**Test Connection**` para ver se conseguimos nos conectar com sucesso ao banco de dados. Se sim, clique **`Salvar Recursos`**, outra coisa, verifique se o nome do host, porta, nome de usuário e senha está correto.

### Configurações de conexão

Aqui precisamos fornecer informações para conexão com a base de dados Redis.

![redis_config](https://cdn.illacloud.com/official-website/img/docs/redis_config.png)

| propriedades        | Obrigatório | Descrição:                                                                                                                             |
| ------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Nome:               | Obrigatório | O nome do recurso ao criar ações na ILLA.                                                                                              |
| Hostname            | Obrigatório | O URL ou endereço IP do seu banco de dados                                                                                             |
| Porta               | Obrigatório | O número da porta do host do servidor que você deve usar para se conectar. Se você não especificar uma porta, a porta padrão é '6379'. |
| Índice de banco     | opcional    | uma estrutura de dados que é usada para otimizar o desempenho de consultas que recuperam dados de uma base de dados Redis              |
| Banco               | Obrigatório | O nome do banco de dados                                                                                                               |
| Usuário:            | Obrigatório | o nome de usuário que você deseja usar ao fazer login no servidor Redis.                                                               |
| Palavra-passe       | Obrigatório | Use esta senha para autenticação.                                                                                                      |
| Opções SSL          | opcional    | decide quão alta uma conexão SSL TCP/IP é priorizada durante as negociações com o servidor.                                            |

### Criar ações

Criamos um recurso do Redis, podemos adicionar a ação selecionando o Redis da lista de ações e escolhendo o botão **`Criar ação`**.

![redis_resource_list](https://cdn.illacloud.com/official-website/img/docs/redis_resource_list.png)

Agora nós adicionamos o servidor Redis como uma ação para a nossa página de construção.

![redis](https://cdn.illacloud.com/official-website/img/docs/redis.png)

### Configurar Redis

| propriedades   | Descrição:                                                         |
| -------------- | ------------------------------------------------------------------ |
| consulta       | comandos para recuperar dados do banco de dados                    |
| Transformador  | Transformar dados em estilo que você gosta de usar JavaScript      |

Exemplo de uso:

-   CHAVES

Este comando recupera uma lista de todas as chaves no banco de dados Redis. Você pode usar caracteres curinga para filtrar as chaves por padrão.

```sql
KEYS *
KEYS user:*
```

-   OBTER

Este comando recupera o valor associado a uma determinada chave.

```sql
GET user:12345
```

-   HGETALL

Este comando recupera todos os campos e seus valores de uma estrutura de dados hash do Redis.

```sql
HGETALL user:12345
```

-   ZRANGE

Este comando recupera uma variedade de valores a partir de uma estrutura de dados do Redis ordenada, com base em suas pontuações.

```sql
ZRANGE high_scores 0 9 WITHSCORES
```

-   GRANDES

Este comando recupera um intervalo de valores a partir de uma estrutura de dados da lista Redis.

```sql
LRANGE my_list 0 4
```

-   EXISTAS

Este comando verifica se uma determinada chave existe no banco de dados Redis.

```sql
EXISTS user: 12345
```

-   TTL

Este comando recupera o valor de tempo para-vivo (TTL) associado a uma chave, que especifica quanto tempo a chave permanecerá no banco de dados antes de expirar.

```sql
TTL user:12345
```
