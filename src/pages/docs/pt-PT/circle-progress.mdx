---
title: Progresso do círculo
metaTitle: Circle progress Component Doc Cloud ILLA
desc: Aprenda a usar o componente de progresso circular para personalizar e decorar seu aplicativo na ILLA Cloud.
tagCategory: doc_menu_circle_progress_click
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/391?languages=pt&filter=basic&value=0
categoryName: 🧬 Montar componentes
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>O que é progresso no círculo?</h2>

O componente de Progresso Círculo é um elemento visual usado para exibir o progresso ou o status de conclusão em um formato circular. É frequentemente utilizada para representar o progresso de tarefas, projectos ou qualquer outro valor mensurável.

### propriedades

| propriedades            | Descrição:                                                                                                                             |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Valor                   | o valor percentual representa o progresso                                                                                              |
| Ocultar rótulo do valor | se o rótulo de valor está oculto                                                                                                       |
| Dica                    | Os usuários podem inserir a dica do componente aqui. A dica será exibida quando estiver focada. O formato Markdown é suportado.        |
| Hidden                  | Controle dinamicamente se o componente está oculto. Você pode alterar o status oculto através de um valor booleano dinâmico.           |
| Alinhamento             | Definir o alinhamento (alinhar com a esquerda ou direita) da etiqueta                                                                  |
| Cor do Tema             | Permite aos usuários especificar a cor e a opacidade do fundo do botão                                                                 |
| Styles                  | a largura do traço do círculo representa o progresso                                                                                   |

### Método

Você pode usar outros componentes para controlar o componente. Apoiamos os seguintes dois métodos:

-   **definirValor**

Para definir o valor de entrada da área de texto, por exemplo, {{"value1"}}

| propriedades | Descrição:  |
| ------------ | ----------- |
| Valor        | Input value |

-   **ValorDeLimite**

Para limpar o valor do componente selecionado

### Dado

O componente tem alguns dados comumente usados, que podem ser chamados via **`{{componentName.propertyName}}`** no aplicativo.

| Nome da propriedade | Descrição:                                                              |
| ------------------- | ----------------------------------------------------------------------- |
| alinhamento         | controle a posição do círculo de progresso                              |
| cor                 | cor do círculo                                                          |
| mostrarNome         | o nome deste componente (iie button1)                                   |
| hidden              | status oculto (verdadeiro ou falso)                                     |
| largura-traço       | largura do traço circular                                               |
| dicaTexto           | valor da dica de texto                                                  |
| trailColor          | cor da trilha ou a parte não preenchida do progresso do círculo         |
| Valor               | valor percentual do progresso                                           |

Exemplo: {{circleProgress1.value}}

### Usar caso

Demonstraremos como fazer um formulário com um componente de progresso circular mostrando o progresso da conclusão do formulário.

#### Componentes Adicionais da Etapa 1

Adicione um componente **`form`** com o texto padrão "Formulário" e um botão "Enviar" a Canvas.

Adicione um componente **`input`**, um componente **`number input`** e um componente **`circle progress`** de `**Insert**` no formulário.

Rotule o componente de entrada para ser "Nome", o componente de entrada de números a ser "Idade" e o componente barra de progresso a ser "Progresso".

Defina o espaço reservado do componente de entrada para "Por favor, coloque seu nome aqui" e o valor padrão da entrada de número para 0 como mostrado abaixo.

![cir_layout](https://cdn.illacloud.com/official-website/img/official-site/components/cir_layout.jpg)

#### Passo 2 configurar o componente

Para os componentes **`input`** e **`number input`**, podemos configurá-los para definir o valor no componente **`circle progress`** para refletir o progresso da conclusão.

1.  Clique no componente **`input`** e **`number input`** para abrir sua página de inspeção. Em **`Event handler`**, clique em `**+ New**`.
2.  No manipulador de Editar evento, selecione **`componente de controle`** em ação, e selecione o componente **`circle progress`** que você deseja atualizar como o alvo do evento.
3.  Escolha a ação **`setValue`** e defina o valor como

```jsx
{
  {
    input1.value && numberInput1.value
      ? "100"
      : input1.value || numberInput1.value
      ? "50"
      : "0";
  }
}
```

![cir_event_config](https://cdn.illacloud.com/official-website/img/official-site/components/cir_event_config.jpg)

Nota: é necessário definir o manipulador de eventos como descrito acima para entradas BOTH e componentes de entrada numérica.

Para o componente **`button`**, podemos configurá-lo para enviar o formulário se o formulário estiver concluído (por exemplo, círculo progresso 100%)

1.  Clique no componente **`button`** para abrir a página de inspeção. Em **`Event handler`**, clique em `**+ New**`.
2.  No manipulador de Editar evento, selecione **`componente de controle`** em ação, e selecione o componente **`form`** que você deseja atualizar como o alvo do evento.
3.  Escolha o método **`submit`** e coloque `{{circleProgress1.value == 100}}` em **`Somente execute quando`**

![cir_sub_config](https://cdn.illacloud.com/official-website/img/official-site/components/cir_sub_config.jpg)

Da mesma forma, podemos configurar o botão para mostrar o aviso quando o usuário estiver tentando enviar um formulário não concluído.

1.  Clique no componente **`button`** para abrir a página de inspeção. Em **`Event handler`**, clique em `**+ New**`.
2.  No manipulador de edição de eventos, selecione **`Mostrar notificação`** na ação, **`título`** "Erro!" e **`descrição`** "Por favor, preencha o formulário antes de enviar". Escolha `Error` para **`type`**, coloque `{{2000}}` para o **`duration`** (2 segundos), e coloque `{{circleProgress1.value<100}}` para **`Somente execute quando`.**

![cir_err_config](https://cdn.illacloud.com/official-website/img/official-site/components/cir_err_config.jpg)

Também podemos configurar o componente `**Form**` para mostrar uma mensagem de sucesso quando enviado.

1.  Clique no componente **`form`** para abrir a página de inspeção. Em **`Event handler`**, clique em `**+ New**`.
2.  No manipulador de eventos de edição, selecione **`Mostrar notificação`** na ação, **`título`** "Enviado com Sucesso". Escolha `Success` para **`type`**, coloque `{{2000}}` para o **`duration`** (2 segundos).

![cir_form_submit](https://cdn.illacloud.com/official-website/img/official-site/components/cir_form_submiy.jpg)

#### Teste de Passo 4

![cir_test](https://cdn.illacloud.com/official-website/img/official-site/components/cir_test.gif)
