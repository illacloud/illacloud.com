---
title: Microsoft SQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/365?languages=pt&filter=basic&value=0
metaTitle: Microsoft SQL Integration Doc ├ILLA Cloud
desc: Aprenda a usar ILLA Cloud para acessar e trabalhar com os dados de Microsoft SQL. Construa apps, visualize dados e automatize fluxos de trabalho com ILLA Cloud.
tagCategory: doc_menu_microsoft_sql_click
categoryName: 🔨 Integrações
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>O que é Microsoft SQL?</h2>

O Microsoft SQL no Illa é uma integração de banco de dados que permite aos usuários se conectar e interagir com um banco de dados do Microsoft SQL (MS SQL). O MS SQL é um popular sistema de gerenciamento de banco de dados de código aberto que é conhecido por sua velocidade, confiabilidade e facilidade de uso.

Com a integração do MS SQL no Illa, você pode consultar, inserir, atualizar e excluir dados de um banco de dados MS SQL usando comandos SQL. Você também pode criar consultas SQL personalizadas e executá-las dentro do Illa. Isto facilita a criação de aplicativos e painéis orientados a dados que exibem dados em tempo real de um banco de dados MS SQL.

### Criar SQL no MS

Há duas maneiras de criar um recurso no Illa depois de entrar em sua conta do Illa.

-   Criar em recursos

Entre na sua conta Illa, selecione `**Recursos**` no topo da página, e clique no botão `**Criar Novo**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Selecione **`Microsoft SQL`** na lista de dados.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Conectar ao banco de dados com os parâmetros necessários descritos em `Configurações de Conexão` abaixo.

Clique em `**Test Connection**` para ver se conseguimos nos conectar com sucesso ao banco de dados. Se sim, clique **`Salvar Recursos`**, outra coisa, verifique se o nome do host, porta, nome de usuário e senha está correto.

Depois de criar um recurso, o MS SQL pronto será exibido como mostrado.

![ms_ex_ready](https://cdn.illacloud.com/official-website/img/docs/ms_ex_ready.png)

-   Criar no Construtor

Entre na sua conta do Illa, crie um projeto no Illa Builder na página `**App**`, e navegue para a **Lista de Ações\`** na parte inferior da página. Clique em `**novo**`, e então selecione **`Microsoft SQL`** na lista de dados. Em seguida, conecte ao banco de dados com os parâmetros necessários descritos em `Configurações de Conexão` abaixo.

Clique em `**Test Connection**` para ver se conseguimos nos conectar com sucesso ao banco de dados. Se sim, clique **`Salvar Recursos`**, outra coisa, verifique se o nome do host, porta, nome de usuário e senha está correto.

### Configurações de conexão

Aqui precisamos fornecer informações para conectar ao MS SQL banco de dados.

![ms_config](https://cdn.illacloud.com/official-website/img/docs/ms_config.png)

| propriedades           | Obrigatório | Descrição:                                                                                                                           |
| ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Nome:                  | Obrigatório | O nome do recurso ao criar ações na ILLA.                                                                                            |
| Hostname               | Obrigatório | O URL ou endereço IP do seu banco de dados                                                                                           |
| Porta                  | Obrigatório | O número da porta do host do servidor que você deve usar para se conectar. Se você não especificar uma porta, porta padrão é '1433'. |
| Banco                  | Obrigatório | O nome do banco de dados                                                                                                             |
| Usuário:               | Obrigatório | o nome de usuário que você deseja usar ao fazer login no servidor MS SQL.                                                            |
| Palavra-passe          | Obrigatório | Use esta senha para autenticação.                                                                                                    |
| Opção de conexão       | opcional    | par chave-valor para especificar a opção de conexão                                                                                  |
| Opções SSL             | opcional    | decide quão alta uma conexão SSL TCP/IP é priorizada durante as negociações com o servidor.                                          |

### Criar ações

Criamos um recurso MS SQL; podemos adicionar a ação selecionando MS SQL da lista de ações e escolhendo o botão **`Criar ação`**.

![ms_resource_list](https://cdn.illacloud.com/official-website/img/docs/ms_resource_list.png)

Agora nós adicionamos o MS SQL como ação à nossa página de construção.

![ms](https://cdn.illacloud.com/official-website/img/docs/ms.png)

### Configurar o MS SQL

| propriedades         | Descrição:                                                                                                                                                                                                                                                                               |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tipo de Configuração | o tipo de configuração que você usa para conectar ao seu banco de dados do MS SQL Server: SQL e Bulk inserido. Para o modo SQL, você pode usar a consulta SQL. Para o modo de inserção em massa, é possível inserir dados em uma tabela selecionada com o formato {{ [{x:a, y:b}, … ] }} |
| Consulta SQL         | Comandos sql para buscar (seleção), inserir, atualizar, excluir dados do banco de dados.                                                                                                                                                                                                 |
| Transformador        | Transformar dados em estilo que você gosta de usar JavaScript                                                                                                                                                                                                                            |

Exemplo de uso:

-   Selecionar

```sql
SELECT * FROM users ORDER BY lastname;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   Atualização

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   excluir

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
