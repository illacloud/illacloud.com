---
title: Upstash
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/427?languages=da&filter=basic&value=0
metaTitle: Upstash Integration Doc ● ILLA Cloud
desc: Lær at forbinde din Upstash Redis database til ILLA Cloud og oprette apps, der kan spørge, manipulere og vise data
tagCategory: doc_menu_upstash_click
categoryName: 🔨 Integrationer
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Hvad er Upstash?</h2>

Upstash er en cloud-native serverless service med Redis kompatibel, der giver en nem måde at integrere Redis funktionalitet i applikationer. I forbindelse med ILLA Cloud giver Upstash-integrationen brugerne mulighed for at udnytte funktionerne og funktionerne i Upstash i ILLA Cloud-applikationen.

Denne tutorial skitserer processen med at oprette en Admin Panel ved hjælp af ILLA Builder og Upstash i et par enkle trin. ILLA Cloud er en lav-kode-platform for udviklere, der muliggør hurtig udvikling og implementering af interne værktøjer. Det giver mulighed for at oprette sider ved at trække og slippe UI komponenter, forbinde til enhver database eller API, og skrive JavaScript. Hvis du vil vide mere om Upstash, kan du besøge deres hjemmeside på https://upstash.com/(https://upstash.com/) Lad os begynde!

### Konfigurer din Back end på Upstash

Opret en konto på Upstash. Du kan logge ind med din Amazon, Github eller Google-konto alternativt.

Efter du har logget ind på din konto på `Upstash`, skal du klikke på `Opret database` knappen i Redis.

![up_home](https://cdn.illacloud.com/official-website/img/docs/up_home.jpg)

Klik på `Upstash` betjeningspanel, på `Opret database`.

![up_create_db](https://cdn.illacloud.com/official-website/img/docs/up_create_db.jpg)

Bemærk: du kan kun have en database i det frie niveau.

For database konfiguration, Du kan navngive din database på den måde, du ønsker. Her hedder det `db1`. Vælg den primære region`og`read region`baseret på din skrive og læse bruger steder. Aktiver`TSL/SSL\`. Det er valgfrit at aktivere udsættelse.

![up_db_config](https://cdn.illacloud.com/official-website/img/docs/up_db_config.jpg)

Klik på **\`opret' **-knappen, vi har nu en database klar til brug.

### Opret Upstash i ILLA Cloud

Der er to måder at oprette en ressource på i Illa efter at have logget ind på din Illa-konto.

-   Opret i ressourcer

Log ind på din Illa-konto, vælg `**Ressourcer**` øverst på siden, og klik på `**Opret ny**` knappen.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Vælg **`Upstash`** fra databaselisten.

![action_list_0](https://cdn.illacloud.com/official-website/img/docs/action_list_0.jpg)

Opret forbindelse til databasen med de krævede parametre, der er beskrevet i 'Forbindelsesindstillinger' nedenfor.

Klik på `**Test Connection**` for at se, om vi kan oprette forbindelse til databasen. Hvis ja, klik på **`Gem ressourcer`**, ellers dobbelttjek værtsnavn, port, brugernavn og adgangskode er korrekt.

Når du har oprettet en ressource, vil den klar `Upstash` blive vist som vist.

![up_ex_ready](https://cdn.illacloud.com/official-website/img/docs/up_ex_ready.jpg)

-   Opret i Builder

Log ind på din Illa-konto, opret et projekt i Illa Builder på siden `**App**` og navigere til **`Action List`** nederst på siden. Klik på `**ny**`, vælg derefter **`Upstash`** fra databaselisten. Opret derefter forbindelse til databasen med de nødvendige parametre, der er beskrevet i 'Forbindelsesindstillinger' nedenfor.

Klik på `**Test Connection**` for at se, om vi kan oprette forbindelse til databasen. Hvis ja, klik på **`Gem ressourcer`**, ellers dobbelttjek værtsnavn, port, brugernavn og adgangskode er korrekt.

### Forbindelsesindstillinger

Her skal vi give oplysninger til at oprette forbindelse til Upstash-databasen.

![up_config](https://cdn.illacloud.com/official-website/img/docs/up_config_rev.jpg)

| Egenskaber      | Påkrævet | Varebeskrivelse                                                                                                        |
| --------------- | -------- | ---------------------------------------------------------------------------------------------------------------------- |
| Navn            | påkrævet | Navnet på ressource når der oprettes handlinger i ILLA.                                                                |
| Hostname        | påkrævet | URL- eller IP-adressen for din database                                                                                |
| Port            | påkrævet | Serveren vært portnummer, som du skal bruge til at forbinde. Hvis du ikke angiver en port, er standardporten '6379'.   |
| Adgangskode     | påkrævet | Brug denne adgangskode til godkendelse.                                                                                |
| SSL-indstilling | påkrævet | om SSL skal aktiveres (anbefales at aktivere)                                                                          |

For at udfylde de krævede oplysninger gå til databasen dashboard side og klik på den database, vi lige har oprettet i din Upstash.

![up_info](https://cdn.illacloud.com/official-website/img/docs/up_info.jpg)

Du kan kopiere det ved at svæve musen over endepunktet, adgangskode og port og klikke på kopien tekst ikonet bag det.

I konfigurationen Upstash integration i ILLA Cloud, sætte det navn, du ønsker for `Name`, `Endpoint` for `værtsnavn`, givet `Port` for `Port`, og kopiere den krypterede `Adgangskode` for `Adgangskode`.

![up_config_done](https://cdn.illacloud.com/official-website/img/docs/up_config_rev_done.jpg)

### Opret Handlinger

Vi har oprettet en Upstash ressource, vi kan tilføje handlingen ved at vælge Upstash fra handlingslisten og vælge **`Opret handling`**-knappen.

![up_resource_list](https://cdn.illacloud.com/official-website/img/docs/up_resource_list.jpg)

Nu har vi tilføjet Upstash serveren som en handling til vores byggeside.

![upstash](https://cdn.illacloud.com/official-website/img/docs/upstash.jpg)

### Indstil Upstash

| Egenskaber         | Varebeskrivelse                                                             |
| ------------------ | --------------------------------------------------------------------------- |
| Redis forespørgsel | brug forespørgsel til at hente og manipulere data gemt i din Redis database |
| Transformator      | omdanne data til den stil, du ønsker at bruge JavaScript                    |

Eksempel på anvendelse:

1.  Hent Værdier:
    -   Få en værdi: Brug kommandoen **`GET`** til at hente den værdi, der er forbundet med en bestemt nøgle. For eksempel: **`GET nøgle`**.
2.  Sæt Værdier:
    -   Sæt en værdi: Brug kommandoen **`SET`** til at indstille værdien af en nøgle. For eksempel **`SET nøgleværdi`**.
3.  Hash Operationer:
    -   Sæt feltværdi par i en hash: Brug **`HSET`**-kommandoen til at indstille et feltværdipar i en hash. For eksempel: **`HSET hashKey felt værdi`**.
    -   Få en værdi fra en hash: Brug kommandoen **`HGET`** til at hente værdien af et bestemt felt i en hash. For eksempel: **`HGET hashKey felt`**.
4.  List Operationer:
    -   Tilføj et element til en liste: Brug kommandoen **`LPUSH`** eller **`RPUSH`** for at tilføje et element til henholdsvis begyndelsen eller slutningen af en liste. For eksempel: **`LPUSH listNøgleværdi`**.
    -   Hent elementer fra en liste: Brug kommandoen **`LRANGE`** til at hente en række elementer fra en liste. For eksempel: **`LRANGE listKey start end`**.
5.  Indstil Operationer:
    -   Tilføj elementer til et sæt: Brug kommandoen **`SADD`** for at tilføje et eller flere elementer til et sæt. For eksempel: **`SADD setKey value1 value2`**.
    -   Hent elementer fra et sæt: Brug kommandoen **`SMEMBERS`** for at få alle medlemmer af et sæt. For eksempel: **`SMEMBERS setKey`**.
6.  Sorterede Sæt Operationer:
    -   Tilføj elementer til et sorteret sæt: Brug kommandoen **`ZADD`** for at tilføje elementer til et sorteret sæt sammen med deres score. For eksempel: **`ZADD sortedSetKey score1 member1 score2 member2`**.
    -   Hent elementer fra et sorteret sæt: Brug kommandoen **`ZRANGE`** til at hente en række elementer fra et sorteret sæt baseret på deres score. For eksempel: **`ZRANGE sortedSetKey start end`**.

Bemærk: Du kan henvise til Redis dokumentationen for en fuldt omfattende liste af kommandoer og deres brug.

## Brug case

Dernæst vil vi vise, hvordan man kortlægger data fra `**Upstash**` til `tabel`-komponenten.

#### Trin 1 Tilføj en handling

Lad os oprette en tabel i Upstash kaldet **`employee_profile`,** herunder 4 kolonner `name`, `level`, `age`, og `ægte`.

Så kan vi oprette en ny handling til Upstash fra handlingslisten i ILLA Cloud og navngive upstash2.

For at liste alle data i medarbejder_profile sættes koden snippet nedenfor i afsnittet Redis forespørgsel.

```sql
GET medarbejder_profil
```

Da output data er en liste over ordbog par, vi ønsker at omdanne det til et array. I Transformer, aktivere det og sætte ned koden nedenfor.

```jsx
const jsonString = data[0].result;

// Parse the JSON string into a JavaScript object
const jsonData = JSON.parse(jsonString);

// Access the "employees" property and map it to a new array
const mappedArray = jsonData.employees.map((employee) => ({
  name: employee.name,
  level: employee.level,
  age: employee.age,
  married: employee.married,
}));

return mappedArray;
```

![up_code](https://cdn.illacloud.com/official-website/img/docs/up_code.jpg)

Klik på **`Gem`** og **`Kør`** for at aktivere denne handling.

![up_data](https://cdn.illacloud.com/official-website/img/docs/up_data.jpg)

#### Trin 2 Tilføj Komponenter

Dernæst kan vi tilføje en **`tabel`** komponent til lærredet.

I feltet **`Datakilde`** på siden Inspect efter at have klikket på komponenten, sættes `{{upstash2.data}}` for at hente data fra upstash.

#### Trin 3 Test

Det endelige udseende skal være som vist.

![up_test](https://cdn.illacloud.com/official-website/img/docs/up_test.jpg)
