---
title: Redis
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/347?languages=da&filter=basic&value=0
metaTitle: Redis Integration Doc ● ILLA Cloud
desc: Opdag, hvordan du forbinder og interagerer med din Redis-database ved hjælp af ILLA Clouds databaseintegrationsfunktion. Tjek denne guide for flere detaljer.
tagCategory: doc_menu_redis_click
categoryName: 🔨 Integrationer
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Hvad er Redis?</h2>

Redis i Illa er en databaseintegration, der giver dig mulighed for at forbinde og interagere med en Redis database. Redis er en populær open source relationelle database management system, der er kendt for sin hastighed, pålidelighed og brugervenlighed.

Med Redis integration i Illa kan du spørge, indsætte, opdatere og slette data fra en Redis database ved hjælp af SQL-kommandoer. Du kan også oprette brugerdefinerede SQL forespørgsler og køre dem i Illa. Dette gør det nemt at opbygge data-drevne applikationer og dashboards, der viser realtidsdata fra en Redis-database.

### Opret Redis

Der er to måder at oprette en ressource på i Illa efter at have logget ind på din Illa-konto.

-   Opret i ressourcer

Log ind på din Illa-konto, vælg `**Ressourcer**` øverst på siden, og klik på `**Opret ny**` knappen.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Vælg **`Redis`** fra databaselisten.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Opret forbindelse til databasen med de krævede parametre, der er beskrevet i 'Forbindelsesindstillinger' nedenfor.

Klik på `**Test Connection**` for at se, om vi kan oprette forbindelse til databasen. Hvis ja, klik på **`Gem ressourcer`**, ellers dobbelttjek værtsnavn, port, brugernavn og adgangskode er korrekt.

Når du har oprettet en ressource, vil den klar Redis blive vist som vist.

![redis_ex_ready](https://cdn.illacloud.com/official-website/img/docs/redis_ex_ready.png)

-   Opret i Builder

Log ind på din Illa-konto, opret et projekt i Illa Builder på siden '**App**', og navigere til **`Action List`** nederst på siden. Klik på `**ny**`, vælg derefter **`Redis`** fra databaselisten. Opret derefter forbindelse til databasen med de nødvendige parametre, der er beskrevet i 'Forbindelsesindstillinger' nedenfor.

Klik på `**Test Connection**` for at se, om vi kan oprette forbindelse til databasen. Hvis ja, klik på **`Gem ressourcer`**, ellers dobbelttjek værtsnavn, port, brugernavn og adgangskode er korrekt.

### Forbindelsesindstillinger

Her er vi nødt til at give oplysninger for tilslutning til Redis-databasen.

![redis_config](https://cdn.illacloud.com/official-website/img/docs/redis_config.png)

| Egenskaber           | Påkrævet | Varebeskrivelse                                                                                                              |
| -------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------- |
| Navn                 | påkrævet | Navnet på ressource når der oprettes handlinger i ILLA.                                                                      |
| Hostname             | påkrævet | URL- eller IP-adressen for din database                                                                                      |
| Port                 | påkrævet | Serveren vært portnummer, som du skal bruge til at forbinde. Hvis du ikke angiver en port, er standardporten '6379'.         |
| Database Indeks      | valgfri  | en datastruktur, der bruges til at optimere ydeevnen af forespørgsler, der hente data fra en Redis database                  |
| Database             | påkrævet | Navnet på databasen                                                                                                          |
| Brugernavn           | påkrævet | det brugernavn du ønsker at bruge, når du logger ind på Redis-serveren.                                                      |
| Adgangskode          | påkrævet | Brug denne adgangskode til godkendelse.                                                                                      |
| SSL-indstillinger    | valgfri  | beslutte, hvor højt en sikker SSL TCP/IP-forbindelse prioriteres under forhandlinger med serveren.                           |

### Opret Handlinger

Vi har oprettet en Redis-ressource, vi kan tilføje handlingen ved at vælge Redis fra handlingslisten og vælge **`Opret handling`**-knappen.

![redis_resource_list](https://cdn.illacloud.com/official-website/img/docs/redis_resource_list.png)

Nu har vi tilføjet Redis-serveren som en handling til vores byggeside.

![redis](https://cdn.illacloud.com/official-website/img/docs/redis.png)

### Indstil Redis

| Egenskaber         | Varebeskrivelse                                                |
| ------------------ | -------------------------------------------------------------- |
| forespørgsel       | kommandoer til at hente data fra database                      |
| Transformator      | omdanne data til den stil, du ønsker at bruge JavaScript       |

Eksempel på anvendelse:

-   NØGLER

Denne kommando henter en liste over alle nøgler i Redis databasen. Du kan bruge jokertegn til at filtrere tasterne efter mønster.

```sql
KEYS *
KEYS user:*
```

-   FÅ

Denne kommando henter værdien associeret med en given nøgle.

```sql
GET user:12345
```

-   HGETALL

Denne kommando henter alle felter og deres værdier fra en Redis hash-datastruktur.

```sql
HGETALL user:12345
```

-   ZRANGE

Denne kommando henter en række værdier fra en Redis sorteret sæt datastruktur, baseret på deres score.

```sql
ZRANGE high_scores 0 9 WITHSCORES
```

-   LRÆNGDE

Denne kommando henter en række værdier fra en Redis liste datastruktur.

```sql
LRANGE my_list 0 4
```

-   EKSISTER

Denne kommando kontrollerer om en given nøgle findes i Redis-databasen.

```sql
EXISTS user:12345
```

-   TTL

Denne kommando henter den tid til live (TTL) værdi forbundet med en nøgle, som angiver, hvor længe nøglen vil forblive i databasen, før den udløber.

```sql
TTL user:12345
```
