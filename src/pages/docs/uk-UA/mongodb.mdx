---
title: MongoDB
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/351?languages=uk&filter=basic&value=0
metaTitle: Інтеграція MongoDB | ILLA Cloud
desc: Дізнайся, як використовувати Cloud ILLA для створення додатків з MongoDB. Дослідіть дані запиту, маніпулювання та особливості візуалізації.
tagCategory: doc_menu_mongodb_click
categoryName: 🔨 Інтеграція
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Що таке MongoDB?</h2>

MongoDB - це популярна база даних, орієнтована на документ NoSQL, яка широко використовується для веб-застосунків та інших програм, керованих даними. MongoDB розроблена так, щоб бути гнучким, масштабованим та швидким і особливо добре підходить для обробки великих обсягів неструктурованих або напівструктурованих даних.

У Іллі Ви можете підключитися до бази даних MongoDB використовуючи ресурси на MongoDB. Цей ресурс дозволяє виконувати резервні запити MongoDB безпосередньо з Illa, не маючи необхідності писати будь-який код. Ви можете використовувати редактор запитів для запису запитів у рідній мові MongoDB, який базується на JavaScript і використовує JSON-подібний синтаксис для роботи з документами.

### Create MongoDB

Існує два способи створити ресурс у Іллі після входу до облікового запису Illa в обліковий запис.

-   Створити в ресурсах

Увійдіть до свого рахунку Ілла, оберіть **Ресурси**`на верху сторінки та натисніть кнопку`**Створити нове**\`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Оберіть **Монголь\`** зі списку бази даних.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Підключіться до бази даних з необхідними параметрами, описаними в меню `Параметри підключення` нижче.

Натисніть `**Test Connection**`, щоб перевірити, чи можна успішно підключитися до бази даних. Якщо так, натисніть **Зберегти ресурси\`**, ще, подвоїти ім'я хоста, ім'я користувача та пароль правильні.

Після створення ресурсу, готовий MongoDB буде відображатися як показаний.

![mongo_ex_ready](https://cdn.illacloud.com/official-website/img/docs/mongo_ex_ready.png)

-   Створити в будівельнику

Увійдіть до свого облікового запису Ілла, створіть проект в конструкторі Ila в сторінці `**App**` і навігація до **`Списку дій`** в нижній частині сторінки. Натисніть `**новий**`, потім оберіть **'Монголь' ** зі списку бази даних. Далі підключіться до бази даних з необхідними параметрами, описаними в меню `Параметри підключення` нижче.

Натисніть `**Test Connection**`, щоб перевірити, чи можна успішно підключитися до бази даних. Якщо так, натисніть **Зберегти ресурси\`**, ще, подвоїти ім'я хоста, ім'я користувача та пароль правильні.

### Параметри підключення

Тут нам потрібна інформація для підключення до бази даних MongoDB.

![mongo_config](https://cdn.illacloud.com/official-website/img/docs/mongo_config.png)

| Властивості               | Обов'язково   | Опис                                                                                                                             |
| ------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Ім'я                      | обов’язково   | Назва ресурсу при створенні дій в ILLA.                                                                                          |
| Тип конфігурації          | необов'язково | тип колекції, що використовується для зберігання даних конфігурації для фрагментів.                                              |
| Hostname                  | обов’язково   | URL або IP-адреса для вашої бази даних                                                                                           |
| Формат зв’язку            | обов’язково   | синтаксис, який використовується для вказання рядка підключення до бази даних MongoDB або кластера.                              |
| Порт                      | обов’язково   | Номер порта сервера, який ви повинні використовувати для підключення. Якщо не вказано порт, то порт за замовчуванням має '3306'. |
| База даних                | обов’язково   | Ім'я бази даних                                                                                                                  |
| Ім'я користувача          | обов’язково   | ім'я користувача, яке ви хочете використовувати при вході в MongoDB сервер.                                                      |
| Пароль                    | обов’язково   | Використовуйте цей пароль для автентифікації.                                                                                    |
| Опції SSL                 | необов'язково | вирішує, наскільки високо захищене SSL TCP/IP підключення пріоритетне при узгодженні з сервером.                                 |

### Створити дії

Ми створили ресурс MongoDB, ми можемо додати дію, вибравши MongoDB зі списку дій і вибравши кнопку **`Створити дію`** .

![mongo_resource_list](https://cdn.illacloud.com/official-website/img/docs/mongo_resource_list.png)

Тепер ми додали сервер MongoDB як дію до нашої сторінки будівництва.

![mongodb](https://cdn.illacloud.com/official-website/img/docs/mongodb.png)

### Configure MongoDB

#### Перегляд попереднього контакту

| Назва методу  | Опис                                                                                |
| ------------- | ----------------------------------------------------------------------------------- |
| Тип дії       | агрегатний, булкер, рахунок, deleteMan. deleteOne, чіткий, findOne                  |
| Колекція      | група пов'язаних документів, які зберігаються в базі даних                          |
| Трансформатор | перетворення даних в бажаний вами стиль з використанням JavaScript                  |

#### Агрегат

обробити декілька документів та повернути обчислені результати.

** Input **

| Властивості  | Обов'язково   | Опис                                        |
| ------------ | ------------- | ------------------------------------------- |
| Агрегація    | необов'язково | визначає операції для виконання даних       |

Наприклад, щоб отримати всі результати, розмір яких дорівнює середньому=>групі результати за типом=>Розрахуйте суму цін для **`Агрегації`**:

```sql
[
	{
		"$match": { "size": "medium" }
	},
	{
		"$group": { "_id": "$type", "totalQuantity": { "$sum": "$price" } }
	}
]
```

** Вивід **

Масив обчислених результатів.

![mongo_agg_data](https://cdn.illacloud.com/official-website/img/docs/mongo_agg_data.png)

Використовуйте `{{mongodb1.data[0].result}}`, щоб знайти всіх.

![mongo_agg_code](https://cdn.illacloud.com/official-website/img/docs/mongo_agg_code.png)

#### Архівувати

виконати кілька операцій запису (вставити, оновити та видалити) за єдиним запитом на сервер

** Input **

| Властивості | Обов'язково   | Опис                                                                          |
| ----------- | ------------- | ----------------------------------------------------------------------------- |
| Операції    | необов'язково | об’єкт, який визначає тип операції для виконання та дані для запису           |

Приклад для **`Операцій`**:

```sql
[
	{ "insertOne": { "document": { "_id": 3, "type": "beef", "size": "medium", "price": 6 } } },
	{ "insertOne": { "document": { "_id": 4, "type": "sausage", "size": "large", "price": 10 } } },
	{ "updateOne": {
			"filter": { "type": "cheese" },
			"update": { "$set": { "price": 8 } }
		}
	}
]
```

** Вивід **

Нижче наведено оновлене повідомлення.

![mongo_bulk_data](https://cdn.illacloud.com/official-website/img/docs/mongo_bulk_data.png)

Оскільки цей метод вносить зміни, але нічого не повертає. Немає вихідних даних для доступу.

#### Підрахунок

порахувати кількість документів, які відповідають заданому запиту в колекції

** Input **

| Властивості | Обов'язково   | Опис                                                |
| ----------- | ------------- | --------------------------------------------------- |
| Запит       | необов'язково | визначає критерії вибору для полігональної операції |

Наприклад, щоб порахувати кількість товару з типом "яловичина", ми розмістили нижче код в **"Query"**:

```sql
{"type":"beef"}
```

** Вивід **

Кількість відповідних документів.

![mongo_count_data](https://cdn.illacloud.com/official-website/img/docs/mongo_count_data.png)

Використай `{{mongodb1.data}}`, щоб отримати його.

![mongo_count_code](https://cdn.illacloud.com/official-website/img/docs/mongo_count_code.png)

#### видалення з

видалити кілька документів, які відповідають заданому фільтру в колекції.

** Input **

| Властивості | Обов'язково   | Опис                                                                                                                    |
| ----------- | ------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Фільтр      | необов'язково | визначає критерії вибору для операції видалення. Якщо не вказані, усі документи у цій колекції будуть видалені.         |

Наприклад, щоб видалити яблука з елементів. Ми можемо поставити код нижче до **`Filter`**.

```sql
 {"type":"apple"}
```

** Вивід **

Кількість відповідних документів.

![mongo_delete2_data](https://cdn.illacloud.com/official-website/img/docs/mongo_delete2_data.png)

Використай `{{mongodb1.data[0].result.DeletedCount}}`, щоб отримати його. Якщо ніякі файли не збігаються, повернене значення буде 0.

![mongo_delete2_code](https://cdn.illacloud.com/official-website/img/docs/mongo_delete2_code.png)

#### делетеон

видалити один документ, який відповідає заданому фільтру в колекції.

** Input **

| Властивості | Обов'язково   | Опис                                               |
| ----------- | ------------- | -------------------------------------------------- |
| Фільтр      | необов'язково | визначає критерії вибору для операції видалення    |

Наприклад, щоб видалити яблуко з товарів. Ми можемо поставити код нижче до **`Filter`**.

```sql
 {"type":"apple"}
```

** Вивід **

Видалити результат.

![mongo_delete1_data](https://cdn.illacloud.com/official-website/img/docs/mongo_delete1_data.png)

Використай `{{mongodb1.data[0].result.DeletedCount}}`, щоб отримати його. Якщо ніякі файли не збігаються, отримане значення буде 0 (оскільки ми видалили всі яблука в **\`deleteMany**, яблука тут немає, тому їх поверніть 0).

![mongo_delete1_code](https://cdn.illacloud.com/official-website/img/docs/mongo_delete1_code.png)

#### відмінний

отримати масив унікальних значень для певного поля в колекції

** Input **

| Властивості | Обов'язково   | Опис                                                 |
| ----------- | ------------- | ---------------------------------------------------- |
| Запит       | необов'язково | визначає критерії вибору для окремих операцій        |
| Поле        | обов’язково   | вказує на поле для отримання різних значень.         |

Наприклад, у нас може бути `{"type":"orange"}` для **`Query`** та `_id` для **`Поля`**.

** Вивід **

масив різних значень

![mongo_dist_data](https://cdn.illacloud.com/official-website/img/docs/mongo_dist_data.png)

Використайте `{{mongodb1.data[0].result.map(item =>({"result":item})}}` щоб отримати масив.

![mongo_dist_code](https://cdn.illacloud.com/official-website/img/docs/mongo_dist_code.png)

#### знайти

отримати документи з колекції, які відповідають вказаному набору критеріїв

** Input **

| Властивості      | Обов'язково   | Опис                                                                                                               |
| ---------------- | ------------- | ------------------------------------------------------------------------------------------------------------------ |
| Запит            | необов'язково | визначає критерії вибору для операції з пошуку                                                                     |
| Проекція         | необов'язково | вказує, які поля повинні містити або виключати в результатах запиту                                                |
| Сортувати за     | необов'язково | вказати порядок сортування документів, що повертаються                                                             |
| Ліміт            | необов'язково | обмежувати кількість повернених документів, не обмежувати кількість за замовчуванням. Межа 0 еквівалентна до межі. |
| Пропустити       | необов'язково | вкажіть кількість пропущених документів за замовчуванням 0.                                                        |

Наприклад, щоб знайти ідентифікатор, ціни і тип усіх апельсинів відсортовані за їх id.

Для **`Query`**, `{"type": "помаранчевий"}`

Для **`Проект`**, `{"_id": 1, "type": 1, "ціна": 1}`
Для **`Сортування`**, `{"_id":1}`

Примітка: замініть «1» на «true» все ще це робить!

** Вивід **

Масив об'єктів документів

![mongo_find_data](https://cdn.illacloud.com/official-website/img/docs/mongo_find_data.png)

Використайте `{{mongodb1.data[0].result}}`, щоб отримати масив.

![mongo_find_code](https://cdn.illacloud.com/official-website/img/docs/mongo_find_code.png)

#### знайти

отримати перший документ із колекції, який відповідає заданому набору критеріїв

** Input **

| Властивості      | Обов'язково   | Опис                                                                   |
| ---------------- | ------------- | ---------------------------------------------------------------------- |
| Запит            | необов'язково | визначає критерії вибору для операції з пошуку                         |
| Проекція         | необов'язково | вказує, які поля повинні містити або виключати в результатах запиту    |
| Пропустити       | необов'язково | вкажіть кількість пропущених документів за замовчуванням 0.            |

Наприклад, щоб знайти ідентифікатор, ціни і тип усіх апельсинів відсортовані за їх id.

Для **`Query`**, `{"type": "помаранчевий"}`

Для **`Проекту`**, `{"_id": 1, "type": 1, "ціна": 1}`

Примітка: замініть «1» на «true» все ще це робить!

** Вивід **

Масив об'єктів документів

![mongo_find1_data](https://cdn.illacloud.com/official-website/img/docs/mongo_find1_data.png)

Використайте `{{[mongodb1.data[0].result]}}`, щоб отримати масив.

![mongo_find1_code](https://cdn.illacloud.com/official-website/img/docs/mongo_find1_code.png)
