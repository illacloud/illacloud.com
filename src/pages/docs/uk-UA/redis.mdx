---
title: Редіс
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/347?languages=uk&filter=basic&value=0
metaTitle: Документація з використанням Redis | ILLA Cloud
desc: Дізнайтеся, як з'єднатися та взаємодіяти з вашою базою даних Redis використовуючи функцію інтеграції бази даних ILLA Cloud. Перегляньте цю інструкцію для більш детальної інформації.
tagCategory: doc_menu_redis_click
categoryName: 🔨 Інтеграція
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Що таке червоне?</h2>

Redis in Illa - це інтеграція бази даних, що дозволяє вам з'єднуватися та взаємодіяти з базою даних Redis. Redis - це популярна система управління базами даних з відкритим вихідним кодом, відома своєю швидкістю, надійністю та легкістю використання.

З інтеграцією Redis в Illa, ви можете запитувати, вставати, оновити, оновлювати та видаляти дані з бази даних Redis за допомогою SQL-команд. Також можна створювати користувацькі SQL запити і запускати їх у межах Illa. Це полегшує створення програм, орієнтованих на дані, і панель на дані, які показують дані в реальному часі з бази даних Redis.

### Створити Redis

Існує два способи створити ресурс у Іллі після входу до облікового запису Illa в обліковий запис.

-   Створити в ресурсах

Увійдіть до свого рахунку Ілла, оберіть **Ресурси**`на верху сторінки та натисніть кнопку`**Створити нове**\`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Оберіть **`Повторити`** зі списку бази даних.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Підключіться до бази даних з необхідними параметрами, описаними в меню `Параметри підключення` нижче.

Натисніть `**Test Connection**`, щоб перевірити, чи можна успішно підключитися до бази даних. Якщо так, натисніть **Зберегти ресурси\`**, ще, подвоїти ім'я хоста, ім'я користувача та пароль правильні.

Після створення ресурсу, готові Redis відображатимуться як показані.

![redis_ex_ready](https://cdn.illacloud.com/official-website/img/docs/redis_ex_ready.png)

-   Створити в будівельнику

Увійдіть до свого облікового запису Ілла, створіть проект в конструкторі Ila в сторінці `**App**` і навігація до **`Списку дій`** в нижній частині сторінки. Натисніть `**новий**`, потім оберіть **`Повторить`** зі списку бази даних. Далі підключіться до бази даних з необхідними параметрами, описаними в меню `Параметри підключення` нижче.

Натисніть `**Test Connection**`, щоб перевірити, чи можна успішно підключитися до бази даних. Якщо так, натисніть **Зберегти ресурси\`**, ще, подвоїти ім'я хоста, ім'я користувача та пароль правильні.

### Параметри підключення

Тут нам потрібна інформація для підключення до бази даних Redis.

![redis_config](https://cdn.illacloud.com/official-website/img/docs/redis_config.png)

| Властивості            | Обов'язково   | Опис                                                                                                                                  |
| ---------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Ім'я                   | обов’язково   | Назва ресурсу при створенні дій в ILLA.                                                                                               |
| Hostname               | обов’язково   | URL або IP-адреса для вашої бази даних                                                                                                |
| Порт                   | обов’язково   | Номер порта сервера, який ви повинні використовувати для підключення. Якщо не вказати порт, то порт за замовчуванням становить 6379'. |
| Індекс бази даних      | необов'язково | структура даних, що використовується для оптимізації виконання запитів, які отримують дані з бази даних Redis                         |
| База даних             | обов’язково   | Ім'я бази даних                                                                                                                       |
| Ім'я користувача       | обов’язково   | ім'я користувача, який Ви бажаєте використовувати при вході до сервера Redis.                                                         |
| Пароль                 | обов’язково   | Використовуйте цей пароль для автентифікації.                                                                                         |
| Опції SSL              | необов'язково | вирішує, наскільки високо захищене SSL TCP/IP підключення пріоритетне при узгодженні з сервером.                                      |

### Створити дії

Ми створили ресурс Redis, ми можемо додати дію вибравши Redis зі списку дій та вибравши кнопку **`Створити дію`**.

![redis_resource_list](https://cdn.illacloud.com/official-website/img/docs/redis_resource_list.png)

Тепер ми додали сервер Redis як дію до нашої сторінки будівництва.

![redis](https://cdn.illacloud.com/official-website/img/docs/redis.png)

### Налаштувати Redis

| Властивості   | Опис                                                               |
| ------------- | ------------------------------------------------------------------ |
| запит         | команди для отримання даних з бази даних                           |
| Трансформатор | перетворення даних в бажаний вами стиль з використанням JavaScript |

Приклад використання:

-   Кнопки

Ця команда отримує список всіх ключів в базі даних Redis. Ви можете використовувати маску для фільтрування клавіш за шаблоном.

```sql
KEYS *
KEYS user:*
```

-   ОТРИМАТИ

Ця команда отримує значення, пов’язане із заданим ключем.

```sql
GET user:12345
```

-   HGETALL

Ця команда приймає всі поля та їхні значення з геш-структури даних Redis.

```sql
HGETALL user:12345
```

-   ZRANGE

Ця команда отримує спектр значень від впорядкованої структури наборів даних Redis, виходячи з їх балів.

```sql
ZRANGE high_scores 0 9 WITHSCORES
```

-   ВЕЛИКІ

Ця команда отримує спектр значень зі списку Redis.

```sql
LRANGE my_list 0 4
```

-   ВИМКНУТИ

Ця команда перевіряє, чи існує даний ключ у базі даних Redis.

```sql
EXISTS user: 12345
```

-   Штрих-код

Ця команда отримує значення, яке потребує часу для роботи (TTL), пов'язане з ключем, який визначає, наскільки ключ буде залишатися в базі даних до її завершення.

```sql
TTL user:12345
```
