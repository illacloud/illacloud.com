---
title: データソースに接続
metaTitle: Illa Cloud Connect to your data sources
desc: データソースへの接続については、ILLA ビルダーでご確認ください。
tagCategory: doc_menu_data_source_connect_click
categoryName: 👋 はじめに
categoryPriority: 1
postPriority: 1
---

* * *

## <h2 hidden>データソースに接続 (アクション)</h2>

データソースへの接続については、ILLAビルダーでご確認ください。

ILLAでは、データベースやAPIを含む幅広いデータソースを組織に接続して統合してリソースとして使用することができます。

### データソース

データソースは、データが保存および処理される場所です。 データソースは、データが保存および処理される場所です。 データソースの例としては、PostgreSQLデータベース、Twilio REST API、Amazon S3バケットなどがあります。 ILLAでは、複数のデータソースを追加し、ユーザがクエリできるようにすることができます。 ILLAは、ユーザーのデータソースへの接続プロセスを簡素化します。 データソースの詳細を気にすることなく、クエリを書くときにのみ、関連するリソースを選択する必要があります。 複数の Amazon S3 バケットなど、同じ型の複数のリソースを追加して区別することもできます。

#### 統合

ILLAは、SQLデータベースやStripeなどの最小構成を必要とする多くの一般的なデータソースに組み込みの統合を提供します。 これらの統合はリソースとも呼ばれ、ILLAを使用してデータソースに接続してクエリを行うことができます。 特定の種類のデータソースが組み込みの統合でサポートされていない場合。 REST、GraphQL、SOAP統合を使用して、ほぼすべてのAPIに接続できます。 これにより、ILLAビルダー内から幅広いデータソースにアクセスできます。 これらの統合はリソースとも呼ばれ、ILLAを使用してデータソースに接続してクエリを行うことができます。 特定の種類のデータソースが組み込みの統合でサポートされていない場合。 REST、GraphQL、SOAP統合を使用して、ほぼすべてのAPIに接続できます。 これにより、ILLAビルダー内から幅広いデータソースにアクセスできます。

#### データベースに接続

ほとんどのデータベースとデータストアに接続します

#### APIに接続

RESTとGraphQLに接続します。

### リソース

ILLAのリソースは、ILLAがデータソースにどのように接続するかを指定する保存設定です。 ILLAでデータソースを使用するには、そのデータソースを作成する必要があります。 クエリを書き込む際にデータにアクセスするための適切なリソースを選択できます。 ILLAでデータソースを使用するには、そのデータソースを作成する必要があります。 クエリを書き込む際にデータにアクセスするための適切なリソースを選択できます。

ILLAの各リソースには、ILLAが対応するデータソースにどのように接続するかを決定する独自の設定オプションがあります。 これらのオプションには、認証資格情報とフロー、データベース接続設定、ベース URL やデフォルトヘッダーなどの API 設定が含まれます。 これらのオプションには、認証資格情報とフロー、データベース接続設定、ベース URL やデフォルトヘッダーなどの API 設定が含まれます。

#### リソースの仕組み

ILLAリソースは、データソースに安全に接続するために必要な設定を保存します。 これにより、ユーザーに直接データソースを公開することなく、簡単にデータをクエリできます。 ILLAリソースは、データソースに安全に接続するために必要な設定を保存します。 これにより、ユーザーに直接データソースを公開することなく、簡単にデータをクエリできます。 リソースに対してクエリが実行される場合 ILLAは、サーバ側のデータソースへのリクエストをプロキシするために、リソースの設定を使用します。 これにより、ILLAのみがデータソースに直接アクセスできるようになります。 これにより、ILLAのみがデータソースに直接アクセスできるようになります。

ILLAは、ユーザーのデータソースへの接続プロセスを簡素化します。 データソースの詳細を気にすることなく、クエリを書くときにのみ、関連するリソースを選択する必要があります。 複数の Amazon S3 バケットなど、同じ型の複数のリソースを追加して区別することもできます。

このアプローチでは、データソースをユーザーから非表示にし、データソースとのすべてのやり取りが安全に処理されるようにします。 このアプローチでは、データソースをユーザーから非表示にし、データソースとのすべてのやり取りが安全に処理されるようにします。 ILLAは、機密情報を公開する可能性があり、すべてのインタラクションで同じ条件を使用するデータソースへのクライアント側の接続を回避します。 さらに、リソースによってクエリキャッシュが可能になり、データソースへのリクエスト数を削減し、コストを削減することができます。 さらに、リソースによってクエリキャッシュが可能になり、データソースへのリクエスト数を削減し、コストを削減することができます。
