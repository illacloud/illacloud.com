---
title: |
  Hydra
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/429?languages=ja&filter=basic&value=0
metaTitle: Hydra Integration Doc | ILLA Cloud
desc: ILLA クラウドを使用して、Hydraデータにアクセスして操作します。 クエリ、操作、およびデータの表示ができるアプリを作成します。
tagCategory: doc_menu_hydra_click
categoryName: 🔨 統合
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>ハイドラとは何ですか？</h2>

HydraはILLA Cloudプラットフォーム内の強力な統合機能で、さまざまなデータソースやアプリケーションをシームレスに接続および統合できます。 中央ハブとして機能するHydraは、さまざまなシステム間の効率的なデータフローと通信を可能にし、リアルタイムデータ同期、自動ワークフロー、合理化されたプロセスを実現します。 その強力な機能により、Hydraは企業が業務を最適化し、コラボレーションを強化することができます。 データ駆動型の意思決定を容易に行うことができます。すべて ILLA Cloudの統合されたエコシステム内です。

### Hydraを作成

アカウントにサインインした後、ILLAでリソースを作成するには2つの方法があります。

-   リソースに作成

Illaアカウントにサインインし、ページ上部の「**リソース**」を選択し、「**新規作成**」ボタンをクリックします。

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

データベース リストから **`Hydra`** を選択します。

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list_3.jpeg)

下の「接続設定」で説明されている必要なパラメータを使用してデータベースに接続します。

データベースに正常に接続できるかどうかを確認するには、`**接続をテスト**` をクリックします。 はいの場合は**`リソースを保存`**をクリックします。それ以外の場合は、ホスト名、ポート、ユーザー名、パスワードが正しいことを確認してください。

リソースを作成すると、Ready Hydraが表示されます。

![hydra_ex_ready](https://cdn.illacloud.com/official-website/img/official-site/intergration/hydra_ex_ready.jpeg)

-   ビルダーで作成

Illa アカウントにサインインし、`**App**` ページで Illa Builder でプロジェクトを作成します。 をクリックし、ページの下部にある **`Action List`** に移動します。 `**new**`をクリックし、データベースリストから**`Hydra`**を選択します。 次に、下の「接続設定」で説明されている必要なパラメータを使用してデータベースに接続します。

データベースに正常に接続できるかどうかを確認するには、`**接続をテスト**` をクリックします。 はいの場合は**`リソースを保存`**をクリックします。それ以外の場合は、ホスト名、ポート、ユーザー名、パスワードが正しいことを確認してください。

### 接続設定

ここでは、Hydraデータベースに接続するための情報を提供する必要があります。

![hydra_config](https://cdn.illacloud.com/official-website/img/official-site/intergration/hydra_config.jpeg)

![hydra_config_1](https://cdn.illacloud.com/official-website/img/official-site/intergration/hydra_config_1.jpeg)

| プロパティー             | 必須   | 説明                                                                                                                                                                                                                        |
| ------------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 名前                 | 必須   | ILLAでアクションを作成する際のリソース名です。                                                                                                                                                                                                 |
| Hostname           | 必須   | データベースの URL または IP アドレス                                                                                                                                                                                                   |
| ポート                | 必須   | 接続に使用するサーバーホストのポート番号。 ポートを指定しない場合、デフォルトのポートは '5432' です。                                                                                                                                                                   |
| データベース             | 必須   | データベースの名前                                                                                                                                                                                                                 |
| ユーザー名              | 必須   | Hydraサーバーにログインするときに使用するユーザー名。                                                                                                                                                                                             |
| パスワード              | 必須   | 認証にこのパスワードを使用してください。                                                                                                                                                                                                      |
| SSL オプション          | 省略可能 | サーバとのネゴシエーション中にセキュアな SSL TCP/IP 接続が優先される方法を決定します。                                                                                                                                                                         |
| CA 証明書             | 必須   | サーバーの身元を確認し、安全を確立するために使用されるデジタル証明書 クライアントとHydraクラスタの間の暗号化された接続 指定しない場合は、デフォルトの証明書を入力します。                                                                                                                                  |
| クライアントキー           | 省略可能 | HydraクライアントとHydraサーバーとの間でセキュアな接続を確立するために使用されるパラメータ。                                                                                                                                                                       |
| クライアント証明書          | 省略可能 | クライアントがHydraサーバーに認証できるセキュリティ機能。                                                                                                                                                                                           |

### アクションを作成

Hydraリソースを作成しました。アクションリストからHydを選択し、**`Create action`**ボタンを選択することでアクションを追加できます。

![hydra_resource_list](https://cdn.illacloud.com/official-website/img/official-site/intergration/hydra_resource_list.jpeg)

これで、ビルドページにHydraサーバーをアクションとして追加しました。

![hydra](https://cdn.illacloud.com/official-website/img/official-site/intergration/hydra.jpeg)

### Configure Hydra

| プロパティー    | 説明                                                               |
| --------- | ---------------------------------------------------------------- |
| SQL クエリ   | データベースからデータを取得(選択)、挿入、更新、削除するための sql コマンド                        |
| トランス      | Javascriptを使用して好きなスタイルにデータを変換します                                 |

使用例:

-   選択

```sql
SELECT * FROM users ORDER BY last name;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   更新

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   削除

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
