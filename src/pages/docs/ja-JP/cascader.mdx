---
title: Cascader
metaTitle: Cascader Component Doc | ILLA Cloud
desc: ILLA CloudのCascaderコンポーネントを使用して、Webアプリ用のマルチレベルメニューを作成します。 Cascaderとは何か、およびCascaderオプションのカスタマイズと制御方法を学びます。 Cascaderとは何か、およびCascaderオプションのカスタマイズと制御方法を学びます。
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/299?languages=ja&filter=basic&value=0
tagCategory: doc_menu_casader_click
categoryName: 🧬 コンポーネントを組み立てます
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>カスケードとは?</h2>

カスケードは、ドロップダウンメニューから階層データ(カテゴリやサブカテゴリなど)を選択できる入力コンポーネントの一種です。 ユーザーがドロップダウンメニューからオプションを選択した場合。 キャスカダーは自動的に更新され、ネストされたドロップダウンメニューの関連するサブカテゴリやオプションが表示されます。 ユーザーがドロップダウンメニューからオプションを選択した場合。 キャスカダーは自動的に更新され、ネストされたドロップダウンメニューの関連するサブカテゴリやオプションが表示されます。

例えば、商品カテゴリを選択するためのカスケード入力コンポーネントがある場合。 最初のドロップダウンメニューには、「Electronics」、「衣料品」、「本」など、利用可能なすべてのカテゴリが表示されます。 ユーザが「Electronics」を選択すると、カスケードは「電話」、「ラップトップ」、「タブレット」などの電子のサブカテゴリを表示するように更新されます。 これにより、ユーザーは階層データをすばやく簡単にナビゲートし、必要なオプションを選択できます。

### プロパティー

Cascaderの利用可能なプロパティ。 Cascaderの利用可能なプロパティ。 JavaScript はコンポーネントに関する情報を読み取りまたは変更するために書かれることがあります。

| プロパティー           | 説明                                                                                                                                                                         |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| データソース           | 各オプションはJSON形式で、キー、値、ラベルの2つを含んでいる必要があります。 配列を使用することで、同じレベルでオプションを作成できます。 子オプションは "children" 属性を使用して作成できます。 配列を使用することで、同じレベルでオプションを作成できます。 子オプションは "children" 属性を使用して作成できます。 |
| 既定値              | コンポーネントの初期値。 コンポーネントの初期値。 {{}}}にJavaScriptを入力すると、初期値を動的に変更することができます。                                                                                                       |
| プレースホルダー         | 値は入力フィールドが空の場合に表示されます。                                                                                                                                                     |
| ラベル              | ユーザーに表示されるフィールドの名前                                                                                                                                                         |
| 図表番号             | フィールドを詳細に説明するために使用されるキャプションです。                                                                                                                                             |
| 非表示ラベル           | ラベルを表示するかどうかを設定します                                                                                                                                                         |
| 位置               | コンポーネントに対する相対的なラベルの位置を設定します                                                                                                                                                |
| 配置               | ラベルの配置(左右に揃える) を設定します                                                                                                                                                      |
| Width            | ラベルがコンポーネントの左側にある場合、ラベルの幅比率を設定します。                                                                                                                                         |
| イベントハンドラー        | トリガクエリ、コンポーネントのイベントに応じてコンポーネントを制御したり、他のAPIを呼び出したりします。                                                                                                                      |
| 無効               | コンポーネントが無効化されているかどうかのステータスを制御します。 コンポーネントが無効化されているかどうかのステータスを制御します。 無効になっている場合、コンポーネントを変更したりフォーカスしたりすることはできません。                                                            |
| 読み取り専用           | コンポーネントが読み取り専用かどうかのステータスを制御します。 コンポーネントが読み取り専用かどうかのステータスを制御します。 読み取り専用コンポーネントを選択してフォーカスできますが、変更することはできません。                                                                 |
| クリアボタンを表示        | 入力フィールドにクリアボタンを表示するかどうかを制御します                                                                                                                                              |
| Expansion method | キャスカダーの展開方法を設定する (クリックまたはホバーによって)                                                                                                                                          |
| ツールチップ           | ユーザはコンポーネントツールチップを入力できます。 ツールチップがフォーカスされると表示されます。 Markdown 形式がサポートされています。 ツールチップがフォーカスされると表示されます。 Markdown 形式がサポートされています。                                                  |
| Hidden           | コンポーネントが非表示になるかどうかを動的に制御します。 動的ブール値を使用して非表示状態を変更できます。 動的ブール値を使用して非表示状態を変更できます。                                                                                             |

### メソッド

他のコンポーネントを使用してコンポーネントを制御できます。 以下の 2 つの方法をサポートします。 以下の 2 つの方法をサポートします。

-   **setValue**

選択したオプションを設定するには、例えば{{"value1"}}

| プロパティー | 説明          |
| ------ | ----------- |
| 値      | Input value |

-   **clearValue**

選択したコンポーネントの値をクリアするには 

使用例:

Cascaderコンポーネントは、組み込みのイベントシステムを使用して、他のコンポーネントの「onChange」イベントのリスニングをサポートしています。 以下の例に従って設定します: 以下の例に従って設定します:

1.  リッスンしたいコンポーネントにイベントトリガーを追加します。 リッスンしたいコンポーネントにイベントトリガーを追加します。 例えば、Radio Group コンポーネントの **`onChange`** イベントをリッスンしたい場合。 そのRadio Groupコンポーネントにイベントトリガーを追加します。
2.  Edit イベントハンドラーで **`Control component`** を実行中に選択し、更新するイベントのターゲットとして **`Cascader`** コンポーネントを選択します。
3.  **`SetValue`**アクションを選択し、Cascaderコンポーネントに適切な値を選択します。 これは、Cascaderコンポーネントが持っている値であり、イベントがトリガーされたときに更新されます。 これは、Cascaderコンポーネントが持っている値であり、イベントがトリガーされたときに更新されます。
4.  イベントトリガーの設定を保存し、リッスンしたい他のコンポーネントに対して処理を繰り返します。

イベントトリガーを設定すると、他のコンポーネントで **`onChange`** イベントがトリガーされるたびに、Cascaderの入力コンポーネントが自動的に更新されます。 これにより、ユーザーの入力にリアルタイムで応答する動的インターフェイスを作成できます。 ユーザーがアプリケーションを操作したり操作したりできるようになります。 これにより、ユーザーの入力にリアルタイムで応答する動的インターフェイスを作成できます。 ユーザーがアプリケーションを操作したり操作したりできるようになります。

### イベントハンドラ：

| イベント  | 説明                                      |
| ----- | --------------------------------------- |
| 変更    | ユーザーが選択したオプションの値を変更したとき                 |
| フォーカス | ユーザがカスケード接続コンポーネント上でマウスカーソルを移動したとき      |
| Blur  | ユーザがオプションを選択し、カスケードコンポーネントに焦点を当てて終了した場合 |

### データ

コンポーネントには一般的に使用されているデータがいくつか含まれており、アプリで **`{{componentName.propertyName}}`** を介して呼び出すことができます。

| プロパティ名         | 説明                           |
| -------------- | ---------------------------- |
| dataSourceMode | カスケード内のデータソースのモード (デフォルトの動的) |
| displayName    | このコンポーネントの名前 (すなわちカスケード1)    |
| expandTrigger  | トリガーイベント (クリック)              |
| 値              | 選択した値                        |
| ラベル            | ラベルの値                        |
| ラベルの揃え         | カスケードラベルの配置 (左右)             |
| labelPosition  | カスケードラベルの位置 (左または右)          |
| ラベル幅           | ラベルの幅を表す整数                   |
| プレースホルダー       | プレースホルダの値                    |

例: {{cascader1.value[0]}

### 大文字小文字を区別する

次に、データソースからのデータをカスケードのオプションに動的にマッピングする方法を説明します。 

#### ステップ 1 アクションを追加

**`project` と呼ばれるサブテーブルと **`therapist`** と呼ばれるサブテーブルを **`project_id` で整列させて作成しましょう。

次に、アクションリストからSupabaseの新しいアクションを作成し、プロジェクト内のすべてのデータをリストし、supabasedb1という名前を付けます。 

```sql
SELECT [project.id](http://project.id/) as project_id, [therapist.name](http://therapist.name/) as therapist_name from therapist join project on thaperist.project_id = [project.id](http://project.id/)
```

#### ステップ 2 データを変換する

次に、Select Section の下にある **`transformer`** セクションを **`enable`** することができます。 次に、Select Section の下にある **`transformer`** セクションを **`enable`** することができます。 テキスト領域内では、必要な値とラベルだけを取得し、JavaScript を使用してカスケード対応のオプションに変換することができます。

```jsx
const transformedArray = data.reduce((acc, current) => {
  const existing = acc.find((item) => item.value === current.project_id);
  if (existing) {
    const therapist = { value: current.therapist_id, label: current.therapist_name };
    existing.children.push(therapist);
  } else {
    const project = { value: current.project_id, label: current.project_name, children: [] };
    const therapist = { value: current.therapist_id, label: current.therapist_name };
    project.children.push(therapist);
    acc.push(project);
  }
  return acc;
}, []);

return transformedArray;
```

最終的なアクションは次と同じように表示される必要があります

![casader 1](https://cdn.illacloud.com/official-website/img/docs/casader1.png)

**`Save`** と **`Run`**をクリックしてアクションを有効にします。

#### ステップ 3 コンポーネントの設定

キャスカダーコンポーネントをキャンバスにドラッグした後、ワンクリックでデータソースを`{{supabasedb1.data}}`に設定できます。 

Optional: Here we set a default value of `{{['a', '1']}}` for corresponding `{{['Psychotherapy'], ['James']}}` labels.

![casader 2](https://cdn.illacloud.com/official-website/img/docs/cascader0.png)
