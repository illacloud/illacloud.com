---
title: Microsoft SQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/365?languages=ja&filter=basic&value=0
metaTitle: Microsoft SQL Integration Doc | ILLA Cloud
desc: ILLA Cloud を使用して、Microsoft SQL データにアクセスして操作する方法を学びます。 アプリのビルド、データの可視化、ILLA Cloud を使用してワークフローの自動化を行います。
tagCategory: doc_menu_microsoft_sql_click
categoryName: 🔨 統合
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Microsoft SQLとは?</h2>

IllaのMicrosoft SQLは、ユーザーがMicrosoft SQL(MS SQL)データベースに接続して対話することができるデータベース統合です。 MS SQLは、速度、信頼性、使いやすさで知られている一般的なオープンソースのリレーショナルデータベース管理システムです。

SQL コマンドを使用して、MS SQL データベースからデータをクエリ、挿入、更新、および削除することができます。 また、カスタムSQLクエリを作成し、Ila内で実行することもできます。 これにより、MS SQLデータベースからリアルタイムデータを表示するデータ駆動型アプリケーションやダッシュボードを簡単に構築できます。

### MS SQL を作成

Illaアカウントにログインした後、Illaにリソースを作成するには2つの方法があります。

-   リソースに作成

Illaアカウントにサインインし、ページ上部の「**リソース**」を選択し、「**新規作成**」ボタンをクリックします。

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

データベース リストから **`Microsoft SQL`** を選択します。

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

下の「接続設定」で説明されている必要なパラメータを使用してデータベースに接続します。

データベースに正常に接続できるかどうかを確認するには、`**接続をテスト**` をクリックします。 はいの場合は**`リソースを保存`**をクリックします。それ以外の場合は、ホスト名、ポート、ユーザー名、パスワードが正しいことを確認してください。 はいの場合は**`リソースを保存`**をクリックします。それ以外の場合は、ホスト名、ポート、ユーザー名、パスワードが正しいことを確認してください。

リソースを作成すると、準備完了の MS SQL が表示されます。

![ms_ex_ready](https://cdn.illacloud.com/official-website/img/docs/ms_ex_ready.png)

-   ビルダーで作成

Illa アカウントにサインインし、`**App**` ページで Illa Builder でプロジェクトを作成します。 をクリックし、ページの下部にある **`Action List`** に移動します。 `**new**`をクリックし、データベース一覧から**`Microsoft SQL`**を選択します。 次に、下の「接続設定」で説明されている必要なパラメータを使用してデータベースに接続します。

データベースに正常に接続できるかどうかを確認するには、`**接続をテスト**` をクリックします。 はいの場合は**`リソースを保存`**をクリックします。それ以外の場合は、ホスト名、ポート、ユーザー名、パスワードが正しいことを確認してください。 はいの場合は**`リソースを保存`**をクリックします。それ以外の場合は、ホスト名、ポート、ユーザー名、パスワードが正しいことを確認してください。

### 接続設定

ここでは、MS SQL データベースに接続するための情報を提供する必要があります。

![ms_config](https://cdn.illacloud.com/official-website/img/docs/ms_config.png)

| プロパティー            | 必須   | 説明                                                                                                        |
| ----------------- | ---- | --------------------------------------------------------------------------------------------------------- |
| 名前                | 必須   | ILLAでアクションを作成する際のリソース名です。                                                                                 |
| Hostname          | 必須   | データベースの URL または IP アドレス                                                                                   |
| ポート               | 必須   | 接続に使用するサーバーホストのポート番号。 接続に使用するサーバーホストのポート番号。 ポートを指定しない場合、デフォルトのポートは '1433' です。                             |
| データベース            | 必須   | データベースの名前                                                                                                 |
| ユーザー名             | 必須   | MS SQL サーバーにログインするときに使用するユーザー名。                                                                           |
| パスワード             | 必須   | 認証にこのパスワードを使用してください。                                                                                      |
| 接続オプション           | 省略可能 | 接続オプションを指定するキーと値のペア                                                                                       |
| SSL オプション         | 省略可能 | サーバとのネゴシエーション中にセキュアな SSL TCP/IP 接続が優先される方法を決定します。                                                         |

### アクションを作成

MS SQL リソースを作成しました。アクションリストからMS SQL を選択し、**`Create action`** ボタンを選択することでアクションを追加できます。

![ms_resource_list](https://cdn.illacloud.com/official-website/img/docs/ms_resource_list.png)

これで、ビルドページにMS SQLサーバーを追加しました。

![ms](https://cdn.illacloud.com/official-website/img/docs/ms.png)

### MS SQL の設定

| プロパティー    | 説明                                                                                                                                                                                                                                        |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 設定タイプ     | MS SQL Server データベースへの接続に使用する構成の種類: SQL と Bulk insert です。 SQL モードでは、SQL クエリを使用できます。 の形式で選択したテーブルにデータを挿入できます。 ] }} ] }}                                                                                                                    |
| SQL クエリ   | fetch (選択)、insert、update、データベースからデータを削除するための sql コマンド。                                                                                                                                                                                    |
| トランス      | JavaScriptを使用して好きなスタイルにデータを変換します                                                                                                                                                                                                          |

使用例:

-   選択

```sql
SELECT * FROM users ORDER BY lastname;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   更新

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   削除

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
