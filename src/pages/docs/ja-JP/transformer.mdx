---
title: トランス
metaTitle: ILLA Cloud transformer doc
desc: ILLAの変換器では、JavaScriptを書いてアプリケーションロジックを改良することができます。
tagCategory: doc_menu_transformer_click
categoryName: 🌀 アプリを作る
categoryPriority: 5
postPriority: 1
---

* * *

## <h2 hidden>トランス</h2>

私たちは、アプリケーションロジックを改善するためにJavaScriptを書くための要件を満たすために、ILLAにトランスフォーマーを置いています。 トランスフォーマーでは、キャラクター入力ボックスのように {{template syntax}} を使用してJavaScriptでコーディングする必要はありません。 長く複雑なJSコードをより流暢に書けるようにします。 加えて、 Transformerは、ページ設計やロジック構築を担当する開発者が、お互いの進歩に制約を受けることなく、開発作業をより効率的に完了するのに役立ちます。 トランスフォーマーでは、キャラクター入力ボックスのように {{template syntax}} を使用してJavaScriptでコーディングする必要はありません。 長く複雑なJSコードをより流暢に書けるようにします。 加えて、 Transformerは、ページ設計やロジック構築を担当する開発者が、お互いの進歩に制約を受けることなく、開発作業をより効率的に完了するのに役立ちます。

Transformer は、既存のデータ操作だけでなく、データ変換時にトランス操作を作成するためにも使用できます。 Transformer は、既存のデータ操作だけでなく、データ変換時にトランス操作を作成するためにも使用できます。 データ変換に関わる複数の操作がある場合は、独立したトランスを選択することをお勧めします。 データを変換するのに複雑な JavaScript ロジックを使用する必要があります。

### クリエイティブな変換

アクションリストの「変圧器」タブをクリックし、「+ 新規」をクリックして変圧器を作成します。 作成後、アクションリストに表示されます。 名前を変更したり、JS コードを記述したり、選択後に設定パネルで保存したり、実行したりできます。 作成後、アクションリストに表示されます。 名前を変更したり、JS コードを記述したり、選択後に設定パネルで保存したり、実行したりできます。

> 「プレビュー」ボタンを使用して、トランスの結果を確認できます。 「プレビュー」ボタンを使用して、トランスの結果を確認できます。 新しい値を取得するには、アプリの残りの部分でトランスを保存する必要があります。

insertはトランス内で返されるデータと同じ値でトランスを呼び出します。 このデータは変圧器のコードで変換器に入力されるデータと共に変化します 文字入力ボックスと同様に、テンプレート構文を使用してグローバル変数 No. 文字入力ボックスと同様に、テンプレート構文を使用してグローバル変数 No. を呼び出すことができます。 を呼び出すことができます。 データを呼び出すためにJavaScriptコードを書いているとき 特定のデータ変数名は、左側のパネルにあるデータ ワークスペースで見つけることができます。コードの書き込みが速くなります。
[//]\: # ([Image])
[//]\: # ([Image])

データ入力ボックス内のtransformer変数を呼び出すには、 `{{transformer name. データ入力ボックス内のトランス変数を呼び出すタグ名}}`。

### クエリトランス

アクションの完了後に、取得したデータを任意の形式に変換できるようにクエリ操作にTransformerを追加しました。アクション内のトランスで変換が完了したら、 {{actionName.data}} を渡すだけでデータを取得できます。
