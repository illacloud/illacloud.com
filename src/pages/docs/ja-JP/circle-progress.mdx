---
title: 円の進行状況
metaTitle: 円の進行状況コンポーネントドキュメント | ILLA Cloud
desc: サークル進行状況コンポーネントを使用して、ILLA Cloud でアプリケーションをカスタマイズして飾る方法を学びます。
tagCategory: doc_menu_circle_progress_click
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/391?languages=ja&filter=basic&value=0
categoryName: 🧬 コンポーネントを組み立てます
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>サークルの進行状況とは?</h2>

Circle Progress コンポーネントは、進行状況や完了状況を円形形式で表示するために使用される視覚要素です。 これは、タスク、プロジェクト、またはその他の測定可能な価値の進行状況を表すためによく使用されます。

### プロパティー

| プロパティー           | 説明                                                                                                              |
| ---------------- | --------------------------------------------------------------------------------------------------------------- |
| 値                | パーセンテージ値は進歩を表します                                                                                                |
| 値のラベルを隠す         | 値のラベルが隠されているかどうか                                                                                                |
| ツールチップ           | ユーザはコンポーネントツールチップを入力できます。 ツールチップがフォーカスされると表示されます。 Markdown 形式がサポートされています。                                       |
| Hidden           | コンポーネントが非表示になるかどうかを動的に制御します。 非表示状態は動的ブール値で変更できます。                                                               |
| 配置               | ラベルの配置(左右に揃える) を設定します                                                                                           |
| テーマの色            | ボタンの背景色と不透明度を指定できます                                                                                             |
| Styles           | 円のストローク幅は進行状況を表します                                                                                              |

### 方法

他のコンポーネントを使用してコンポーネントを制御できます。 以下の 2 つの方法をサポートします。

-   **setValue**

テキストエリアの入力値を設定するには、例えば {{"value1"}}

| プロパティー | 説明          |
| ------ | ----------- |
| 値      | Input value |

-   **clearValue**

選択したコンポーネントの値をクリアするには

### データ

コンポーネントには一般的に使用されているデータがいくつか含まれており、アプリで **`{{componentName.propertyName}}`** を介して呼び出すことができます。

| プロパティ名        | 説明                                                                 |
| ------------- | ------------------------------------------------------------------ |
| 配置            | 進行円の位置を制御する                                                        |
| 色             | 円の色                                                                |
| displayName   | このコンポーネントの名前 (すなわちボタン1)                                            |
| hidden        | hidden status (true または false)                                     |
| ストローク幅        | 円の枠線の幅                                                             |
| tooltipText   | ツールチップテキストの値                                                       |
| trailColor    | 軌跡の色か円の進行の塗りつぶし部分か                                                 |
| 値             | 進捗のパーセンテージ値                                                        |

例: {{circleProgress1.value}}

### 大文字小文字を区別する

フォームの完了進捗を示す円進行コンポーネントでフォームを作成する方法を示します。

#### ステップ1 コンポーネントを追加

デフォルトのテキスト「フォーム」とキャンバスに「送信」ボタンで**`form`**コンポーネントを追加します。

**`input`** コンポーネント、**`number input`** コンポーネント、そして `**Insert**` から **`circle progress`** コンポーネントをフォームに追加します。

入力コンポーネントに「名前」、数値入力コンポーネントに「年齢」、および「進行状況」コンポーネントにラベル付けします。

入力コンポーネントのプレースホルダを「ここにあなたの名前を入力してください」に設定し、入力された数値のデフォルト値を以下のように0に設定します。

![cir_layout](https://cdn.illacloud.com/official-website/img/official-site/components/circle_layout.jpg)

#### ステップ 2 コンポーネントの設定

**`input`** と **`number input`** コンポーネントについては、 **`circle progress`** コンポーネントに値を設定して、完了の進行状況を反映させるように設定します。

1.  **`input`** と **`number input`** コンポーネントをクリックして、検査ページを開きます。 **`Event handler`**の下で `*** 新しい**` をクリックします。
2.  Edit event handlerで**`Control component`** を実行中に選択します。 をクリックし、更新する**`circle progress`**コンポーネントをイベントのターゲットとして選択します。
3.  **`setValue`**アクションを選択し、値を次のように設定します

```jsx
{
  {
    input1.value && numberInput1.value
      ? "100"
      : input1.value || numberInput1.value
      ? "50"
      : "0";
  }
}
```

![cir_event_config](https://cdn.illacloud.com/official-website/img/official-site/components/cir_event_config.jpg)

注意: 前述のようにイベントハンドラをBOTH入力と数値入力コンポーネントに設定する必要があります。

"Submit" **`button`** コンポーネントでは、フォームが完了した場合にフォームを送信するように設定できます (すなわち円の進行状況100%)

1.  **`button`** コンポーネントをクリックして検査ページを開きます。 **`Event handler`**の下で `*** 新しい**` をクリックします。
2.  Edit event handlerで**`Control component`** を実行中に選択します。 をクリックし、更新する **`form`** コンポーネントをイベントのターゲットとして選択します。
3.  **`submit`**メソッドを選択し、**`{{circleProgress1.value == 100}}`を**`Only run when`\*\*に入れます。

![cir_sub_config](https://cdn.illacloud.com/official-website/img/official-site/components/cir_sub_config.jpg)

同様に、未完了のフォームを送信しようとしているときに警告を表示するようにボタンを設定することができます。

1.  **`button`** コンポーネントをクリックして検査ページを開きます。 **`Event handler`**の下で `*** 新しい**` をクリックします。
2.  Edit イベントハンドラーで**`Show notification`** in action, **`title`** "Error!" and **`description`** "Please fill the form before submiting". **`type`**の`Error`、`{{2000}}`を**`duration`**（2秒）にし、\*\*`Only run when`に`{{circleProgress1.value<100}}`を入れます。

![cir_err_config](https://cdn.illacloud.com/official-website/img/official-site/components/cir_err_config.jpg)

`**Form**` コンポーネントを設定して、送信時に成功メッセージを表示することもできます。

1.  **`form`** コンポーネントをクリックして検査ページを開きます。 **`Event handler`**の下で `*** 新しい**` をクリックします。
2.  編集イベントハンドラーで**`通知を表示`** を選択します。**`title`** "送信しました。 **`種類`**に`成功`を、`{{2000}}`を**`期間`**（2秒）にします。

![cir_form_submit](https://cdn.illacloud.com/official-website/img/office-site/components/cir_form_submiy.jpg)

#### ステップ4 テスト

![cir_test](https://cdn.illacloud.com/official-website/img/official-site/components/cir_test.gif)
