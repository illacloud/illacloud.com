---
title: アップロード
metaTitle: ILLA クラウドアップロードコンポーネントドキュメント
desc: ローカルマシンから任意のクラウドストレージにファイルをアップロードするには、アップロードコンポーネントを使用します
tagCategory: doc_menu_upload_click
categoryName: 🧬 コンポーネントを組み立てます
categoryPriority: 6
postPriority: 1
---

* * *

## <h2 hidden>アップロード</h2>

アップロードコンポーネントは、ユーザーがローカルマシンから任意のクラウドストレージにAPIを介してファイルをアップロードできるようにするために使用されます。 次に、アップロードコンポーネントをクラウドストレージサービスに関連付ける方法を紹介します。 アップロードコンポーネントを使用して画像を選択し、画像などで表示する方法。

<img src='https://cdn.illacloud.com/official-website/img/docs/upload1.png' alt="" />

### プロパティー

| 属性             | 説明                                                                                                                                                                                    |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| タイプ            | ボタンとDropzoneを含むアップロードコンポーネントの種類を設定します。                                                                                                                                                |
| テキスト           | アップロードコンポーネントに表示されるテキスト。                                                                                                                                                              |
| 選択タイプ          | 選択タイプを複数のファイル、単一のファイル、またはディレクトリに設定します。                                                                                                                                                |
| ファイルの種類        | アップロード可能なファイル拡張子のリスト。 値はすべてのファイルタイプを許可しません。                                                                                                                                           |
| 新規に選択したファイルを追加 | 選択した新しいファイルを選択したファイルに追加するか、複数のファイルまたはディレクトリの選択タイプで選択したファイルを置き換えます。                                                                                                                    |
| ファイルリスト        | アップロードコンポーネントの下に選択したファイルのリストを表示するかどうかを設定します。 アップロード者の高さは、ファイルリストの長さに基づいて動的に変更されます。                                                                                                    |
| 解析値            | JSON、CSV、TSV、Excel、およびTXTファイルをサポートして、選択したファイルを解析しようとします。 パースされたデータは {{upload1.parsedValue}} 経由でアクセスできます。 解析できないファイルは null になります。 このオプションに関係なく、すべてのファイルは value64 エンコードされた文字列として使用できます。 |
| 読み込み中          | コンポーネントがロードインジケータを表示するかどうか。                                                                                                                                                           |
| 無効             | このコンポーネントを無効にするか、無効にするかどうかを設定します。 例えば、input1 コンポーネントが null の場合、このコンポーネントは無効にする必要があります: {{ input1.value ==''}}                                                                         |
| ツールチップ         | ユーザーがコンポーネントにカーソルを合わせたときにコンポーネントにツールチップが表示されます。                                                                                                                                       |
| 必須項目           | 必須かどうかを設定します。                                                                                                                                                                         |
| Min size       | 追加可能な最小ファイルサイズを設定します。                                                                                                                                                                 |
| Max size       | 追加可能な最大ファイルサイズを設定します。                                                                                                                                                                 |
| カスタムルール        | 検証に失敗したときに表示されるテキストを設定する。                                                                                                                                                             |
| 検証メッセージを隠す     | 検証に失敗したときに検証メッセージを表示するかどうかを設定する。                                                                                                                                                      |
| フォームデータキー      | コンポーネントのキーを設定する。 フォームにこのコンポーネントを追加した後、フォームを送信する際にこのコンポーネントのデータを識別するためにこのキーが使用されます。                                                                                                    |
| Hidden         | このコンポーネントを表示するかどうかを設定し、どのような条件で表示するかを設定します。 たとえば、現在のユーザが User A でない場合、このコンポーネントは非表示にする必要があります: {{ currentUserInfo.nickname != 'User A' }}                                             |
| バリアント型         | ボタンアップロードコンポーネントをアウトラインまたは塗りつぶしに設定します。                                                                                                                                                |
| テーマの色          | format@@0コンポーネントの色を設定します。                                                                                                                                                             |

### 方法

アップロードコンポーネントを制御するために他のコンポーネントを使用することができます。 以下の3つの方法をサポートします。

| clearValidation | 検証メッセージをクリアするために使用されます            |
| --------------- | --------------------------------- |
| clearValue      | 選択したものを消去するために使用                  |
| setDisabled     | アップロードコンポーネントを無効または有効にするために使用します。 |

### イベントハンドラ：

選択したファイルが変更されたときにアクションをトリガーする

### アップローダーのデータ

アップロードコンポーネントには、アプリケーションで呼び出すことができるいくつかの一般的に使用されるデータがあります。

`value` ファイルの base64 データからなる配列。 

`files` オブジェクトの配列。 オブジェクトのキーには `lastModified` 、 `name` 、 `size` 、 `type` が含まれます。 `{{upload1.files.map(file =>file) を使用できます。 ame)}}`でファイル名の配列を取得し、lastModified array、size array、type array も同じ方法で取得できます。 

### クラウドストレージサービスにファイルをアップロード

<img src='https://cdn.illacloud.com/official-website/img/docs/upload2.gif' alt="" />

#### S3リソースを介して単一のファイルをアップロード

アクションの構成は以下のとおりです。 

オブジェクト名をアップロード

```jsx
{{upload1.files.map(file =>file.name)[0]}}
```

データをアップロード

```jsx
{{upload1.value[0]}}
```

<img src='https://cdn.illacloud.com/official-website/img/docs/upload3.png' alt="" />

#### S3リソースを介して複数のファイルをアップロード

オブジェクト名リストをアップロード

```jsx
{{upload1.files.map(file =>file.name)}}
```

データリストをアップロード

```jsx
{{upload1.value}}
```

<img src='https://cdn.illacloud.com/official-website/img/docs/upload4.png' alt="" />

画像をアップロードし、画像コンポーネントに表示します。
画像ソースを、アップロードコンポーネントの base64 データに変更します。 base64 データは配列内の value 属性に格納されます。 そして、ファイルのプレフィックスをファイルデータで分割する必要があります。 コードは以下の通りです。 

```jsx
{{'data:image/jpeg;base64,'+upload1.value[0]}}
```

<img src='https://cdn.illacloud.com/official-website/img/docs/upload5.gif' alt="" />
