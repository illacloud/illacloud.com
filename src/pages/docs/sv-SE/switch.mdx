---
title: Växla
metaTitle: Växla komponent Doc ILLA Cloud
desc: Lär dig hur du använder Switch-komponenten i ILLA Cloud för att skapa ett användargränssnitt som låter användare växla mellan två alternativ. Se exempel och egenskaper hos Switch.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/331?languages=sv&filter=basic&value=0
tagCategory: doc_menu_switch_click
categoryName: 🧬 Montera komponenter
categoryPriority: 6
postPriority: 10
---

## <h2 hidden>Vad är Switch?</h2>

Switch komponenten är ett användargränssnitt element som tillåter användare att växla mellan två stater: "on" och "off".

### Egenskaper

| Egenskaper        | Beskrivning                                                                                                                |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Förvalt värde     | Det initiala värdet av komponenten. Du kan dynamiskt ändra initialvärdet genom att skriva JavaScript i {{}}.               |
| Etikett           | Namnet på fältet som visas för användaren                                                                                  |
| Bildtext          | En text som används för att beskriva fältet i detalj                                                                       |
| Dold etikett      | Ange om etiketten ska visas                                                                                                |
| Befattning        | Ställ in positionen för etiketten i förhållande till komponenten                                                           |
| Justering         | Ställ in riktningen på etiketten                                                                                           |
| Händelsehanterare | Utlösa frågor, styra komponenter, eller ringa andra API:er som svar på komponenthändelser.                                 |
| Laddar            | Om komponenten ska visa en laddningsindikator.                                                                             |
| Inaktiverad       | Kontrollera status för om komponenten är inaktiverad. Komponenten kan inte ändras eller fokuseras när den är inaktiverad.  |
| Verktygstips      | Användaren kan ange komponentverktygstips här. Verktygstipen kommer att visas när den är fokuserad. Markdown format stöds. |
| Hidden            | Styr dynamiskt om komponenten är dold. Du kan ändra den dolda statusen genom dynamiskt booleskt värde.                     |
| Tema färg         | Tillåter användare att ange knappens bakgrundsfärg och opacitet                                                            |

### Metod

Du kan använda andra komponenter för att styra komponenten. Vi stöder följande tre metoder:

-   **setValue**

För att ställa in det valda värdet, till exempel, {{'value1'}}

| Egenskaper | Beskrivning |
| ---------- | ----------- |
| Värde      | Valt värde  |

-   **clearValue**

För att rensa det valda värdet

-   **växla**

ändra växeln från "off" tillstånd till "on" tillstånd, eller vice versa

Exempel Användning:

Byt komponentstöd för att lyssna på "onClick" händelsen av andra komponenter med hjälp av inbyggda händelsesystem. Ställ in det genom att följa dessa exempelsteg:

1.  Lägg till en händelseutlösare till den komponent som du vill lyssna på. Till exempel, om du vill lyssna på **`onClick`** händelsen av en knappkomponent, skulle du lägga till en händelseutlösare till den knappkomponenten.
2.  I händelsehanteraren Redigera, välj **`Control component`** i åtgärd, välj **`Switch`** komponenten som du vill uppdatera som mål för händelsen.
3.  Välj **`toggle`** åtgärden. När händelsen utlöses (dvs när knappen klickas), växla komponenten med och dess status kommer att uppdateras.
4.  Spara inställningarna för händelsen och upprepa processen för andra komponenter som du vill lyssna på.

När du har konfigurerat händelseutlösarna uppdateras växlingskomponenten automatiskt när händelsen **`onClick`** utlöses på de andra komponenterna. Detta gör att du kan skapa dynamiska gränssnitt som svarar på användarens indata i realtid. göra det enklare för användare att navigera och interagera med din applikation.

### Händelsehanterare

| Händelse | Beskrivning                                                                                           |
| -------- | ----------------------------------------------------------------------------------------------------- |
| Ändra    | När en användare ändrar det valda optionsvärdet, utför specifika åtgärder som anpassats av användare. |

### Data

Knappkomponenten har några vanliga data, som kan anropas via **`{{componentName.propertyName}}`** i appen.

| Egenskapens namn | Beskrivning                                                 |
| ---------------- | ----------------------------------------------------------- |
| colorScheme      | bakgrundsfärg på knappen                                    |
| visningsnamn     | namnet på denna komponent (dvs knapp1)                      |
| hidden           | ett booleskt värde anger den dolda statusen för knappen     |
| etikett          | etikett värde                                               |
| etikettJustera   | justering av switch etikett (vänster eller höger)           |
| etikettPosition  | positionen för växlingsetikett (vänster eller höger)        |
| etikettBredd     | heltal som representerar bredden av etikett.                |
| etikettFullt     | ett booleskt värde anger om etiketten är full.              |
| värde            | En användare sätter värdet till att vara sant eller falskt  |

### Använd ärende

Därefter kommer vi att visa hur man ändrar status för andra komponenter dynamiskt. 

#### Steg 1 Lägg till en komponent

Först adderar vi komponenten **`Switch`** och etiketterar den som 'Dölj'. Sedan använder vi **`text`** komponenten som en influerad komponent som ändras från 'visade' till 'dold' tillstånd. Vi ändrar dess innehåll till "Hej världen" 

#### Steg 2 Konfigurera en komponent

I egenskapen **`Hidden`** för Text. Klicka på **`fx`** ikonen så att vi kan styra komponentens dolda tillstånd genom att ställa in det samma som värdet på vår switch med JavaScript:

```jsx
{{switch1.value}}
```

Nu, när strömbrytaren är på, är värdet för strömbrytaren **`true`,** texten kommer att döljas, och vice versa.

Växla **`På`** 

![switch0](https://cdn.illacloud.com/official-website/img/docs/switch0.png)

Växla **`Av`**

![switch_off](https://cdn.illacloud.com/official-website/img/docs/switch_off.png)
