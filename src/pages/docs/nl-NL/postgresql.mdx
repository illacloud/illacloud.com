---
title: PostgreSQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/329?languages=nl&filter=basic&value=0
metaTitle: PostgreSQL integratie Doc ・ILLA Cloud
desc: Verbind en communiceer met een PostgreSQL database met behulp van ILLA Cloud. Maak apps die gegevens kunnen opzoeken, bewerken en visualiseren. Automatiseren en optimaliseren van data workflows.
tagCategory: doc_menu_postgresql_click
categoryName: 🔨 Integraties
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Wat is PostgreSQL?</h2>

PostgreSQL in Illa is een database integratie waarmee u verbinding en interactie kunt maken met een PostgreSQL database. PostgreSQL is een populair open-source database management systeem dat bekend staat om zijn stabiliteit, betrouwbaarheid en krachtige functies.

Met de PostgreSQL integratie in Illa kunt u gegevens opzoeken, invoegen, bijwerken en verwijderen uit een PostgreSQL database met SQL commando's. U kunt ook aangepaste SQL-query's maken en uitvoeren binnen Illa actie. Dit maakt het eenvoudig om data-driven applicaties en dashboards te bouwen die real-time gegevens weergeven uit een PostgreSQL database.

### Maak PostgreSQL

Er zijn twee manieren om een bron in Illa te maken nadat je bent ingelogd op je Illa account.

-   Maak in Bronnen

Log in op je Illa account, selecteer `**Resources**` bovenaan de pagina, en klik op de `**Maak Nieuw**` knop.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Selecteer **`PostgreSQL`** uit de database lijst.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Maak verbinding met de database met de vereiste parameters beschreven in `Connectie Instellingen` hieronder.

Klik op `**Test Connection**` om te zien of we met succes verbinding kunnen maken met de database. Zo ja, klik **`Resources`**, anders controleer je de hostnaam, port, gebruikersnaam en wachtwoord correct.

Na het aanmaken van een bron wordt de aangepaste PostgreSQL weergegeven zoals getoond.

![ex_post_ready](https://cdn.illacloud.com/official-website/img/docs/ex_post_ready.png)

-   Maak in Bouwer

Log in op je Illa account, maak een project aan in Illa Builder in de `**App**` pagina en navigeer naar de **`Action List`** onderaan de pagina. Klik op `**nieuw**`, en selecteer dan **`PostgreSQL`** uit de database lijst. Maak vervolgens verbinding met de database met de vereiste parameters beschreven in `Connectie Instellingen` hieronder.

Klik op `**Test Connection**` om te zien of we met succes verbinding kunnen maken met de database. Zo ja, klik **`Resources`**, anders controleer je de hostnaam, port, gebruikersnaam en wachtwoord correct.

### Verbindingsinstellingen

Hier moeten we informatie verstrekken voor het verbinden met de PostgreSQL database.

![post_config](https://cdn.illacloud.com/official-website/img/docs/post_config.png)

| Eigenschappen     | vereist   | Beschrijving                                                                                                                   |
| ----------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------ |
| naam              | verplicht | De naam voor resource bij het maken van acties in de ILLA.                                                                     |
| Hostname          | verplicht | De URL of het IP-adres van uw database                                                                                         |
| Poort             | verplicht | Het poortnummer van de server host dat u moet gebruiken om te verbinden. Als je geen poort opgeeft, is standaard poort '5432'. |
| Database          | verplicht | De naam van de database                                                                                                        |
| Gebruikersnaam    | verplicht | de gebruikersnaam die u wilt gebruiken bij het inloggen op de PostgreSQL server.                                               |
| Wachtwoord        | verplicht | Gebruik dit wachtwoord voor verificatie.                                                                                       |
| SSL opties        | optioneel | Bepaal hoe hoog een veilige SSL TCP/IP verbinding prioriteit heeft tijdens het onderhandelen met de server.                    |

### Acties aanmaken

We hebben een PostgreSQL bron gemaakt, we kunnen de actie toevoegen door de PostgreSQL van de actie lijst te selecteren en te kiezen voor de knop **`Create action`**.

![post_resource_list](https://cdn.illacloud.com/official-website/img/docs/post_resource_list.png)

Nu hebben we de PostgreSQL server toegevoegd als een actie aan onze bouwpagina.

![post](https://cdn.illacloud.com/official-website/img/docs/post.png)

### PostgreSQL configureren

| Eigenschappen  | Beschrijving                                                                                      |
| -------------- | ------------------------------------------------------------------------------------------------- |
| SQL query      | sql commando's om op te halen (select), invoegen, bijwerken, gegevens uit de database verwijderen |
| Transformator  | data omzetten naar de stijl die je leuk vindt met behulp van JavaScript                           |

Voorbeeld gebruik:

-   Selecteren

```sql
SELECT * FROM users ORDER BY lastname;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   Vernieuwen

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   Verwijderen

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
