---
title: Cambia
metaTitle: Interruttore Doc <unk> ILLA Cloud
desc: Scopri come utilizzare il componente Switch in ILLA Cloud per creare un'interfaccia utente che consente agli utenti di passare da due opzioni. Vedi esempi e proprietà di Switch.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/331?languages=it&filter=basic&value=0
tagCategory: doc_menu_switch_click
categoryName: 🧬 Assembla i componenti
categoryPriority: 6
postPriority: 10
---

## <h2 hidden>Cos'è Switch?</h2>

Il componente Switch è un elemento di interfaccia utente che consente agli utenti di passare tra due stati: "on" e "off".

### Proprietà

| Proprietà          | Descrizione                                                                                                                                        |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Valore predefinito | Il valore iniziale del componente. È possibile modificare dinamicamente il valore iniziale digitando JavaScript in {{}}.                           |
| Etichetta          | Il nome del campo visualizzato all'utente                                                                                                          |
| Didascalia         | Una didascalia utilizzata per descrivere in dettaglio il campo                                                                                     |
| Etichetta nascosta | Imposta se visualizzare l'etichetta                                                                                                                |
| Posizione          | Imposta la posizione dell'etichetta rispetto al componente                                                                                         |
| Allineamento       | Imposta l'allineamento dell'etichetta                                                                                                              |
| Gestore Eventi     | Attiva interrogazioni, componenti di controllo o chiama altre API in risposta a eventi di componenti.                                              |
| Caricamento        | Indica se il componente deve mostrare un indicatore di caricamento.                                                                                |
| Disabilitato       | Controlla lo stato se il componente è disabilitato. Il componente non può essere modificato o focalizzato quando è disabilitato.                   |
| Suggerimento       | L'utente può inserire il suggerimento del componente qui. Il suggerimento verrà mostrato quando è messo a fuoco. Il formato Markdown è supportato. |
| Hidden             | Controllare dinamicamente se il componente è nascosto. È possibile modificare lo stato nascosto attraverso il valore booleano dinamico.            |
| Colore Tema        | Consente agli utenti di specificare il colore di sfondo e l'opacità del pulsante                                                                   |

### Metodo

È possibile utilizzare altri componenti per controllare il componente. Sosteniamo i seguenti tre metodi:

-   **setValue**

Per impostare il valore selezionato, ad esempio, {{'value1'}}

| Proprietà | Descrizione        |
| --------- | ------------------ |
| Valore    | Valore selezionato |

-   **clearValue**

Per cancellare il valore selezionato

-   **toggle**

cambiare il passaggio dallo stato "spento" allo stato "on", o viceversa

Esempio Di Utilizzo:

Cambia il supporto del componente ascoltando l'evento `onClick` di altri componenti usando il sistema di eventi integrato. Impostalo seguendo questi passaggi di esempio:

1.  Aggiungi un trigger di evento al componente che vuoi ascoltare. Ad esempio, se si desidera ascoltare l'evento **`onClick`** di un componente Pulsante, si aggiungerebbe un trigger evento a quel componente Pulsante.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, seleziona il componente **`Switch`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli l'azione **`toggle`**. Quando l'evento viene attivato (cioè quando viene fatto clic sul pulsante), il componente dell'interruttore viene attivato e il suo stato verrà aggiornato.
4.  Salva le impostazioni di attivazione dell'evento e ripeti il processo per qualsiasi altro componente che desideri ascoltare.

Una volta che hai impostato i trigger dell'evento, il componente dell'interruttore verrà automaticamente aggiornato ogni volta che l'evento **`onClick`** viene attivato sugli altri componenti. Questo consente di creare interfacce dinamiche che rispondono all'input dell'utente in tempo reale, rendendo più facile per gli utenti la navigazione e l'interazione con la tua applicazione.

### Gestore eventi

| Evento | Descrizione                                                                                                           |
| ------ | --------------------------------------------------------------------------------------------------------------------- |
| Cambia | Quando un utente cambia il valore dell'opzione selezionato, eseguire un'azione specifica personalizzata dagli utenti. |

### Dati

Il componente del pulsante ha alcuni dati comunemente usati, che possono essere chiamati tramite **`{{componentName.propertyName}}`** nell'app.

| Nome proprietà | Descrizione                                                       |
| -------------- | ----------------------------------------------------------------- |
| colorScheme    | colore di sfondo del pulsante                                     |
| displayName    | il nome di questo componente (cioè il pulsante1)                  |
| hidden         | un valore booleano indica lo stato nascosto del pulsante          |
| etichetta      | valore dell'etichetta                                             |
| labelAllinea   | allineamento dell'etichetta dell'interruttore (sinistra o destra) |
| labelPosizione | posizione dell'etichetta dell'interruttore (sinistra o destra)    |
| labelWidth     | il numero intero che rappresenta la larghezza dell'etichetta.     |
| labelFull      | un valore booleano indica se l'etichetta è piena.                 |
| valore         | Un utente imposta il valore di essere vero o falso                |

### Caso di utilizzo

Successivamente, dimostreremo come cambiare lo stato di altri componenti dinamicamente. 

#### Passo 1 Aggiungi un componente

Per prima cosa aggiungiamo il componente **`Switch`** ed etichettalo come 'Nascondi'. Quindi usiamo il componente **`text`** come componente influenzato che cambia da 'visualizzato' a 'nascosto'. Cambiamo il suo contenuto in 'Ciao Mondo' 

#### Passo 2 Configura un componente

Nella proprietà **`Hidden`** per il testo. Clicca sull'icona **`fx`** in modo da poter controllare lo stato nascosto del componente impostandolo lo stesso valore del nostro interruttore usando JavaScript:

```jsx
{{switch1.value}}
```

Ora, ogni volta che l'interruttore è acceso, il valore per l'interruttore è **`true`,** il testo verrà nascosto, e viceversa.

Cambia **`On`** 

![switch0](https://cdn.illacloud.com/official-website/img/docs/switch0.png)

Cambia **`Off`**

![switch_off](https://cdn.illacloud.com/official-website/img/docs/switch_off.png)
