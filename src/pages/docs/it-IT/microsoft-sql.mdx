---
title: Microsoft SQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/365?languages=it&filter=basic&value=0
metaTitle: Integrazione Microsoft SQL Doc ILLA Cloud
desc: Scopri come utilizzare ILLA Cloud per accedere e lavorare con i dati Microsoft SQL. Costruisci app, visualizza i dati e automatizza i flussi di lavoro con ILLA Cloud.
tagCategory: doc_menu_microsoft_sql_click
categoryName: 🔨 Integrazioni
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Cos'è Microsoft SQL?</h2>

Microsoft SQL in Illa è una integrazione di database che consente agli utenti di connettersi e interagire con un database Microsoft SQL (MS SQL). MS SQL è un popolare sistema di gestione dei database relazionale open-source noto per la sua velocità, affidabilità e facilità di utilizzo.

Con l'integrazione MS SQL in Illa, è possibile interrogare, inserire, aggiornare ed eliminare i dati da un database MS SQL utilizzando i comandi SQL. È inoltre possibile creare query SQL personalizzate ed eseguirle all'interno di Illa. Ciò facilita la creazione di applicazioni e dashboard guidate dai dati che visualizzano dati in tempo reale da un database MS SQL.

### Crea MS SQL

Ci sono due modi per creare una risorsa in Illa dopo aver effettuato l'accesso al tuo account Illa.

-   Crea in Risorse

Accedi al tuo account Illa, seleziona `**Risorse**` nella parte superiore della pagina e clicca sul pulsante `**Crea Nuovo**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Seleziona **`Microsoft SQL`** dall'elenco dei database.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

Dopo aver creato una risorsa, il MS SQL pronto verrà visualizzato come mostrato.

![ms_ex_ready](https://cdn.illacloud.com/official-website/img/docs/ms_ex_ready.png)

-   Crea in Costruttore

Accedi al tuo account Illa, crea un progetto in Illa Builder nella pagina `**App**`, e vai alla **`Action List`** in fondo alla pagina. Fare clic su `**nuovo**`, quindi selezionare **`Microsoft SQL`** dall'elenco dei database. Poi, connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

### Impostazioni Di Connessione

Qui dobbiamo fornire informazioni per la connessione al database MS SQL.

![ms_config](https://cdn.illacloud.com/official-website/img/docs/ms_config.png)

| Proprietà              | Richiesto    | Descrizione                                                                                                                             |
| ---------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| Nome                   | obbligatorio | Il nome della risorsa quando si creano azioni nell'ILLA.                                                                                |
| Hostname               | obbligatorio | L'URL o l'indirizzo IP per il tuo database                                                                                              |
| Porta                  | obbligatorio | Il numero di porta dell'host del server che dovresti usare per connetterti. Se non specifichi una porta, la porta predefinita è '1433'. |
| Database               | obbligatorio | Il nome della banca dati                                                                                                                |
| Username               | obbligatorio | il nome utente che si desidera utilizzare quando si accede al server MS SQL.                                                            |
| Password               | obbligatorio | Usa questa password per l'autenticazione.                                                                                               |
| Opzione di connessione | opzionale    | coppia chiave-valore per specificare l'opzione di connessione                                                                           |
| Opzioni SSL            | opzionale    | decide quanto alta sia prioritaria una connessione SSL TCP/IP sicura durante le trattative con il server.                               |

### Crea Azioni

Abbiamo creato una risorsa MS SQL; possiamo aggiungere l'azione selezionando MS SQL dall'elenco di azioni e scegliendo il pulsante **`Crea azione`**.

![ms_resource_list](https://cdn.illacloud.com/official-website/img/docs/ms_resource_list.png)

Ora abbiamo aggiunto il server MS SQL come azione alla nostra pagina di costruzione.

![ms](https://cdn.illacloud.com/official-website/img/docs/ms.png)

### Configura MS SQL

| Proprietà              | Descrizione                                                                                                                                                                                                                                                                                       |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tipo Di Configurazione | il tipo di configurazione che si utilizza per connettersi al database MS SQL Server: inserto SQL e Bulk. Per la modalità SQL, è possibile utilizzare SQL query. Per la modalità di inserimento Bulk, è possibile inserire i dati in una tabella selezionata con il formato {{ [{x:a, y:b}, … ] }} |
| Query SQL              | comandi sql da recuperare (selezionare), inserire, aggiornare, eliminare i dati dal database.                                                                                                                                                                                                     |
| Trasformatore          | trasformare i dati nello stile che ti piace usando JavaScript                                                                                                                                                                                                                                     |

Esempio di utilizzo:

-   Seleziona

```sql
SELECT * FROM users ORDER BY lastname;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   Aggiorna

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   Elimina

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
