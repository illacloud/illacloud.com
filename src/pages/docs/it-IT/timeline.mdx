---
title: Timeline
metaTitle: Componente Timeline Doc ILLA Cloud
desc: Impara come utilizzare il componente Timeline da ILLA Cloud
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/401?languages=it&filter=basic&value=0
tagCategory: doc_menu_timeline_click
categoryName: 🧬 Assembla i componenti
categoryPriority: 6
postPriority: 4
---

## <h2 hidden>Che cos'è la Timeline?</h2>

Un componente timeline è una rappresentazione grafica di eventi o punti dati disposti cronologicamente lungo un asse orizzontale o verticale. Consente agli utenti di visualizzare e navigare attraverso una serie di eventi o tappe in modo lineare.

### Proprietà

Proprietà disponibili della timeline. JavaScript può essere scritto per leggere o modificare informazioni sui componenti.

| Proprietà    | Descrizione                                                                                         |
| ------------ | --------------------------------------------------------------------------------------------------- |
| Oggetti      | la raccolta di singoli eventi o punti dati che sono visualizzati sulla timeline                     |
| Direzione    | l'orientamento o la disposizione della componente della timeline (orizzontale o verticale)          |
| In Attesa    | lo stato di un elemento nella timeline                                                              |
| Altezza      | dimensione o altezza verticale del componente timeline                                              |
| Hidden       | se il componente della timeline è visibile o nascosto                                               |

### Metodi

È possibile utilizzare altri componenti per controllare il componente. Sosteniamo i due metodi seguenti:

-   **setValue**

Per impostare il valore di input dell'area di testo, ad esempio, {{"value1"}}

| Proprietà   | Descrizione |
| ----------- | ----------- |
| Valore      | Input value |

-   **clearValue**

Per cancellare il valore del componente selezionato

### Dati

Il componente ha alcuni dati comunemente usati, che possono essere chiamati tramite **`{{componentName.propertyName}}`** nell'app.

| Nome proprietà     | Descrizione                                                                                                 |
| ------------------ | ----------------------------------------------------------------------------------------------------------- |
| direzione          | orientamento o layout della timeline                                                                        |
| dynamicHeight      | la capacità di regolare l'altezza della timeline dinamicamente in base al suo contenuto o ad altri fattori. |
| displayName        | il nome di questo componente (ossia la timeline1)                                                           |
| hidden             | se il componente della timeline è visibile o nascosto                                                       |
| elementi           | la raccolta di singoli eventi o punti dati visualizzati sulla linea temporale                               |
| in attesa          | lo stato di un elemento nella timeline                                                                      |
| resizeDirection    | la direzione in cui il componente timeline può essere ridimensionato dall'utente                            |

Esempio: {{timeline1.items}}

### Caso di utilizzo

Successivamente, dimostreremo come mappare i dati dall'origine dei dati al componente della timeline.

#### Passo 1 Aggiungere un'azione

Creiamo una tabella in Supabase chiamata **`timeline`,** che include solo 1 colonne: pietre miliari. Ci sono 4 pietre miliari che vogliamo mappare al componente della timeline.

Quindi possiamo creare una nuova azione per Supabase dalla lista di azione e chiamato supabasedb1.

Per elencare tutti i dati nella milestone, inserire la snippet di codice qui sotto nella sezione query SQL.

```sql
seleziona * dalla timeline;
```

Poiché i dati di output sono un elenco di coppie di dizionari, vogliamo trasformarli in un array. In Transformer, abilitarlo e mettere giù il codice qui sotto.

```jsx
const transformedData = data.map((obj) => Object.values(obj)[0]);
return transformedData;
```

Clicca su **`Salva`** e **`Esegui`** per attivare questa azione.

![timeline_data](https://cdn.illacloud.com/official-website/img/official-site/components/timeline_data.jpg)

#### Passo 2 Aggiungi Componenti

Successivamente, possiamo aggiungere un componente **`timeline`** alla tela.

Nel campo `**Items**` nella pagina Inspect dopo aver cliccato sul componente, metti `{{supabasedb1.data}}` per recuperare i dati da supabase.

#### Fase 3 Test

L'aspetto finale dovrebbe essere come mostrato.

![timeline_test](https://cdn.illacloud.com/official-website/img/official-site/components/timeline_test.jpg)
