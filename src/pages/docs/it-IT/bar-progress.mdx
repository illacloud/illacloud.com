---
title: Avanzamento barra
metaTitle: Avanzamento barra Componente Doc ILLA Cloud
desc: Scopri come utilizzare il componente di avanzamento della barra per personalizzare e decorare la tua app in ILLA Cloud.
tagCategory: doc_menu_bar_progress_click
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/387?languages=it&filter=basic&value=0
categoryName: 🧬 Assembla i componenti
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>Cos'è il progresso della barra?</h2>

Il componente di progresso della barra è un elemento visivo che rappresenta il progresso o il completamento di un compito o processo. Mostra una barra orizzontale che riempie o si restringe in base al valore di avanzamento specificato.

### Proprietà

| Proprietà                 | Descrizione                                                                                                                                               |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Valore                    | il valore percentuale rappresenta l'avanzamento                                                                                                           |
| Nascondi etichetta valore | se l'etichetta del valore è nascosta                                                                                                                      |
| Etichetta                 | Il nome del campo visualizzato all'utente                                                                                                                 |
| Etichetta nascosta        | Imposta se visualizzare l'etichetta                                                                                                                       |
| Didascalia                | Una didascalia utilizzata per descrivere in dettaglio il campo                                                                                            |
| Posizione                 | Imposta la posizione dell'etichetta rispetto al componente                                                                                                |
| Allineamento              | Imposta l'allineamento (allineare a sinistra o a destra) dell'etichetta                                                                                   |
| Width                     | Quando l'etichetta è sul lato sinistro del componente, impostare il rapporto di larghezza dell'etichetta.                                                 |
| Suggerimento              | Gli utenti possono inserire il suggerimento del componente qui. Il suggerimento verrà mostrato quando è messo a fuoco. Il formato Markdown è supportato.  |
| Hidden                    | Controllare dinamicamente se il componente è nascosto. È possibile modificare lo stato nascosto attraverso un valore booleano dinamico.                   |
| Colore Tema               | Consente agli utenti di specificare il colore di sfondo e l'opacità del pulsante                                                                          |
| Styles                    | la larghezza della corsa della barra rappresenta l'avanzamento                                                                                            |

### Metodo

È possibile utilizzare altri componenti per controllare il componente. Sosteniamo i due metodi seguenti:

-   **setValue**

Per impostare il valore di input dell'area di testo, ad esempio, {{"value1"}}

| Proprietà   | Descrizione |
| ----------- | ----------- |
| Valore      | Input value |

-   **clearValue**

Per cancellare il valore del componente selezionato

### Dati

Il componente ha alcuni dati comunemente usati, che possono essere chiamati tramite **`{{componentName.propertyName}}`** nell'app.

| Nome proprietà     | Descrizione                                                   |
| ------------------ | ------------------------------------------------------------- |
| colore             | colore della barra                                            |
| displayName        | il nome di questo componente (cioè il pulsante1)              |
| hidden             | stato nascosto (vero o falso)                                 |
| etichetta          | valore dell'etichetta                                         |
| labelAllinea       | allineamento dell'etichetta di input (sinistra o destra)      |
| labelPosizione     | posizione dell'etichetta di input (sinistra o destra)         |
| labelWidth         | il numero intero che rappresenta la larghezza dell'etichetta. |
| Larghezza tratto   | larghezza del tratto della barra                              |
| tooltipText        | valore del testo del suggerimento                             |
| valore             | valore percentuale del progresso                              |

Esempio: {{barProgress1.value}}

### Caso di utilizzo

Dimostreremo come fare un modulo con progressi a barre in basso, mostrando il progresso di completamento del modulo.

#### Passo 1 Aggiungi Componenti

Aggiungi un componente **`form`** con il testo predefinito "Form" e un pulsante "Invia" per la superficie.

Aggiungi un componente **`input`**, un componente **`number input`** e un componente **`bar progress`** da `**Inserisci**` nel modulo.

Etichetta il componente di input come "Nome", il componente di input numerico come "Età" e il componente di avanzamento barra come "Progress".

Imposta il segnaposto del componente di input su "Inserisci qui il tuo nome" e il valore predefinito del numero di input su 0 come mostrato di seguito.

![bar_layout](https://cdn.illacloud.com/official-website/img/official-site/components/bar_layout.jpg)

#### Passo 2 Configura il componente

Per i componenti **`input`** e **`number input`** possiamo configurarli per impostare il valore nel componente **`bar progress`** per riflettere l'avanzamento del completamento.

1.  Clicca sul componente **`input`** e **`number input`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`bar progress`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli l'azione **`setValue`** e imposta il valore come

```jsx
{
  {
    input1.value && numberInput1.value
      ? "100"
      : input1.value || numberInput1.value
      ? "50"
      : "0";
  }
}
```

![bar_input_num_config](https://cdn.illacloud.com/official-website/img/official-site/components/bar_input_num_config.jpg)

Nota: è necessario impostare il gestore degli eventi come descritto sopra ai componenti di input BOTH e di input numerico.

Per il componente **`button`** possiamo configurarlo per inviare il modulo se il modulo è completato (cioè avanzamento barra 100%)

1.  Clicca sul componente **`button`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`form`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli il metodo **`submit`** e inserisci `{{barProgress1.value == 100}}` in **`Solo quando avvia`**

![bar_button_submit_config](https://cdn.illacloud.com/official-website/img/official-site/components/bar_button_submit_config.jpg)

Allo stesso modo, possiamo configurare il pulsante per mostrare un avviso quando l'utente sta cercando di inviare un modulo non completato.

1.  Clicca sul componente **`button`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore dell'evento Modifica, seleziona **`Mostra notifica`** in azione, **`title`** "Error!" e **`description`** "Compila il modulo prima di inviare". Scegli `Error` per **`type`**, inserisci `{{2000}}` per **`duration`** (2 secondi), e inserisci `{{barProgress1.value<100}}` per **`Solo quando avvia`.**

![bar_button_show_config](https://cdn.illacloud.com/official-website/img/official-site/components/bar_button_show_config.jpg)

#### Fase 4 Test

![bar_test](https://cdn.illacloud.com/official-website/img/official-site/components/bar_test.gif)
