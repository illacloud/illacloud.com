---
title: Upstash
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/427?languages=it&filter=basic&value=0
metaTitle: Integrazione Upstash Doc ILLA Cloud
desc: Scopri come collegare il database Upstash Redis a ILLA Cloud e creare applicazioni in grado di interrogare, manipolare e visualizzare i dati
tagCategory: doc_menu_upstash_click
categoryName: 🔨 Integrazioni
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Cos'è Upstash?</h2>

Upstash è un servizio cloud-native senza server con Redis compatibile che fornisce un modo semplice per integrare la funzionalità Redis nelle applicazioni. Nel contesto di ILLA Cloud, l'integrazione di Upstash consente agli utenti di sfruttare le funzionalità e le funzionalità di Upstash all'interno dell'applicazione ILLA Cloud.

Questo tutorial delinea il processo di creazione di un pannello di amministrazione utilizzando ILLA Builder e Upstash in pochi semplici passaggi. ILLA Cloud è una piattaforma a basso codice per gli sviluppatori che consente lo sviluppo rapido e la distribuzione di strumenti interni. Consente di creare pagine trascinando e rilasciando componenti UI, collegandosi a qualsiasi database o API e scrivendo JavaScript. Per saperne di più su Upstash, visita il suo sito web su https://upstash.com/(https://upstash.com/) Iniziamo!

### Imposta il back end su Upstash

Crea un account su Upstash. Puoi accedere utilizzando i tuoi account Amazon, Github o Google in alternativa.

Dopo aver effettuato l'accesso al tuo account su `Upstash`, clicca sul pulsante `Crea database` in Redis.

![up_home](https://cdn.illacloud.com/official-website/img/docs/up_home.jpg)

Nella dashboard `Upstash`, clicca `Create database`.

![up_create_db](https://cdn.illacloud.com/official-website/img/docs/up_create_db.jpg)

Nota: è possibile avere solo un database nel livello libero.

Per la configurazione del database, È possibile nominare il database come si desidera. Qui lo chiamiamo `db1`. Scegli la `regione primaria` e la `regione di lettura` in base alle tue posizioni di scrittura e lettura dell'utente. Abilita `TSL/SSL`. È facoltativo per abilitare Eviction.

![up_db_config](https://cdn.illacloud.com/official-website/img/docs/up_db_config.jpg)

Clicca sul pulsante **`create`** e ora abbiamo un database pronto per l'uso.

### Crea Upstash in ILLA Cloud

Ci sono due modi per creare una risorsa in Illa dopo aver effettuato l'accesso al tuo account Illa.

-   Crea in Risorse

Accedi al tuo account Illa, seleziona `**Risorse**` nella parte superiore della pagina e clicca sul pulsante `**Crea nuovo**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Seleziona **`Upstash`** dalla lista dei database.

![action_list_0](https://cdn.illacloud.com/official-website/img/docs/action_list_0.jpg)

Connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

Dopo aver creato una risorsa, il `Upstash` pronto verrà visualizzato come mostrato.

![up_ex_ready](https://cdn.illacloud.com/official-website/img/docs/up_ex_ready.jpg)

-   Crea in Costruttore

Accedi al tuo account Illa, crea un progetto in Illa Builder nella pagina `**App**`, e vai alla **`Action List`** in fondo alla pagina. Clicca `**nuovo**`, quindi seleziona **`Upstash`** dalla lista dei database. Poi, connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

### Impostazioni Di Connessione

Qui abbiamo bisogno di fornire informazioni per la connessione al database Upstash.

![up_config](https://cdn.illacloud.com/official-website/img/docs/up_config_rev.jpg)

| Proprietà   | Richiesto    | Descrizione                                                                                                                               |
| ----------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Nome        | obbligatorio | Il nome della risorsa quando si creano azioni nell'ILLA.                                                                                  |
| Hostname    | obbligatorio | L'URL o l'indirizzo IP per il tuo database                                                                                                |
| Porta       | obbligatorio | Il numero di porta dell'host del server che dovresti usare per connetterti. Se non si specifica una porta, la porta predefinita è '6379'. |
| Password    | obbligatorio | Usa questa password per l'autenticazione.                                                                                                 |
| Opzione SSL | obbligatorio | se abilitare la SSL (consigliare di abilitare)                                                                                            |

Per compilare le informazioni richieste, vai alla pagina del cruscotto del database e clicca sul database che abbiamo appena creato nel tuo Upstash.

![up_info](https://cdn.illacloud.com/official-website/img/docs/up_info.jpg)

È possibile copiare il mouse sopra il punto finale, la password e la porta e fare clic sull'icona di testo copia dietro di esso.

Nella configurazione di integrazione Upstash in ILLA Cloud, inserisci il nome che vuoi per `Name`, `Endpoint` per `hostname`, dato `Port` per `Port`, e copia la `Password` crittografata per `Password`.

![up_config_done](https://cdn.illacloud.com/official-website/img/docs/up_config_rev_done.jpg)

### Crea Azioni

Abbiamo creato una risorsa Upstash, possiamo aggiungere l'azione selezionando Upstash dall'elenco delle azioni e scegliendo il pulsante **`Crea azione`**.

![up_resource_list](https://cdn.illacloud.com/official-website/img/docs/up_resource_list.jpg)

Ora abbiamo aggiunto il server Upstash come azione alla nostra pagina di costruzione.

![upstash](https://cdn.illacloud.com/official-website/img/docs/upstash.jpg)

### Configura Upstash

| Proprietà            | Descrizione                                                                           |
| -------------------- | ------------------------------------------------------------------------------------- |
| Interrogazione Redis | utilizzare la query per recuperare e manipolare i dati memorizzati nel database Redis |
| Trasformatore        | trasformare i dati nello stile che ti piace usando JavaScript                         |

Esempio di utilizzo:

1.  Ottieni Valori:
    -   Ottieni un valore: usa il comando **`GET`** per recuperare il valore associato a una chiave specifica. Per esempio: **`GET key`**.
2.  Imposta Valori:
    -   Imposta un valore: usa il comando **`SET`** per impostare il valore di una chiave. Per esempio **`SET key value`**.
3.  Operazioni Di Hash:
    -   Imposta coppie di valori di campo in un hash: Usa il comando **`HSET`** per impostare una coppia di valori di campo in un hash. Per esempio: **`HSET hashKey valore del campo`**.
    -   Ottieni un valore da un hash: usa il comando **`HGET`** per recuperare il valore di un campo specifico in un hash. Per esempio: **`HGET hashKey field`**.
4.  Elenco Operazioni:
    -   Aggiungi un elemento a una lista: usa il comando **`LPUSH`** o **`RPUSH`** per aggiungere un elemento all'inizio o alla fine di una lista, rispettivamente. Per esempio: **`LPUSH listKey value`**.
    -   Recupera elementi da una lista: usa il comando **`LRANGE`** per recuperare un intervallo di elementi da una lista. Per esempio: **`LRANGE listKey start end`**.
5.  Imposta Operazioni:
    -   Aggiungi elementi a un set: usa il comando **`SADD`** per aggiungere uno o più elementi a un set. Per esempio: **`SADD setKey value1 value2`**.
    -   Recupera elementi da un set: usa il comando **`SMEMBERS`** per ottenere tutti i membri di un set. Per esempio: **`SMEMBERS setKey`**.
6.  Operazioni Set Ordinate:
    -   Aggiungi elementi a un set ordinato: usa il comando **`ZADD`** per aggiungere elementi a un set ordinato insieme ai loro punteggi. Per esempio: **`ZADD sortedSetKey score1 member1 score2 member2`**.
    -   Recupera gli elementi da un set ordinato: Usa il comando **`ZRANGE`** per recuperare un intervallo di elementi da un set ordinato in base ai loro punteggi. Per esempio: **`ZRANGE sortedSetKey start end`**.

Nota: È possibile fare riferimento alla documentazione Redis per un elenco completo di comandi e il loro utilizzo.

### Caso di utilizzo

Successivamente, dimostreremo come mappare i dati da `**Upstash**` al componente `table`.

#### Passo 1 Aggiungere un'azione

Creiamo una tabella in Upstash chiamata **`employee_profile`,** che include 4 colonne `name`, `level`, `age`, e `sposato`.

Quindi possiamo creare una nuova azione per Upstash dall'elenco di azioni in ILLA Cloud e il nome upstash2.

Per elencare tutti i dati nel profilo dipendente_inserire la snippet di codice qui sotto nella sezione Ricerca Redis.

```sql
GET dipendente_profilo
```

Poiché i dati di output sono un elenco di coppie di dizionari, vogliamo trasformarli in un array. In Transformer, abilitarlo e mettere giù il codice qui sotto.

```jsx
const jsonString = data[0].result;

// Parse the JSON string into a JavaScript object
const jsonData = JSON.parse(jsonString);

// Access the "employees" property and map it to a new array
const mappedArray = jsonData.employees.map((employee) => ({
  name: employee.name,
  level: employee.level,
  age: employee.age,
  married: employee.married,
}));

return mappedArray;
```

![up_code](https://cdn.illacloud.com/official-website/img/docs/up_code.jpg)

Clicca su **`Salva`** e **`Esegui`** per attivare questa azione.

![up_data](https://cdn.illacloud.com/official-website/img/docs/up_data.jpg)

#### Passo 2 Aggiungi Componenti

Successivamente, possiamo aggiungere un componente **`table`** alla tela.

Nel campo **`Fonte dati`** nella pagina Ispeziona dopo aver cliccato sul componente, inserisci `{{upstash2.data}}` per recuperare i dati dall'upstash.

#### Fase 3 Test

L'aspetto finale dovrebbe essere come mostrato.

![up_test](https://cdn.illacloud.com/official-website/img/docs/up_test.jpg)
