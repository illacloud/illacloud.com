---
title: REST API
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/385?languages=it&filter=basic&value=0
metaTitle: Integrazione REST API Doc ILLA Cloud
desc: Scopri come utilizzare ILLA Cloud per accedere e lavorare con i dati REST API. Costruisci app, visualizza i dati e automatizza i flussi di lavoro con ILLA Cloud.
tagCategory: doc_menu_rest_api_click
categoryName: 🔨 Integrazioni
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Cos'è REST API?</h2>

In ILLA Cloud, la funzionalità REST API consente agli sviluppatori di interagire con sistemi esterni, servizi e fonti di dati utilizzando lo stile architettonico di Trasferimento di Stato Rappresentativo (REST). Con l'API REST in ILLA Cloud, gli sviluppatori possono integrare perfettamente le loro applicazioni a basso codice con altre applicazioni, database o servizi.

L'API REST in ILLA Cloud consente di creare endpoint personalizzati in grado di gestire le richieste HTTP, come GET, POST, PUT, e DELETE. Questo consente agli sviluppatori di eseguire varie operazioni, tra cui il recupero dei dati, l'invio dei dati, l'aggiornamento dei record e l'eliminazione delle informazioni.

### Crea REST API

Ci sono due modi per creare una risorsa in Illa dopo aver effettuato l'accesso al tuo account Illa.

-   Crea in Risorse

Accedi al tuo account Illa, seleziona `**Risorse**` nella parte superiore della pagina e clicca sul pulsante `**Crea Nuovo**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Seleziona **`REST API`** dall'elenco dei database.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

Dopo aver creato una risorsa, l'API REST pronta verrà visualizzata come mostrato.

![rest_ex_ready](https://cdn.illacloud.com/official-website/img/official-site/components/rest_ex_ready.jpg)

-   Crea in Costruttore

Accedi al tuo account Illa, crea un progetto in Illa Builder nella pagina `**App**`, e vai alla **`Action List`** in fondo alla pagina. Fare clic su `**nuovo**`, quindi selezionare **`REST API`** dall'elenco dei database. Poi, connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

### Impostazioni Di Connessione

Qui abbiamo bisogno di fornire informazioni per la connessione al database REST API.

![rest_config](https://cdn.illacloud.com/official-website/img/official-site/components/rest_config.jpg)

| Proprietà           | Richiesto    | Descrizione                                                                                        |
| ------------------- | ------------ | -------------------------------------------------------------------------------------------------- |
| Nome                | obbligatorio | Il nome della risorsa quando si creano azioni nell'ILLA.                                           |
| Url Base            | obbligatorio | L'URL o l'indirizzo IP per il tuo database                                                         |
| Parametri URL       | opzionale    | fornire informazioni aggiuntive o modificare il comportamento delle richieste API                  |
| Intestazioni        | opzionale    | metadati aggiuntivi sulla richiesta o sulla risposta API                                           |
| Cookie              | opzionale    | piccoli pezzi di dati che vengono memorizzati sul lato client e inviati con ogni richiesta API     |
| Autenticazione      | opzionale    | verificare l'identità dell'utente o dell'applicazione che fa la richiesta API                      |

### Crea Azioni

Abbiamo creato una risorsa REST API; possiamo aggiungere l'azione selezionando REST API dalla lista delle azioni e scegliendo il pulsante **`Crea azione`**.

![rest_resource_list](https://cdn.illacloud.com/official-website/img/official-site/components/rest_resource_list.jpg)

Ora abbiamo aggiunto il server REST API come azione alla nostra pagina di costruzione.

![rest](https://cdn.illacloud.com/official-website/img/official-site/components/rest.jpg)

### Configura REST API

| Proprietà          | Descrizione                                                                                                                           |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| Tipo Di Azione     | GET, POST, PUT, HEAD, PATCH, DELETE, OPZIONI                                                                                          |
| Parametro URL      | fornire informazioni aggiuntive o modificare il comportamento delle richieste API se non specificato nell'impostazione di connessione |
| Intestazioni       | metadati aggiuntivi sulla richiesta o risposta API se non specificati nell'impostazione di connessione                                |
| Trasformatore      | trasformare i dati nello stile che ti piace usando JavaScript                                                                         |

#### OTTA

recuperare o recuperare dati da un server. Quando si effettua una richiesta GET, il client specifica l'URL della risorsa che desidera accedere e il server risponde con i dati richiesti.

#### POST

crea una nuova risorsa sul server. Permette ai client di inviare dati al server per essere elaborati e memorizzati. Quando si invia una richiesta POST, il cliente include i dati nel corpo della richiesta.

#### PUT

aggiornare o sostituire una risorsa esistente. Viene tipicamente utilizzato per modificare lo stato o il contenuto di una risorsa identificata da un URL specifico.

#### TESTA

simile al metodo GET, ma con una differenza fondamentale: il metodo HEAD recupera solo le intestazioni della risposta, senza il corpo di risposta.

#### PERCORSO

aggiornare una risorsa specifica. È tipicamente utilizzato per apportare modifiche parziali a una risorsa esistente senza sostituire l'intera risorsa.

#### ELIMINA

richiedere la rimozione di una risorsa o dati specifici identificati da un determinato URL

### Caso di utilizzo

Dimostreremo come riempire automaticamente le informazioni del modulo dai dati REST API.

#### Passo 1 Aggiungere un'azione

Creiamo una tabella nell'API REST con colonne `id`, `name`, e `age`. Il documento id 1 è ciò che vogliamo mostrare nel componente del modulo.

![rest_get_all](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_all.jpg)

Quindi possiamo creare una nuova azione per REST API dalla lista di azioni nell'app che vogliamo generare, elencando tutti i dati nel secchio e denominato restapi1.

Seleziona `GET` per `**Tipo di azione**`. Nei Parametri URL, inserisci `id` per la chiave e `1` per il valore**.**

Clicca su **`Salva`** e **`Esegui`** per attivare questa azione.

![rest_get_1](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_1.jpg)

#### Passo 2 Aggiungi Componenti

Aggiungi un componente **`form`** con il testo predefinito "Form" e un pulsante "Invia" per la superficie.

Aggiungi un componente **`input`**, un componente **`number input`** e un componente **`button`** da `**Inserisci**` nel modulo.

Etichettare il componente in ingresso per essere "Nome", il componente in ingresso numero per essere "Età" e il componente pulsante per essere "Riempimento automatico".

Imposta il segnaposto del componente di input su "Inserisci qui il tuo nome" e il valore predefinito del numero di input su 0 come mostrato di seguito.

![rest_layout](https://cdn.illacloud.com/official-website/img/official-site/components/rest_layout.jpg)

#### Passo 3 Configura il componente

Per i componenti "Riempimento automatico" **`button`** possiamo configurarlo per impostare il valore nel componente **`input`**.

1.  Fare clic sul componente "Riempimento automatico" **`button`** per aprire la sua pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`input`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli l'azione **`setValue`** e imposta il valore come `{{restapi1.data[0]["name"]}}`

![rest_get_input](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_input.jpg)

Allo stesso modo, per i componenti "Auto Fill" **`button`** per impostare il valore nel componente **`number input`**.

1.  Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`number input`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli l'azione **`setValue`** e imposta il valore come `{{restapi1.data[0]["age"]}}`

![rest_get_num](https://cdn.illacloud.com/official-website/img/official-site/components/rest_get_num.jpg)

Per il componente "Invia" **`button`** possiamo configurarlo per inviare il modulo se il modulo è completato

1.  Clicca sul componente **`button`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`form`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli il metodo **`submit`**

![rest_submit_config](https://cdn.illacloud.com/official-website/img/official-site/components/rest_submit_config.jpg)

#### Fase 4 Test

![rest_test](https://cdn.illacloud.com/official-website/img/official-site/components/rest_test.gif)
