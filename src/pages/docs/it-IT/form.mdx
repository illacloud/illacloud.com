---
title: Modulo
metaTitle: Modulo Componente Doc ILLA Cloud
desc: Scopri come utilizzare il componente del modulo per personalizzare la tua app in ILLA Cloud.
tagCategory: doc_menu_form_click
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/397?languages=it&filter=basic&value=0
categoryName: 🧬 Assembla i componenti
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>Cos'è il Form?</h2>

Il componente del modulo è un elemento visivo che rappresenta il progresso o il completamento di un compito o di un processo. Mostra una barra orizzontale che riempie o si restringe in base al valore di avanzamento specificato.

### Proprietà

| Proprietà                                 | Descrizione                                                                                                                                                          |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Gestore eventi                            | rilevare e rispondere a specifiche azioni o eventi dell'utente, come click, tasti o presentazioni di moduli.                                                         |
| Disabilitato                              | non interattivo e non può essere modificato o attivato dall'utente.                                                                                                  |
| Disabilita invio                          | disabilitando la funzionalità di invio del modulo.                                                                                                                   |
| Convalida input nell'invio                | il modulo in entrata deve essere convalidato per correttezza o completezza al momento della presentazione del modulo.                                                |
| Ripristina dopo aver inviato con successo | reimposta automaticamente i suoi valori e lo stato ai loro valori iniziali o predefiniti dopo un invio riuscito                                                      |
| Suggerimento                              | un piccolo messaggio pop-up o informativo che appare quando l'utente passa sopra o interagisce con un elemento specifico, come un componente di modulo.              |
| Mostra intestazione                       | se un componente del modulo debba mostrare una sezione di intestazione o titolo                                                                                      |
| Mostra piè di pagina                      | se un componente del modulo debba mostrare una sezione a piè di pagina                                                                                               |
| Hidden                                    | nasconde il componente del modulo dalla vista dell'utente                                                                                                            |
| Colore Del Bordo                          | colore del bordo che circonda un componente del modulo                                                                                                               |
| Raggio Bordo                              | curvatura degli angoli del bordo di un componente di forma                                                                                                           |
| Border Width                              | spessore o larghezza del bordo che circonda un componente di forma                                                                                                   |
| Sfondo                                    | definisce il colore o l'immagine che riempie l'area di contenuto di un componente del modulo                                                                         |
| Ombra                                     | effetto visivo dell'aggiunta di un sottile, offset "ombra" ad un componente di forma, facendolo apparire leggermente sollevato o fluttuante sopra lo sfondo          |

### Metodo

È possibile utilizzare altri componenti per controllare il componente. Sosteniamo i due metodi seguenti:

-   **invia**

Per inviare le informazioni compilate nel campo del modulo

-   **reset**

reimpostare tutti i campi di input nel componente del modulo

-   **setValue**

Per impostare il valore di input dell'area di testo, ad esempio, {{"value1"}}

| Proprietà   | Descrizione |
| ----------- | ----------- |
| Valore      | Input value |

-   validate

Per verificare che le informazioni di input siano legali

### Dati

Il componente ha alcuni dati comunemente usati, che possono essere chiamati tramite **`{{componentName.propertyName}}`** nell'app.

| Nome proprietà            | Descrizione                                                                                                                                                             |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| backgroundColor           | specifica il colore di sfondo di un componente del modulo                                                                                                               |
| borderColor               | definisce il colore del bordo che circonda un componente del modulo                                                                                                     |
| borderWidth               | determina lo spessore o la larghezza del bordo che circonda un componente di forma                                                                                      |
| disabilitato              | se un componente del modulo è disabilitato o no                                                                                                                         |
| displayName               | rappresenta il nome o l'etichetta assegnato a un componente del modulo                                                                                                  |
| footerHeight              | specifica l'altezza o la dimensione della sezione a piè di pagina di un componente del modulo                                                                           |
| formData                  | contiene i dati o i valori inseriti dall'utente in un componente del modulo                                                                                             |
| headerHeight              | determina l'altezza o la dimensione della sezione di intestazione di un componente del modulo                                                                           |
| non valido                | se l'input o il valore di un componente del modulo sono considerati non validi o meno.                                                                                  |
| radius                    | definisce il raggio di bordo o la curvatura degli angoli di un componente di forma                                                                                      |
| ResetAftersuccessful      | indica che un componente del modulo dovrebbe reimpostare automaticamente i suoi valori e indicare i loro valori iniziali o predefiniti dopo un invio riuscito           |
| ombra                     | controlla la presenza e l'aspetto di un effetto ombra su un componente di forma                                                                                         |
| showFooter                | determina se la sezione a piè di pagina di un componente del modulo deve essere visualizzata o meno                                                                     |
| showHeader                | visibilità della sezione di intestazione di un componente del modulo                                                                                                    |
| tooltipText               | contiene il testo o il contenuto di un suggerimento associato a un componente del modulo                                                                                |
| validateInputOnSubmit     | specifica che i valori di input di un componente del modulo devono essere convalidati per correttezza o completezza quando il modulo è presentato                       |

Esempio: {{form1.disabled}}

### Caso di utilizzo

Dimostreremo come fare un modulo con nome ed età come input, così come i progressi di barra per mostrare l'avanzamento del completamento del modulo.

#### Passo 1 Aggiungi Componenti

Aggiungi un componente **`form`** con il testo predefinito "Form" e un pulsante "Invia" per la superficie.

Aggiungi un componente **`input`**, un componente **`number input`** e un componente **`bar progress`** da `**Inserisci**` nel modulo.

Etichetta il componente di input come "Nome", il componente di input numerico come "Età" e il componente di avanzamento barra come "Progress".

Imposta il segnaposto del componente di input su "Inserisci qui il tuo nome" e il valore predefinito del numero di input su 0 come mostrato di seguito.

![bar_layout](https://cdn.illacloud.com/official-website/img/official-site/components/bar_layout.jpg)

#### Passo 2 Configura il componente

Per i componenti **`input`** e **`number input`** possiamo configurarli per impostare il valore nel componente **`bar progress`** per riflettere l'avanzamento del completamento.

1.  Clicca sul componente **`input`** e **`number input`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`bar progress`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli l'azione **`setValue`** e imposta il valore come

```jsx
{
  {
    input1.value && numberInput1.value
      ? "100"
      : input1.value || numberInput1.value
      ? "50"
      : "0";
  }
}
```

![bar_input_num_config](https://cdn.illacloud.com/official-website/img/official-site/components/bar_input_num_config.jpg)

Nota: è necessario impostare il gestore degli eventi come descritto sopra ai componenti di input BOTH e di input numerico.

Per il componente **`button`** possiamo configurarlo per inviare il modulo se il modulo è completato (cioè avanzamento barra 100%)

1.  Clicca sul componente **`button`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, e seleziona il componente **`form`** che vuoi aggiornare come obiettivo dell'evento.
3.  Scegli il metodo **`submit`** e inserisci `{{barProgress1.value == 100}}` in **`Solo quando avvia`**

![bar_button_submit_config](https://cdn.illacloud.com/official-website/img/official-site/components/bar_button_submit_config.jpg)

Allo stesso modo, possiamo configurare il pulsante per mostrare un avviso quando l'utente sta cercando di inviare un modulo non completato.

1.  Clicca sul componente **`button`** per aprire la pagina di ispezione. Sotto **`Gestore evento`**, clicca `**+ Nuovo**`.
2.  Nel gestore dell'evento Modifica, seleziona **`Mostra notifica`** in azione, **`title`** "Error!" e **`description`** "Compila il modulo prima di inviare". Scegli `Error` per **`type`**, inserisci `{{2000}}` per **`duration`** (2 secondi), e inserisci `{{barProgress1.value<100}}` per **`Solo quando avvia`.**

![bar_button_show_config](https://cdn.illacloud.com/official-website/img/official-site/components/bar_button_show_config.jpg)

#### Fase 4 Test

![bar_test](https://cdn.illacloud.com/official-website/img/official-site/components/bar_test.gif)
