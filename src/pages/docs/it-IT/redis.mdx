---
title: Redis
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/347?languages=it&filter=basic&value=0
metaTitle: Integrazione Redis Doc ILLA Cloud
desc: Scopri come connettersi e interagire con il database Redis utilizzando la funzione di integrazione del database di ILLA Cloud. Consulta questa guida per maggiori dettagli.
tagCategory: doc_menu_redis_click
categoryName: 🔨 Integrazioni
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Che cos'è Redis?</h2>

Redis in Illa è una integrazione di database che consente di connettersi e interagire con un database Redis. Redis è un popolare sistema di gestione dei database relazionale open-source noto per la sua velocità, affidabilità e facilità di utilizzo.

Con l'integrazione di Redis in Illa, è possibile interrogare, inserire, aggiornare ed eliminare i dati da un database Redis utilizzando i comandi SQL. È inoltre possibile creare query SQL personalizzate ed eseguirle all'interno di Illa. Ciò facilita la creazione di applicazioni e dashboard guidate dai dati che visualizzano dati in tempo reale da un database Redis.

### Crea Redis

Ci sono due modi per creare una risorsa in Illa dopo aver effettuato l'accesso al tuo account Illa.

-   Crea in Risorse

Accedi al tuo account Illa, seleziona `**Risorse**` nella parte superiore della pagina e clicca sul pulsante `**Crea Nuovo**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Seleziona **`Redis`** dalla lista dei database.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

Dopo aver creato una risorsa, i Redis pronti verranno visualizzati come mostrato.

![redis_ex_ready](https://cdn.illacloud.com/official-website/img/docs/redis_ex_ready.png)

-   Crea in Costruttore

Accedi al tuo account Illa, crea un progetto in Illa Builder nella pagina `**App**`, e vai alla **`Action List`** in fondo alla pagina. Fare clic su `**nuovo**`, quindi selezionare **`Redis`** dall'elenco dei database. Poi, connettiti al database con i parametri richiesti descritti in `Impostazioni di connessione` qui sotto.

Clicca su `**Test Connection**` per vedere se possiamo connetterci correttamente al database. Se sì, fai clic su **`Salva risorse`**, doppi controlli il nome host, la porta, il nome utente e la password sono corretti.

### Impostazioni Di Connessione

Qui abbiamo bisogno di fornire informazioni per la connessione al database Redis.

![redis_config](https://cdn.illacloud.com/official-website/img/docs/redis_config.png)

| Proprietà       | Richiesto    | Descrizione                                                                                                                               |
| --------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Nome            | obbligatorio | Il nome della risorsa quando si creano azioni nell'ILLA.                                                                                  |
| Hostname        | obbligatorio | L'URL o l'indirizzo IP per il tuo database                                                                                                |
| Porta           | obbligatorio | Il numero di porta dell'host del server che dovresti usare per connetterti. Se non si specifica una porta, la porta predefinita è '6379'. |
| Indice Database | opzionale    | una struttura di dati utilizzata per ottimizzare le prestazioni delle interrogazioni che recuperano i dati da un database Redis           |
| Database        | obbligatorio | Il nome della banca dati                                                                                                                  |
| Username        | obbligatorio | il nome utente che si desidera utilizzare quando si accede al server Redis.                                                               |
| Password        | obbligatorio | Usa questa password per l'autenticazione.                                                                                                 |
| Opzioni SSL     | opzionale    | decide quanto alta sia prioritaria una connessione SSL TCP/IP sicura durante le trattative con il server.                                 |

### Crea Azioni

Abbiamo creato una risorsa Redis, possiamo aggiungere l'azione selezionando Redis dall'elenco delle azioni e scegliendo il pulsante **`Crea azione`**.

![redis_resource_list](https://cdn.illacloud.com/official-website/img/docs/redis_resource_list.png)

Ora abbiamo aggiunto il server Redis come azione alla nostra pagina di costruzione.

![redis](https://cdn.illacloud.com/official-website/img/docs/redis.png)

### Configura Redis

| Proprietà     | Descrizione                                                   |
| ------------- | ------------------------------------------------------------- |
| query         | comandi per recuperare i dati dal database                    |
| Trasformatore | trasformare i dati nello stile che ti piace usando JavaScript |

Esempio di utilizzo:

-   CHIAVI

Questo comando recupera un elenco di tutte le chiavi nel database Redis. È possibile utilizzare caratteri jolly per filtrare i tasti per modello.

```sql
KEYS *
KEYS user:*
```

-   OTTA

Questo comando recupera il valore associato a una determinata chiave.

```sql
GET user:12345
```

-   HGETALL

Questo comando recupera tutti i campi e i loro valori da una struttura dati di hash Redis.

```sql
HGETALL user:12345
```

-   ZRANGE

Questo comando recupera un intervallo di valori da una struttura di dati ordinata Redis, in base ai loro punteggi.

```sql
ZRANGE high_score 0 9 WITHSCORES
```

-   GRANDI

Questo comando recupera un intervallo di valori da una struttura di dati della lista Redis.

```sql
LRANGE my_list 0 4
```

-   ESISTI

Questo comando controlla se una determinata chiave esiste nel database Redis.

```sql
EXISTS user:12345
```

-   TTL

Questo comando recupera il valore time-to-live (TTL) associato a una chiave, che specifica per quanto tempo la chiave rimarrà nel database prima della scadenza.

```sql
TTL user:12345
```
