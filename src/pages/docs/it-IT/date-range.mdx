---
title: Date Range
metaTitle: Date Range Component Doc | ILLA Cloud
desc: Scopri come utilizzare il componente Gamma di date da ILLA Cloud per consentire agli utenti di selezionare una data di inizio e di fine da un'interfaccia utente del calendario nei moduli o nelle interfacce di inserimento dati.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/337?languages=it&filter=basic&value=0
tagCategory: doc_menu_date_range_click
categoryName: 🧬 Assembla i componenti
categoryPriority: 6
postPriority: 4
---

## <h2 hidden>Che cos'è l'intervallo di dati?</h2>

Il componente intervallo di date è un componente UI che consente agli utenti di selezionare un intervallo di date da un'interfaccia di calendario, tipicamente utilizzato in moduli o interfacce di inserimento dati in cui l'utente deve specificare un intervallo di date di inizio e di fine.

Quando un componente Gamma di date viene aggiunto alla tela, visualizza due interfacce di calendario fianco a fianco. Un'interfaccia di calendario viene utilizzata per selezionare la data di inizio, mentre l'altra viene utilizzata per selezionare la data di fine. L'utente può navigare attraverso i calendari facendo clic sulle frecce precedenti o successive per passare a un mese diverso, o facendo clic su una data specifica per selezionarla. Le date selezionate verranno visualizzate nei campi di input.

### Properties

Date Range proprietà disponibili. JavaScript può essere scritto per leggere o modificare informazioni sui componenti.

| Proprietà                       | Descrizione                                                                                                                                                                               |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Data di inizio                  | Il valore predefinito della data di inizio del componente                                                                                                                                 |
| Data di fine                    | Il valore predefinito della data di fine del componente                                                                                                                                   |
| Formato                         | Una stringa di formato data valida. Vedi https://day.js.org/docs/en/parse/string-format.                                                                                                  |
| Segnaposto Iniziale             | Il valore verrà mostrato quando il campo di immissione della data di inizio è vuoto.                                                                                                      |
| Segnaposto Finale               | Il valore verrà mostrato quando il campo di inserimento della data di fine è vuoto.                                                                                                       |
| Data massima                    | imposta la data massima che un utente può selezionare dall'interfaccia del calendario.                                                                                                    |
| Data minima                     | specifica la prima data in cui un utente può selezionare dall'interfaccia del calendario.                                                                                                 |
| Etichetta                       | Il nome del campo visualizzato all'utente                                                                                                                                                 |
| Didascalia                      | Una didascalia utilizzata per descrivere in dettaglio il campo                                                                                                                            |
| Etichetta nascosta              | Imposta se visualizzare l'etichetta                                                                                                                                                       |
| Posizione                       | Imposta la posizione dell'etichetta rispetto al componente                                                                                                                                |
| Allineamento                    | Imposta l'allineamento (allineare a sinistra o a destra) dell'etichetta                                                                                                                   |
| Width                           | Quando l'etichetta è sul lato sinistro del componente, impostare il rapporto di larghezza dell'etichetta.                                                                                 |
| Gestore Eventi                  | Attiva interrogazioni, componenti di controllo o chiama altre API in risposta a eventi di componenti.                                                                                     |
| Disabilitato                    | Controlla lo stato se il componente è disabilitato. Il componente non può essere modificato o focalizzato quando è disabilitato.                                                          |
| Sola Lettura                    | Controllare lo stato se il componente è in sola lettura. Un componente di sola lettura può essere selezionato e messo a fuoco ma non può essere modificato.                               |
| Suggerimento                    | Gli utenti possono inserire il suggerimento del componente qui. Il suggerimento verrà mostrato quando è messo a fuoco. Il formato Markdown è supportato.                                  |
| Mostra pulsante cancella        | controllare se un pulsante chiaro è visualizzato nel campo di input                                                                                                                       |
| Campo obbligatorio              | Valido solo quando l'interruttore è attivo.                                                                                                                                               |
| Regola personalizzata           | Crea qui la tua logica di convalida. Le regole devono essere fatte in JavaScript e coperte da {{}}.                                                                                       |
| Nascondi messaggio di convalida | È possibile nascondere il messaggio di errore cambiando lo stato nascosto quando il valore di input non è corretto. È possibile modificare dinamicamente lo stato nascosto da JavaScript. |
| Modulo Dati Chiave              | Specifica una chiave di un componente di un modulo di wrapping durante la costruzione dell'attributo dati.                                                                                |
| Hidden                          | Controllare dinamicamente se il componente è nascosto. È possibile modificare lo stato nascosto attraverso il valore booleano dinamico.                                                   |
| Colore Tema                     | Consente agli utenti di specificare il colore di sfondo e l'opacità del pulsante                                                                                                          |

### Methods

È possibile utilizzare altri componenti per controllare il componente. Sosteniamo i due metodi seguenti:

-   **setStartValue**

Per impostare il valore della data di inizio, per esempio, {{"value1"}}

| Proprietà | Descrizione           |
| --------- | --------------------- |
| Valore    | Input valore iniziale |

-   **setEndValue**

Per impostare il valore della data di fine, ad esempio, {{"value2"}}

| Proprietà | Descrizione     |
| --------- | --------------- |
| Valore    | Input end value |

-   **clearValue**

Per cancellare il valore del componente selezionato 

-   **validate**

Per verificare che le informazioni di input siano legali

-   **clearValidate**

Per cancellare il messaggio di convalida

### Gestore eventi

| Evento | Descrizione                                                                                                         |
| ------ | ------------------------------------------------------------------------------------------------------------------- |
| Cambia | Quando un utente cambia il valore della data selezionato, eseguire un'azione specifica personalizzata dagli utenti. |

### Dati

Il componente ha alcuni dati comunemente usati, che possono essere chiamati tramite **`{{componentName.propertyName}}`** nell'app.

| Nome proprietà        | Descrizione                                                       |
| --------------------- | ----------------------------------------------------------------- |
| colorScheme           | la proprietà colore di Data                                       |
| customRule            | la regola personalizzata specificata dall'utente                  |
| dateFormat            | il formiato della data                                            |
| disabilitato          | un valore booleano indica lo stato disabilitato                   |
| displayName           | il nome di questo componente (ossia cascader1)                    |
| startPlaceholder      | valore segnaposto per la data di inizio                           |
| endPlaceholder        | valore segnaposto per la data di fine                             |
| eventi                | un valore Array per la sequenza di eventi                         |
| formDataKey           | il Modulo Data Key of Date                                        |
| hidden                | un valore booleano indica lo stato nascosto del pulsante          |
| hideValidationMessage | un valore booleano indica se il messaggio di convalida è nascosto |
| etichetta             | valore dell'etichetta                                             |
| labelAllinea          | allineamento dell'etichetta cascader (sinistra o destra)          |
| labelPosizione        | posizione dell'etichetta cascader (sinistra o destra)             |
| labelWidth            | il numero intero che rappresenta la larghezza dell'etichetta      |
| readOnly              | un valore booleano indica lo stato readyOnly di input.            |
| obbligatorio          | un valore booleano indica lo stato richiesto dell'input.          |
| mostraPulisci         | un valore booleano indica che la data sarà mostrata pulita        |
| valore                | Valore selezionato                                                |

Esempio: {{dateRange1.value[0]}}

### Caso di utilizzo

Successivamente, dimostreremo come mappare i dati dall'origine dei dati all'intervallo di date e come impostare il valore finale alla data di oggi con un pulsante. 

#### Passo 1 Aggiungere un'azione

Creiamo una tabella nell'API REST chiamata **`promoteCodes`,** con 6 colonne: **`code`**, **`id`**, **`createdAt`**, **`expiredAt`**, **`startedAt`, `updatedAt`**. Useremo l'inizioAt e scadutoAl momento per l'uso del campione. 

Quindi possiamo creare una nuova azione per REST API dall'elenco di azioni, elencando tutti i dati in promoteCodes e chiamato restapi1. 

Clicca su **`Salva`** e **`Esegui`** per attivare questa azione.

#### Passo 2 Aggiungi Componenti

Successivamente, possiamo aggiungere un componente **`date range`** e un componente **\``button`** alla tela. Per il valore predefinito della data di inizio del componente dell'intervallo di date, usiamo l'inizioAl momento del primo codice di promozione dai dati api restanti.

```sql
{{restapi1.data.promoteCodes[0].startedAt}}
```

Allo stesso modo, per il valore predefinito della data di fine, usiamo scadutoAl momento del primo codice di promozione dai dati API restanti.

```sql
{{restapi1.data.promoteCodes[0].expiredAt}}
```

Abbiamo anche etichettato il pulsante come 'Aggiornamento' come mostrato di seguito.

![date_range_0](https://cdn.illacloud.com/official-website/img/docs/date_range_0.png)

#### Passo 3 Configura il componente

Per il componente **`button`** possiamo configurarlo per impostare il valore della data di fine nel componente **`date range`** alla data odierna.

1.  Nel gestore degli eventi di Modifica, seleziona **`Componente di controllo`** in azione, seleziona il componente **`date range`** che vuoi aggiornare come obiettivo dell'evento.
2.  Scegli l'azione **`setEndValue`** e imposta **`End date`** come `{{currentUserInfo.updatedAt}}` 

![date_range_config](https://cdn.illacloud.com/official-website/img/docs/date_range_config.png)

#### Fase 4 Test

Ora che quando si fa clic sul pulsante 'Aggiornamento', la data di fine dovrebbe cambiare alla data di oggi. Per quanto riguarda qui, abbiamo 2023-5-6, ma può essere diverso quando si sta testando.

![date_range_1](https://cdn.illacloud.com/official-website/img/docs/date_range_1.png)
