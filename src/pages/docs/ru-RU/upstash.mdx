---
title: Upstash
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/427?languages=ru&filter=basic&value=0
metaTitle: Upstash интеграция Doc | ILLA Cloud
desc: Узнайте, как подключить базу данных Upstash Redis к ILLA Cloud и создавать приложения, которые могут запрашивать, манипулировать и отображать данные
tagCategory: doc_menu_upstash_click
categoryName: 🔨 Интеграции
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Что такое Переключатель?</h2>

Upstash - это облачная служба без сервера с Redis совместимой, которая предоставляет простой способ интегрировать функциональность Redis в приложения. В контексте облака ILLA интеграция Upstash позволяет пользователям использовать возможности и возможности Upstash в рамках приложения ILLA.

В этом уроке описывается процесс создания панели администратора с помощью ILLA Builder и Upstash за несколько простых шагов. ILLA Cloud - это низкокодовая платформа для разработчиков, позволяющая быстро разрабатывать и устанавливать внутренние инструменты. Он позволяет создавать страницы, перетаскивая пользовательские компоненты, подключаясь к любой базе данных или API, а также писать JavaScript. Чтобы узнать больше о Upstash, посетите их веб-сайт https://upstash.com/(https://upstash.com/) Давайте начнем!

### Настройте ваш возвратный конец в Upstash

Создайте учетную запись в Upstash. Вы можете войти с помощью ваших учетных записей Amazon, Github или Google.

После входа в свою учетную запись на сайте `Upstash`, нажмите на кнопку `Создать базу данных` в Redis.

![up_home](https://cdn.illacloud.com/official-website/img/docs/up_home.jpg)

На панели инструментов `Upstash` нажмите `Создать базу данных`.

![up_create_db](https://cdn.illacloud.com/official-website/img/docs/up_create_db.jpg)

Примечание: Вы можете иметь только ОДНОЙ базы данных в свободном уровне.

Для конфигурации базы данных, вы можете назвать вашу базу данных так, как вы хотите. Здесь мы назовем его `db1`. Выберите `Основной регион` и `чтение региона` на основе записи и чтения местоположения пользователей. Включите `TSL/SSL`. Не обязательно включить выселение.

![up_db_config](https://cdn.illacloud.com/official-website/img/docs/up_db_config.jpg)

Нажмите кнопку **`create`**, теперь мы готовы к использованию.

### Создать Upstash в ILLA Cloud

Есть два способа создать ресурс в Illa после входа в ваш аккаунт Illa.

-   Создать в ресурсах

Войдите в свою учетную запись Illa и выберите `**Ресурсы**` в верхней части страницы и нажмите кнопку `**Создать новое**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Выберите **`Upstash`** из списка баз данных.

![action_list_0](https://cdn.illacloud.com/official-website/img/docs/action_list_0.jpg)

Подключитесь к базе данных с требуемыми параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

После создания ресурса готовый `Upstash` будет показан.

![up_ex_ready](https://cdn.illacloud.com/official-website/img/docs/up_ex_ready.jpg)

-   Создать в Builder

Войдите в свою учетную запись Illa и создайте проект в Illa Builder на странице `**App**`. и перейдите в **«Список действий»** внизу страницы. Нажмите `**new**`, затем выберите **`Upstash`** из списка баз данных. Затем подключитесь к базе данных с нужными параметрами, описанными в разделе «Параметры соединения».

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

### Настройки соединения

Здесь мы должны предоставить информацию для подключения к базе данных Upstash.

![up_config](https://cdn.illacloud.com/official-website/img/docs/up_config_rev.jpg)

| Свойства           | Требуется    | Описание                                                                                                                          |
| ------------------ | ------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| Наименование       | обязательный | Название ресурса при создании действий в ILLA.                                                                                    |
| Hostname           | обязательный | URL или IP-адрес базы данных                                                                                                      |
| Порт               | обязательный | Номер порта хоста сервера, который необходимо использовать для подключения. Если вы не указали порт, то по умолчанию порт '6379'. |
| Пароль             | обязательный | Используйте этот пароль для аутентификации.                                                                                       |
| Опция SSL          | обязательный | включить ли SSL (рекомендовать для включения)                                                                                     |

Чтобы заполнить необходимую информацию, перейдите на страницу управления базой данных и нажмите на базу данных, которую мы только что создали в вашем Upstash.

![up_info](https://cdn.illacloud.com/official-website/img/docs/up_info.jpg)

Его можно скопировать, наведя курсор мыши на конечную точку, пароль, порт, а также кликнув на иконку копирования за ней.

В конфигурации интеграции Upstash в ILLA Cloud, укажите нужное имя для `Name`, `Endpoint` для `hostname`, заданный `Port` для `Port`, и скопируйте зашифрованный `Password` для `Password`.

![up_config_done](https://cdn.illacloud.com/official-website/img/docs/up_config_rev_done.jpg)

### Создать действия

Мы создали ресурс Upstash, мы можем добавить действие, выбрав Upstash из списка действий и выбрав кнопку **`Create action`**.

![up_resource_list](https://cdn.illacloud.com/official-website/img/docs/up_resource_list.jpg)

Теперь мы добавили сервер Upstash в качестве действия на нашу страницу строительства.

![upstash](https://cdn.illacloud.com/official-website/img/docs/upstash.jpg)

### Настройка Upstash

| Свойства      | Описание                                                                                |
| ------------- | --------------------------------------------------------------------------------------- |
| Запрос Redis  | использовать запрос для получения и управления данными, хранящимися в базе данных Redis |
| Трансформатор | преобразование данных в понравившийся стиль JavaScript                                  |

Пример использования:

1.  Получить значения:
    -   Получить значение: используйте команду **`GET`**, чтобы получить значение, связанное с определённым ключом. Например: **`GET ключ`**.
2.  Значения:
    -   Установите значение: используйте команду **`SET`**, чтобы установить значение ключа. Например **`SET key value`**.
3.  Операции с хэшами:
    -   Установка пары значений полей в хэша: используйте команду **`HSET`**, чтобы установить пару значений в поле. Например: **`HSET hashKey значение поля `**.
    -   Получить значение из хэша: Используйте команду **`HGET`** для получения значения конкретного поля в хэшах. Например: **`HGET hashKey поле`**.
4.  Список операций:
    -   Добавьте элемент в список: Используйте команду **`LPUSH`** или **`RPUSH`**, чтобы добавить элемент в начало или конец списка, соответственно. Например: **`LPUSH listKey value`**.
    -   Получение элементов из списка: Используйте команду **`LRANGE`** для получения диапазона элементов из списка. Например: **`LRANGE listKey start end`**.
5.  Установить операции:
    -   Добавьте элементы к набору: используйте команду **`SADD`**, чтобы добавить один или несколько элементов к набору. Например: **`SADD setKey value1 value2`**.
    -   Получить элементы из набора: Используйте команду **`SMEMBERS`** для получения всех членов набора. Например: **`SMEMBERS setKey`**.
6.  Отсортированные операции:
    -   Добавьте элементы к сортированному набору: используйте команду **`ZADD`**, чтобы добавить элементы к сортированному набору вместе с их результатами. Например: **`ZADD sortedSetKey score1 member1 score2 member2`**.
    -   Получайте элементы из отсортированного набора: используйте команду **`ZRANGE`** для получения диапазона элементов из отсортированного набора, основываясь на их результатах. Например: **`ZRANGE sortedSetKey start end`**.

Примечание: Вы можете обратиться к документации Redis для получения полного списка команд и их использования.

## Вариант использования

Далее мы покажем, как сопоставить данные из `**Upstash**` в `table` компонент.

#### Шаг 1 Добавить действие

Давайте создадим в Upstash таблицу под названием **`employee_profile`,** в том числе 4 колонки `name`, `level`, `age` и `married`.

Затем мы можем создать новое действие для Upstash из списка действий в ILLA Cloud и имя upstash2.

To list all data in employee_profile put the code snippet below in the Redis query section.

```sql
Получить профиль employee_profile
```

Поскольку выходные данные являются списком пар словарей, мы хотим преобразовать их в массив. В Transformer, включите его и поместите код ниже.

```jsx
const jsonString = data[0].result;

// Распространение строки JSON в JavaScript объект
const jsonData = JSON. arse(jsonString);

// Доступ к свойству "employees" и сопоставление с новым массивом
const mappedArray = jsonData. mployees.map((employee) => ({
  name: employee.name,
  level: employee. evel,
  age: employ.age,
  married: employed.married,
}));

return mappedArray;
```

![up_code](https://cdn.illacloud.com/official-website/img/docs/up_code.jpg)

Нажмите **`Сохранить`** и **`Выполнить`** для активации этого действия.

![up_data](https://cdn.illacloud.com/official-website/img/docs/up_data.jpg)

#### Шаг 2 Добавление компонентов

Далее мы можем добавить в холст **`table`** компонент.

В поле **`Источник данных`** на странице Инспекции после нажатия на компонент, поместите `{{upstash2.data}}`, чтобы получить данные из upstash.

#### Шаг 3 Тест

Окончательный вид должен быть таким, как показано.

![up_test](https://cdn.illacloud.com/official-website/img/docs/up_test.jpg)
