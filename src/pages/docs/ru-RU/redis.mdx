---
title: Редис
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/347?languages=ru&filter=basic&value=0
metaTitle: Интеграция Redis | ILLA Cloud
desc: Узнайте, как подключать и взаимодействовать с базой данных Redis, используя интеграцию с базой данных ILLA Cloud. Посмотрите это руководство для более подробной информации.
tagCategory: doc_menu_redis_click
categoryName: 🔨 Интеграции
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Что такое повторение?</h2>

Redis in Illa - это интеграция с базой данных, которая позволяет подключать и взаимодействовать с базой данных Redis. Redis является популярной реляционной системой управления базами данных с открытым исходным кодом, известной своей скоростью, надежностью и легкостью использования.

С интеграцией Redis в Illa, вы можете запрашивать, вставлять, обновлять и удалять данные из базы данных Redis с помощью команд SQL. Вы также можете создать пользовательские SQL-запросы и запустить их в Illa. Это позволяет легко создавать приложения и панели инструментов, которые отображают данные в реальном времени из базы данных Redis.

### Создать Redis

Есть два способа создать ресурс в Illa после входа в ваш аккаунт Illa.

-   Создать в ресурсах

Войдите в учетную запись Illa и выберите `**Ресурсы**` в верхней части страницы и нажмите кнопку `**Создать новое**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Выберите **`Redis`** из списка баз данных.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Подключитесь к базе данных с требуемыми параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

После создания ресурса будет показан готовый Redis.

![redis_ex_ready](https://cdn.illacloud.com/official-website/img/docs/redis_ex_ready.png)

-   Создать в Builder

Войдите в учетную запись Illa и создайте проект в Illa Builder на странице `**App**` и перейдите в **«Список действий»** внизу страницы. Нажмите `**new**`, затем выберите **`Redis`** из списка баз данных. Затем подключитесь к базе данных с нужными параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

### Настройки соединения

Здесь мы должны предоставить информацию для подключения к базе данных Redis.

![redis_config](https://cdn.illacloud.com/official-website/img/docs/redis_config.png)

| Свойства               | Требуется    | Описание                                                                                                                                     |
| ---------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Наименование           | обязательный | Название ресурса при создании действий в ILLA.                                                                                               |
| Hostname               | обязательный | URL или IP-адрес базы данных                                                                                                                 |
| Порт                   | обязательный | Номер порта хоста сервера, который необходимо использовать для подключения. Если вы не указали порт, то порт по умолчанию — «6379».          |
| Индекс базы данных     | опционально  | структура данных, которая используется для оптимизации производительности запросов, которые извлекают данные из базы данных Redis            |
| База данных            | обязательный | Название базы данных                                                                                                                         |
| Имя пользователя       | обязательный | имя пользователя, которое вы хотите использовать при входе на сервер Redis.                                                                  |
| Пароль                 | обязательный | Используйте этот пароль для аутентификации.                                                                                                  |
| Опции SSL              | опционально  | принимает решение о том, насколько высоким является безопасное соединение с SSL TCP/IP во время переговоров с сервером.                      |

### Создать действия

Мы создали Redis ресурс, мы можем добавить действие, выбрав Redis из списка действий и выбрав кнопку **`Создать действие`**.

![redis_resource_list](https://cdn.illacloud.com/official-website/img/docs/redis_resource_list.png)

Теперь мы добавили Redis сервер в качестве действия на нашу страницу строительства.

![redis](https://cdn.illacloud.com/official-website/img/docs/redis.png)

### Настроить Redis

| Свойства      | Описание                                                       |
| ------------- | -------------------------------------------------------------- |
| запрос        | команды для получения данных из базы данных                    |
| Трансформатор | преобразование данных в понравившийся стиль JavaScript         |

Пример использования:

-   КЛАВИША

Эта команда получает список всех ключей в базе данных Redis. Вы можете использовать маски для фильтрации ключей по шаблону.

```sql
KEYS *
KEYS user:*
```

-   ПОЛУЧИТЬ

Эта команда получает значение, связанное с заданным ключом.

```sql
GET user:12345
```

-   HGETALL

Эта команда извлекает все поля и их значения из структуры хэшей Redis.

```sql
HGETALL user:12345
```

-   ZRANGE

Эта команда получает диапазон значений из отсортированной структуры данных Redis, основываясь на их результатах.

```sql
Высокий_баллы_ZRANGE 0 9 WITHSCORES
```

-   ЛЕДИНА

Эта команда получает диапазон значений из структуры данных списка Redis.

```sql
LRANGE my_list 0 4
```

-   ВЫХОДЫ

Эта команда проверяет, существует ли данный ключ в базе данных Redis.

```sql
EXISTS user: 12345
```

-   TTL

Эта команда получает значение time-to-live (TTL), связанное с ключом, которое указывает, сколько времени будет оставаться в базе данных до истечения.

```sql
TTL user:12345
```
