---
title: MongoDB
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/351?languages=ru&filter=basic&value=0
metaTitle: MongoDB Интеграция Doc | ILLA Cloud
desc: Узнайте, как использовать ILLA Cloud для создания приложений с помощью MongoDB. Исследуйте запросы данных, функции манипуляции и визуализации.
tagCategory: doc_menu_mongodb_click
categoryName: 🔨 Интеграции
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Что такое MongoDB?</h2>

MongoDB - это популярная документально ориентированная база данных NoSQL, которая широко используется для веб-приложений и другого программного обеспечения на основе данных. MongoDB предназначен для гибкого, масштабируемого и быстрого использования и особенно хорошо подходит для обработки большого количества неструктурированных или полуструктурированных данных.

В Illa вы можете подключиться к базе данных MongoDB с помощью MongoDB Query Resource. Этот ресурс позволяет выполнять MongoDB запросы прямо из Illa, не написать код. Вы можете использовать редактор запросов для написания запросов на родном языке запроса MongoDB, , который основан на JavaScript и использует для работы с документами подобный JSON-синтаксис.

### Create MongoDB

Есть два способа создать ресурс в Illa после входа в ваш аккаунт Illa.

-   Создать в ресурсах

Войдите в учетную запись Illa и выберите `**Ресурсы**` в верхней части страницы и нажмите кнопку `**Создать новое**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Выберите **`MongoDB`** из списка баз данных.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Подключитесь к базе данных с требуемыми параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

После создания ресурса будет показан готовый MongoDB.

![mongo_ex_ready](https://cdn.illacloud.com/official-website/img/docs/mongo_ex_ready.png)

-   Создать в Builder

Войдите в учетную запись Illa и создайте проект в Illa Builder на странице `**App**` и перейдите в **«Список действий»** внизу страницы. Нажмите `**new**`, затем выберите **`MongoDB`** из списка баз данных. Затем подключитесь к базе данных с нужными параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

### Настройки соединения

Здесь необходимо предоставить информацию для подключения к базе данных MongoDB.

![mongo_config](https://cdn.illacloud.com/official-website/img/docs/mongo_config.png)

| Свойства                  | Требуется    | Описание                                                                                                                                 |
| ------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Наименование              | обязательный | Название ресурса при создании действий в ILLA.                                                                                           |
| Тип конфигурации          | опционально  | тип коллекции, используемый для хранения конфигурационных данных для острых кластеров.                                                   |
| Hostname                  | обязательный | URL или IP-адрес базы данных                                                                                                             |
| Формат подключения        | обязательный | синтаксис, используемый для указания строки подключения к базе данных MongoDB или кластеру.                                              |
| Порт                      | обязательный | Номер порта хоста сервера, который необходимо использовать для подключения. Если вы не указали порт, то порт по умолчанию — «3306».      |
| База данных               | обязательный | Название базы данных                                                                                                                     |
| Имя пользователя          | обязательный | имя пользователя, которое вы хотите использовать при входе на сервер MongoDB.                                                            |
| Пароль                    | обязательный | Используйте этот пароль для аутентификации.                                                                                              |
| Опции SSL                 | опционально  | принимает решение о том, насколько высоким является безопасное соединение с SSL TCP/IP во время переговоров с сервером.                  |

### Создать действия

Мы создали ресурс MongoDB, мы можем добавить действие, выбрав MongoDB из списка действий и выбрав кнопку **`Создать действие`**.

![mongo_resource_list](https://cdn.illacloud.com/official-website/img/docs/mongo_resource_list.png)

Теперь мы добавили сервер MongoDB в качестве действия на нашу страницу строительства.

![mongodb](https://cdn.illacloud.com/official-website/img/docs/mongodb.png)

### Configure MongoDB

#### Общий обзор

| Название метода | Описание                                                                       |
| --------------- | ------------------------------------------------------------------------------ |
| Тип действия    | агрегировать, bulkwrite, count, deleteMany. deleteOne, distinct, find, findOne |
| Коллекция       | группа связанных документов, которые хранятся вместе в базе данных             |
| Трансформатор   | преобразование данных в понравившийся стиль JavaScript                         |

#### Агрегировать

обрабатывает несколько документов и возвращает рассчитанные результаты.

** Input **

| Свойства  | Требуется   | Описание                                                    |
| --------- | ----------- | ----------------------------------------------------------- |
| Агрегация | опционально | определяет операции, которые необходимо выполнить по данным |

Например, чтобы получить все результаты, размер которых средний=>группировать результаты по типу =>Вычислить сумму цен каждого типа для **`Aggregation`**:

```sql
[
	{
		"$match": { "size": "medium" }
	},
	{
		"$group": { "_id": "$type", "totalQuantity": { "$sum": "$price" } }
	}
]
```

** Вывод **

Массив вычисляемых результатов.

![mongo_agg_data](https://cdn.illacloud.com/official-website/img/docs/mongo_agg_data.png)

Используй `{{mongodb1.data[0].result}}`, чтобы поймать всех.

![mongo_agg_code](https://cdn.illacloud.com/official-website/img/docs/mongo_agg_code.png)

#### Массовая запись

выполнение нескольких операций записи (вставка, обновление и удаление) в одном запросе на сервер

** Input **

| Свойства | Требуется   | Описание                                                                          |
| -------- | ----------- | --------------------------------------------------------------------------------- |
| Операции | опционально | объект, который определяет тип операции и записываемые данные                     |

Пример для **`Operations`**:

```sql
[
	{ "insertOne": { "document": { "_id": 3, "type": "beef", "size": "medium", "price": 6 } },
	{ "insertOne": { "document": { "_id": 4, "type": "sausage", "size": "large", "price": 10 } },
	{ "updateOne": {
			"filter": { "type": "cheese" },
			"update": { "$set": { "price": 8 } }
		}
	}
]
```

** Вывод **

Обновленное сообщение отображается ниже.

![mongo_bulk_data](https://cdn.illacloud.com/official-website/img/docs/mongo_bulk_data.png)

Поскольку этот метод вносит изменения, но не возвращает ничего. Нет выходных данных для доступа.

#### Счетчик

считать количество документов, соответствующих заданному запросу в коллекции

** Input **

| Свойства    | Требуется   | Описание                                              |
| ----------- | ----------- | ----------------------------------------------------- |
| Запрос      | опционально | определяет критерии выбора для операции счетчика      |

Например, чтобы посчитать количество предметов с типом «говядина», мы можем поместить ниже кода в **`Query`**:

```sql
{"type":"говяди"}
```

** Вывод **

Количество совпадающих документов.

![mongo_count_data](https://cdn.illacloud.com/official-website/img/docs/mongo_count_data.png)

Используйте `{{mongodb1.data}}`, чтобы получить.

![mongo_count_code](https://cdn.illacloud.com/official-website/img/docs/mongo_count_code.png)

#### удалитьМного

удалить несколько документов, соответствующих данному фильтру в коллекции.

** Input **

| Свойства   | Требуется   | Описание                                                                                                                    |
| ---------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| Фильтр     | опционально | определяет критерии выбора для операции удаления, если не указано, все документы в этой коллекции будут удалены.            |

Например, чтобы удалить яблоки из элементов. Мы можем поместить код ниже **`Filter`**.

```sql
 {"type":"apple"}
```

** Вывод **

Количество совпадающих документов.

![mongo_delete2_data](https://cdn.illacloud.com/official-website/img/docs/mongo_delete2_data.png)

Используйте `{{mongodb1.data[0].result.DeletedCount}}`, чтобы получить. Если файлы не совпадают, возвращаемое значение будет равно 0.

![mongo_delete2_code](https://cdn.illacloud.com/official-website/img/docs/mongo_delete2_code.png)

#### удалитьОдин

удалить один документ, соответствующий данному фильтру в коллекции.

** Input **

| Свойства   | Требуется   | Описание                                               |
| ---------- | ----------- | ------------------------------------------------------ |
| Фильтр     | опционально | определяет критерии выбора для операции удаления       |

Например, чтобы удалить яблоко из элементов. Мы можем поместить код ниже **`Filter`**.

```sql
 {"type":"apple"}
```

** Вывод **

Удалить результат.

![mongo_delete1_data](https://cdn.illacloud.com/official-website/img/docs/mongo_delete1_data.png)

Используйте `{{mongodb1.data[0].result.DeletedCount}}`, чтобы получить. Если файлы не совпадают, то возвращаемое значение будет равно 0 (Винс, мы удалили все яблоки в **`deleteMany`**, не осталось яблок, таким образом, возвращается 0).

![mongo_delete1_code](https://cdn.illacloud.com/official-website/img/docs/mongo_delete1_code.png)

#### различный

получение массива уникальных значений для указанного поля в коллекции

** Input **

| Свойства    | Требуется    | Описание                                                 |
| ----------- | ------------ | -------------------------------------------------------- |
| Запрос      | опционально  | определяет критерии выбора для отдельной операции        |
| Поле        | обязательный | определяет поле для получения разных значений.           |

Например, у нас могут быть `{"type":"orange"}` для **`Query`** и `_id` для **`Field`**.

** Вывод **

массив отдельных значений

![mongo_dist_data](https://cdn.illacloud.com/official-website/img/docs/mongo_dist_data.png)

Используйте `{{mongodb1.data[0].result.map(item =>({"result":item})}}`, чтобы получить массив.

![mongo_dist_code](https://cdn.illacloud.com/official-website/img/docs/mongo_dist_code.png)

#### найти

извлечение документов из коллекции, которая соответствует заданному набору критериев

** Input **

| Свойства          | Требуется   | Описание                                                                                                                |
| ----------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| Запрос            | опционально | определяет критерии выбора для операции поиска                                                                          |
| Проекция          | опционально | указать, какие поля включить или исключить в результаты запроса                                                         |
| Сортировать по    | опционально | укажите порядок сортировки возвращаемых документов                                                                      |
| Лимит             | опционально | ограничить количество возвращаемых документов по умолчанию без ограничений. Предел 0 эквивалентен никаким ограничениям. |
| Пропустить        | опционально | укажите количество пропущенных документов, по умолчанию 0.                                                              |

Например, чтобы найти идентификатор, цену и тип всех апельсинов, отсортированных по их идентификатору.

Для **`Query`**, `{"type": "orange"}`

Для **`Projection`**, `{"_id": 1, "type": 1, "price": 1}`
Для **`Sort By`**, `{"_id":1}`

Примечание: заменить «1» на «true» еще сделайте это!

** Вывод **

Массив объектов документов

![mongo_find_data](https://cdn.illacloud.com/official-website/img/docs/mongo_find_data.png)

Используйте `{{mongodb1.data[0].result}}`, чтобы получить массив.

![mongo_find_code](https://cdn.illacloud.com/official-website/img/docs/mongo_find_code.png)

#### найти1

получить первый документ из коллекции, которая соответствует заданному набору критериев

** Input **

| Свойства         | Требуется   | Описание                                                              |
| ---------------- | ----------- | --------------------------------------------------------------------- |
| Запрос           | опционально | определяет критерии выбора для операции поиска                        |
| Проекция         | опционально | указать, какие поля включить или исключить в результаты запроса       |
| Пропустить       | опционально | укажите количество пропущенных документов, по умолчанию 0.            |

Например, чтобы найти идентификатор, цену и тип всех апельсинов, отсортированных по их идентификатору.

Для **`Query`**, `{"type": "orange"}`

Для **`Projection`**, `{"_id": 1, "type": 1, "price": 1}`

Примечание: заменить «1» на «true» еще сделайте это!

** Вывод **

Массив объектов документов

![mongo_find1_data](https://cdn.illacloud.com/official-website/img/docs/mongo_find1_data.png)

Используйте `{{[mongodb1.data[0].result]}}`, чтобы получить массив.

![mongo_find1_code](https://cdn.illacloud.com/official-website/img/docs/mongo_find1_code.png)
