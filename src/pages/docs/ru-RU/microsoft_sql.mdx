---
title: Microsoft SQL
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/365?languages=ru&filter=basic&value=0
metaTitle: Microsoft SQL Integration Doc | ILLA Cloud
desc: Узнайте, как использовать ILLA Cloud для доступа и работы с данными Microsoft SQL. Создавайте приложения, визуализируйте данные и автоматизируйте рабочие процессы с помощью ILLA Cloud.
tagCategory: doc_menu_microsoft_sql_click
categoryName: 🔨 Интеграции
categoryPriority: 3
postPriority: 1
---

## <h2 hidden>Что такое Microsoft SQL?</h2>

Microsoft SQL в Illa - это интеграция с базой данных, позволяющая пользователям подключаться и взаимодействовать с базой данных Microsoft SQL (MS SQL). MS SQL является популярной реляционной системой управления базами данных с открытым исходным кодом, известной своей скоростью, надежностью и легкостью использования.

Интеграция с MS SQL в Illa позволяет запрашивать, вставлять, обновлять и удалять данные из базы данных MS SQL с помощью команд SQL. Вы также можете создать пользовательские SQL-запросы и запустить их в Illa. Это позволяет легко создавать приложения, управляемые данными, и приборные панели, которые отображают данные в реальном времени из базы данных MS SQL.

### Создать MS SQL

Есть два способа создать ресурс в Illa после входа в ваш аккаунт Illa.

-   Создать в ресурсах

Войдите в учетную запись Illa и выберите `**Ресурсы**` в верхней части страницы и нажмите кнопку `**Создать новое**`.

![external_resource](https://cdn.illacloud.com/official-website/img/docs/external_resource.png)

Выберите **`Microsoft SQL`** из списка баз данных.

![action_list](https://cdn.illacloud.com/official-website/img/docs/action_list.png)

Подключитесь к базе данных с требуемыми параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

После создания ресурса будет показан готовый MS SQL.

![ms_ex_ready](https://cdn.illacloud.com/official-website/img/docs/ms_ex_ready.png)

-   Создать в Builder

Войдите в учетную запись Illa и создайте проект в Illa Builder на странице `**App**` и перейдите в **«Список действий»** внизу страницы. Нажмите `**new**`, затем выберите **`Microsoft SQL`** из списка баз данных. Затем подключитесь к базе данных с нужными параметрами, описанными в «Параметры соединения» ниже.

Нажмите `**Тестовое подключение**`, чтобы увидеть, успешно ли мы можем подключиться к базе данных. Если да, нажмите **`Сохранить ресурсы`**, иначе проверьте имя хоста, порт, имя пользователя и пароль.

### Настройки соединения

Здесь мы должны предоставить информацию для подключения к базе данных MS SQL.

![ms_config](https://cdn.illacloud.com/official-website/img/docs/ms_config.png)

| Свойства                  | Требуется    | Описание                                                                                                                                 |
| ------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Наименование              | обязательный | Название ресурса при создании действий в ILLA.                                                                                           |
| Hostname                  | обязательный | URL или IP-адрес базы данных                                                                                                             |
| Порт                      | обязательный | Номер порта хоста сервера, который необходимо использовать для подключения. Если вы не указали порт, то по умолчанию порт '1433'.        |
| База данных               | обязательный | Название базы данных                                                                                                                     |
| Имя пользователя          | обязательный | имя пользователя, которое вы хотите использовать при входе на MS SQL сервер.                                                             |
| Пароль                    | обязательный | Используйте этот пароль для аутентификации.                                                                                              |
| Опция подключения         | опционально  | пара ключ-значение для указания опции подключения                                                                                        |
| Опции SSL                 | опционально  | принимает решение о том, насколько высоким является безопасное соединение с SSL TCP/IP во время переговоров с сервером.                  |

### Создать действия

Мы создали ресурс MS SQL; мы можем добавить действие, выбрав MS SQL из списка действий и выбрав кнопку **`Create action`**.

![ms_resource_list](https://cdn.illacloud.com/official-website/img/docs/ms_resource_list.png)

Теперь мы добавили MS SQL сервер в качестве действия на нашу страницу строительства.

![ms](https://cdn.illacloud.com/official-website/img/docs/ms.png)

### Настройка MS SQL

| Свойства         | Описание                                                                                                                                                                                                                                                          |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Тип конфигурации | тип конфигурации, которую вы используете для подключения к базе данных MS SQL Server: SQL и Bulk insert. Для режима SQL можно использовать SQL запрос. Для режима пакетной вставки вы можете вставить данные в выбранную таблицу в формате {{ [{x:a, y:b}, … ] }} |
| SQL-запрос       | sql команды для получения (select), вставки, обновления, удаления данных из базы данных.                                                                                                                                                                          |
| Трансформатор    | преобразование данных в понравившийся стиль JavaScript                                                                                                                                                                                                            |

Пример использования:

-   Выбрать

```sql
SELECT * FROM users ORDER BY lastname;
```

-   Insert

```sql
INSERT INTO users

	(firstname, lastname, email)

VALUES

	(

		{{ NewUser.data.firstNameInput }},

		{{ NewUser.data.lastNameInput }},

		{{ NewUser.data.emailInput }}

	);
```

-   Обновить

```sql
UPDATE users
  SET firstname = '{{form1.updatedProfile.first}}'
  WHERE id = {{ form1.updatedProfile.uid}};
```

-   Удалить

```sql
DELETE FROM users WHERE id = {{ form1.updatedProfile.uid }};
```
