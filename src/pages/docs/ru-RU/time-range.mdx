---
title: Диапазон времени
metaTitle: Документ с диапазоном времени компонента | ILLA Cloud
desc: Узнайте, как использовать компонент диапазона времени из ILLA Cloud для того, чтобы пользователи могли выбирать несколько раз с 24-часового интерфейса в ваших приложениях.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/343?languages=ru&filter=basic&value=0
tagCategory: doc_menu_time_range_click
categoryName: 🧬 <unk> ble компоненты
categoryPriority: 6
postPriority: 4
---

## <h2 hidden>Что такое временной диапазон?</h2>

Компонент диапазона времени - это UI компонент, который позволяет пользователям выбирать несколько раз из 24-часового интерфейса, обычно используется в формах или интерфейсах ввода данных, где пользователю необходимо указать диапазон времени начала и окончания.

При добавлении в холст компонента диапазона времени отображается два 24-часовых интерфейса. Один интерфейс используется для выбора времени начала, а другой используется для выбора времени окончания. Пользователь может настроить время, перемещая курсор мыши или используя стрелки вверх и вниз.

### Properties

Доступные свойства диапазона времени. JavaScript может быть написан на чтение или изменение информации о компонентах.

| Свойства                   | Описание                                                                                                                                                                      |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Время начала               | Значение времени начала компонента по умолчанию                                                                                                                               |
| Время окончания            | Значение времени окончания компонента по умолчанию                                                                                                                            |
| Формат                     | Правильная строка формата даты. См. https://day.js.org/docs/en/parse/string-format.                                                                                           |
| Заполнитель старта         | Значение будет показано, когда поле ввода времени начала пустое.                                                                                                              |
| Заполнитель Энда           | Значение будет показано, когда поле ввода времени окончания пустое.                                                                                                           |
| Размер шага                | размер шага в минутах                                                                                                                                                         |
| Метка                      | Имя поля, отображаемого пользователю                                                                                                                                          |
| Подпись                    | Заголовок, используемый для описания поля в деталях                                                                                                                           |
| Скрытая метка              | Выберите, показывать ли метку                                                                                                                                                 |
| Позиция                    | Установить положение метки относительно компонента                                                                                                                            |
| Выравнивание               | Установить выравнивание (выравнивание влево или вправо) метки                                                                                                                 |
| Width                      | Когда ярлык находится в левой части компонента, установите ширину метки.                                                                                                      |
| Обработчик событий         | Триггеры запросов, управляющих компонентов или вызвать другие API в ответ на события компонента.                                                                              |
| Отключено                  | Контролировать состояние отключения компонента. Компонент не может быть изменен или сфокусирован, если он отключен.                                                           |
| Подсказка                  | Пользователи могут ввести подсказку компонента здесь. Подсказка будет показана при фокусировке. Поддерживается формат Markdown.                                               |
| Показать кнопку "Очистить" | контролировать, отображается ли кнопка "Очистить" в поле ввода                                                                                                                |
| Обязательное поле          | Действителен только когда выключатель включен.                                                                                                                                |
| Пользовательское правило   | Создайте логику проверки здесь. Правила должны быть сделаны в JavaScript и покрыты {{}}.                                                                                      |
| Скрыть сообщение проверки  | Вы можете скрыть сообщение об ошибке, переключившись на скрытый статус, когда введенное значение неверно. Вы можете динамически изменить скрытый статус с помощью JavaScript. |
| Ключ данных формы          | Укажите ключ компонента оберточной формы при создании атрибута данных.                                                                                                        |
| Hidden                     | Динамическое управление скрытием компонента. Вы можете изменить скрытый статус через динамическое логическое значение.                                                        |
| Цвет темы                  | Позволяет пользователям указывать цвет фона и прозрачность кнопки                                                                                                             |

### Methods

Вы можете использовать другие компоненты для управления компонентом. Мы поддерживаем следующие два метода:

-   **setStartTime**

Чтобы установить значение начальной даты, например, {{"value1"}}

| Свойства | Описание                       |
| -------- | ------------------------------ |
| Значение | Ввести значение времени начала |

-   **setEndTime**

Чтобы установить значение даты окончания, например, {{"value2"}}

| Свойства | Описание                          |
| -------- | --------------------------------- |
| Значение | Ввести значение времени окончания |

-   **clearValue**

Чтобы очистить значение выбранного компонента 

-   **validate**

Чтобы убедиться, что введенная информация является законной

-   **clearValidate**

Чтобы очистить сообщение проверки

### Обработчик события

| Событие  | Описание                                                                                                             |
| -------- | -------------------------------------------------------------------------------------------------------------------- |
| Изменить | Когда пользователь изменяет выбранное значение времени, выполните определенное действие, настроенное пользователями. |

### Данные

Компонент имеет несколько распространенных данных, которые можно вызвать через **`{{componentName.propertyName}}`** в приложении.

| Название свойства         | Описание                                                       |
| ------------------------- | -------------------------------------------------------------- |
| цветовая схема            | цвет свойства даты                                             |
| настраиваемое правило     | указанное пользователем правило                                |
| отключено                 | логическое значение указывает на отключенный статус            |
| отображаемое имя          | название этого компонента (например, каскадер1)                |
| startTime                 | значение времени начала                                        |
| endTime                   | значение конечного времени                                     |
| Заполнитель запуска       | значение плейсхолдера для времени начала                       |
| endPlaceholder            | значение плейсхолдера для конечного времени                    |
| события                   | значение массива для последовательности событий                |
| формат                    | форма даты и времени                                           |
| formDataKey               | ключ данных формы даты                                         |
| hidden                    | логическое значение указывает на скрытый статус кнопки         |
| скрыть проверку сообщения | логическое значение указывает, скрыто ли сообщение проверки    |
| этикетка                  | значение метки                                                 |
| выровнять метку           | выравнивание этикетки каскадера (слева или справа)             |
| пометка этикетки          | позиция каскадера метки (слева или справа)                     |
| ширина этикетки           | целое число, представляющее ширину метки                       |
| минутшаг                  | шаг за шагом                                                   |
| обязательный              | логическое значение указывает на требуемый статус ввода.       |
| показать очистку          | логическое значение, указывающее на дату будут показаны ясными |
| значение                  | Выбранное значение                                             |

Пример: {{rangeTime1.value[0]}}

### Вариант использования

Далее мы покажем, как сопоставить данные из источника данных до диапазона времени и установить конечное значение до текущего времени кнопкой. 

#### Шаг 1 Добавить действие

Давайте создадим таблицу в REST API **`promoteCodes`,** с 6 столбцами: **`code`**, **`id`**, **`createdAt`**, **`expiredAt`**, **`startedAt`, `updatedAt`**. Мы будем использовать startedAt и истекаем время для использования образцов. 

Затем мы можем создать новое действие для REST API из списка действий, перечислив все данные в promoteCodes и названные restapi1. 

Нажмите **`Сохранить`** и **`Выполнить`** для активации этого действия.

#### Шаг 2 Добавление компонентов

Далее в холсту можно добавить компонент **`time range`** и компонент **`button`**. Для значения времени старта компонента диапазона дат мы используем начатый в момент первого промокода из остальных api данных.

```sql
{{restapi1.data.promoteCodes[0].startedAt}}
```

Точно так же для значения времени окончания по умолчанию мы используем истек срок действия первого промокода из остальных данных api.

```sql
{{restapi1.data.promoteCodes[0].expiredAt}}
```

Мы также назвали кнопку "Сейчас", как показано ниже.

![time_range_0](https://cdn.illacloud.com/official-website/img/docs/time_range_0.png)

#### Шаг 3 Настройка компонента

Для компонента **`button`** мы можем настроить его, чтобы установить значение даты окончания в компоненте **`time range`** на сегодняшнюю дату.

1.  В обработчике события Редактирование выберите **\`Control component ** в действии, выберите компонент **`Диапазон времени`**, который вы хотите обновить в качестве цели события.
2.  Выберите **`setEndTime`** и установите **`End time`** как \`{{currentUserInfo.updatedAt}} 

![time_range_config](https://cdn.illacloud.com/official-website/img/docs/time_range_config.png)

#### Шаг 4 Тест

Теперь, когда вы нажимаете кнопку "Сейчас", время окончания должно измениться на сегодняшнюю дату. Что касается здесь, мы имеем 20:03:05, но это может быть иным, когда вы тестируете его.

![time_range_1](https://cdn.illacloud.com/official-website/img/docs/time_range_1.png)
