---
title: Переключатель
metaTitle: Переключить компонент Doc |  ILLA Cloud
desc: Узнайте, как использовать компонент Switch в ILLA Cloud для создания пользовательского интерфейса, который позволяет пользователям переключаться между двумя параметрами. Смотрите примеры и свойства Switch.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/331?languages=ru&filter=basic&value=0
tagCategory: doc_menu_switch_click
categoryName: 🧬 <unk> ble компоненты
categoryPriority: 6
postPriority: 10
---

## <h2 hidden>Что такое Переключатель?</h2>

Компонент Switch - это элемент пользовательского интерфейса, позволяющий пользователям переключаться между двумя состояниями: "вкл" и "выкл".

### Свойства

| Свойства              | Описание                                                                                                                        |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Значение по умолчанию | Начальное значение компонента. Вы можете динамически изменить начальное значение, введя JavaScript в {{}}.                      |
| Метка                 | Имя поля, отображаемого пользователю                                                                                            |
| Подпись               | Заголовок, используемый для описания поля в деталях                                                                             |
| Скрытая метка         | Выберите, показывать ли метку                                                                                                   |
| Позиция               | Установить положение метки относительно компонента                                                                              |
| Выравнивание          | Задать выравнивание метки                                                                                                       |
| Обработчик событий    | Триггеры запросов, управляющих компонентов или вызвать другие API в ответ на события компонента.                                |
| Загрузка              | Должен ли компонент показывать индикатор загрузки.                                                                              |
| Отключено             | Контролировать состояние отключения компонента. Компонент не может быть изменен или сфокусирован, если он отключен.             |
| Подсказка             | Здесь пользователь может ввести подсказку компонента. Подсказка будет показана при фокусировке. Поддерживается формат Markdown. |
| Hidden                | Динамическое управление скрытием компонента. Вы можете изменить скрытый статус через динамическое логическое значение.          |
| Цвет темы             | Позволяет пользователям указывать цвет фона и прозрачность кнопки                                                               |

### Метод

Вы можете использовать другие компоненты для управления компонентом. Мы поддерживаем следующие три метода:

-   **setValue**

Чтобы установить выбранное значение, например, {{'value1'}}

| Свойства | Описание           |
| -------- | ------------------ |
| Значение | Выбранное значение |

-   **clearValue**

Чтобы очистить выбранное значение

-   **переключение**

переключатель с состояния "выкл" на "вкл" или наоборот

Пример использования:

Переключите поддержку компонента в прослушивании события `onClick` других компонентов, используя встроенную систему событий. Настройте его, следуя следующим примерам:

1.  Добавить срабатывание события к компоненту, который вы хотите прослушивать. Например, если вы хотите прослушать событие **`onClick`** компонента кнопки, вы добавите событие триггер к этому компоненту кнопки.
2.  В обработчике события Редактирование выберите **`Control component`** в действии, выберите компонент **`Switch`**, который вы хотите обновить в качестве цели события.
3.  Выберите действие **`toggle`**. Когда событие срабатывает (например, при нажатии кнопки), компонент переключателя с переключением и его статус будет обновлен.
4.  Сохраните настройки триггера событий и повторите процесс для всех других компонентов, которые вы хотите прослушивать.

Как только вы настроили инициирование событий, компонент переключателя будет автоматически обновляться всякий раз, когда на других компонентах запускается событие **onClick\`**. Это позволяет создавать динамические интерфейсы, отвечающие на ввод пользователя в режиме реального времени, упростить навигацию и взаимодействие с приложением.

### Обработчик события

| Событие  | Описание                                                                                                             |
| -------- | -------------------------------------------------------------------------------------------------------------------- |
| Изменить | Когда пользователь изменяет выбранное значение параметра, выполните конкретное действие, настроенное пользователями. |

### Данные

Компонент кнопок имеет некоторые широко используемые данные, которые можно вызвать через **`{{componentName.propertyName}}`** в приложении.

| Название свойства  | Описание                                               |
| ------------------ | ------------------------------------------------------ |
| цветовая схема     | цвет фона кнопки                                       |
| отображаемое имя   | название этого компонента (например кнопка)            |
| hidden             | логическое значение указывает на скрытый статус кнопки |
| этикетка           | значение метки                                         |
| выровнять метку    | выравнивание ярлыка переключателя (влево или вправо)   |
| пометка этикетки   | положение метки переключателя (влево или вправо)       |
| ширина этикетки    | целое число, представляющее ширину метки.              |
| этикетка заполнена | логическое значение указывает, если метка заполнена.   |
| значение           | Пользователь устанавливает значение true или false     |

### Вариант использования

Далее мы покажем, как динамически переключить статус других компонентов. 

#### Шаг 1 Добавить компонент

Сначала мы добавим компонент **`Switch`** и поместим его как 'Hide'. Затем мы используем компонент **`text`** в качестве влияемого компонента, который изменяется с 'отображаемого' на 'скрытое'. Мы меняем его содержимое на «Привет, мир» 

#### Шаг 2 Настройка компонента

В свойстве **`Hidden`** для Text. Нажмите на значок **`fx`**, чтобы мы могли контролировать скрытое состояние компонента, установив его то же значение, что и у нашего переключателя с помощью JavaScript:

```jsx
{{switch1.value}}
```

Теперь, когда выключатель включен, значение переключателя **`true`,** текст будет скрыт, и наоборот.

Сменить **`Включить`** 

![switch0](https://cdn.illacloud.com/official-website/img/docs/switch0.png)

Сменить **`Выкл`**

![switch_off](https://cdn.illacloud.com/official-website/img/docs/switch_off.png)
