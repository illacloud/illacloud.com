---
title: Таблица
metaTitle: Компонент таблиц | ILLA Cloud
desc: Узнайте, как использовать компонент таблицы для настройки приложения в ILLA Cloud.
tagCategory: doc_menu_table_click
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/267?languages=ru&filter=basic&value=0
categoryName: 🧬 ble компоненты
categoryPriority: 6
postPriority: 1
---

## <h2 hidden>Что такое дата?</h2>

Компонент таблицы в облаке ILLA - это мощная функция, позволяющая пользователям отображать, организовывать и обрабатывать данные таблиц в приложении. Он обеспечивает структурированный макет строками и колонками, позволяющий пользователям просматривать и взаимодействовать с данными удобным и структурированным способом.

### Свойства

| Свойства                                     | Описание                                                                                                                                         |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Источник данных                              | источник данных, заполняющих таблицу                                                                                                             |
| Не указан статус                             | содержимое или сообщение, отображаемое когда в таблице нет данных                                                                                |
| Загрузка                                     | находится ли компонент таблицы в настоящее время в состоянии загрузки                                                                            |
| Столбцы                                      | и конфигурации колонок в таблице. (подробнее о столбцах ниже)                                                                                    |
| Столбец                                      | количество столбцов                                                                                                                              |
| Выбор нескольких строк                       | Использовать {{table.selectedRow[n].columnName}} для доступа к данным.                                                                           |
| Выбор ячейки                                 | Разрешать ли пользователям выбирать ячейку                                                                                                       |
| Нажмите вне экрана, чтобы отменить выбор     | Поддерживается в режиме однократного выделения строк. После включения щелкните по области, отличной от таблицы, чтобы удалить выделенную строку. |
| Переполнение                                 | как таблица обрабатывает содержимое, превышающее доступное пространство в контейнере таблицы (пагинация или прокрутка)                           |
| Включить нумерацию на стороне сервера        | Включить ли нумерацию на сервере                                                                                                                 |
| Размер страницы                              | количество строк для отображения на странице при использовании страниц на стороне сервера                                                        |
| Обновить                                     | Показывать значок обновления.                                                                                                                    |
| Скачать                                      | Показывать кнопку загрузки на панели инструментов.                                                                                               |
| Скачать необработанные данные                | Показывать ли иконку загрузки необработанных данных.                                                                                             |
| Фильтр                                       | Показывать кнопку фильтра на панели инструментов                                                                                                 |
| Обработчик события                           | обнаружение и ответ на конкретные пользовательские действия или события, такие как клики, нажатия клавиш, или формы представлений.               |
| Отключено                                    | неинтерактивный и не может быть изменен или вызван пользователем.                                                                                |

#### **Обзор свойств столбца**

Для каждого столбца мы можем настроить его свойства для настройки отображаемых данных для каждого столбца.

| Свойства                                                 | Описание                                                                                                                                                                                                                         |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Название столбца                                         | определяет имя или метку столбца, который служит в качестве заголовка для идентификации данных, которые он представляет                                                                                                          |
| Тип столбца                                              | определяет тип данных, которые содержит колонка, включая автоматический текст, дату, тег, время, дату, время, время, время, число, процент, ссылка, кнопка, логическая группа, изображение, iconGroup, рейтинг, Markdown, валюта |
| Сопоставленное значение                                  | Используйте {{ currentRow }} для доступа к данным текущей строки. Например, для вычисления "Стоимость за клик" на основе колонок "стоимость" и "кликов":                                                                         |
| {{ currentRow.cost / currentRow.clickTimes }}            |                                                                                                                                                                                                                                  |
| Включить сортировку                                      | позволяет ли столбец сортировать данные таблицы, основываясь на значениях в этом столбце                                                                                                                                         |
| Справочная информация                                    | Установите фон столбца. Используйте {{ напишите условный оператор для установки цвета, отображаемого каждой ячейкой в разных условиях.                                                                                           |
| Выравнивание                                             | контролирует горизонтальное выравнивание содержимого в ячейках столбца                                                                                                                                                           |

#### Типы столбцов:

-   Авто

динамический тип, который автоматически определяет тип данных на основе значений в столбце

-   Текст

представляет столбец, который содержит текстовые данные

-   Дата

представление значений данных, соответствующих определенным датам

| Свойства   | Описание                                                                       |
| ---------- | ------------------------------------------------------------------------------ |
| Формат     | принимает различные коды или шаблоны форматирования, по умолчанию — YYYY-MM-DD |

-   Тег

специализированный формат для отображения тегов или меток, связанных с записями данных

| Свойства     | Описание                                                        |
| ------------ | --------------------------------------------------------------- |
| Метка метки  | представляет текст или метку, назначенные тегу в колонке "Тег"  |
| Цвет тега    | определяет цвет, присвоенный метке в колонке "Тег"              |

-   Время

точно отображать и манипулировать значениями времени в таблице

| Свойства   | Описание                                                                       |
| ---------- | ------------------------------------------------------------------------------ |
| Формат     | визуальное представление данных времени в столбце, по умолчанию — HH:mm:ss     |

-   Дата

магазины и представление значений даты и времени

| Свойства   | Описание                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------- |
| Формат     | указывает, как значения даты и времени должны отображаться в столбце, по умолчанию — YYYY-MM-DD HH:mm:ss |

-   Номер

обрабатывать числовые данные

| Свойства                     | Описание                                                                                                                    |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Десятичные знаки             | укажите количество десятичных знаков для отображения числовых значений в столбце                                            |
| Показать тысячи разделителей | должен ли столбец отображать тысячи разделителей, чтобы улучшить читаемость и простоту толкования для больших чисел         |

-   Процент

обрабатывать процентные значения в краткой и привлекательной форме

| Свойства                     | Описание                                                                         |
| ---------------------------- | -------------------------------------------------------------------------------- |
| Десятичные знаки             | укажите количество знаков после запятой в столбце                                |
| Показать тысячи разделителей | следует ли включать тысячи разделителей в отображаемые процентные значения       |

-   Ссылка

включить кликабельные ссылки в конкретный столбец

-   Кнопка

включить интерактивные кнопки внутри определенного столбца

| Свойства           | Описание                                                                                |
| ------------------ | --------------------------------------------------------------------------------------- |
| Обработчик события | определяет действие или событие, которое происходит, когда нажата кнопка внутри столбца |
| Отключено          | кнопка в столбце включена или отключена                                                 |
| Цвет темы          | выбрать цвет темы для кнопки                                                            |
| Вариант            | визуальный стиль или вариант кнопки (налево или контур)                                 |

-   группа кнопок

позволяет пользователям создавать интерактивные группы кнопок в столбце

Для каждой кнопки в столбцах можно настроить её свойства.

| Свойства                 | Описание                                                                                                                                |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| Сопоставленное значение  | связывать каждую кнопку в группе кнопок с определенным значением или полем из источника данных                                          |
| Вариант                  | визуальный стиль или внешний вид кнопок в группе                                                                                        |
| Обработчик события       | действие или функция срабатывает, когда кнопка в группе нажата или взаимодействует с                                                    |
| Отключено                | включены или отключены кнопки в группе кнопок                                                                                           |
| Цвет темы                | настроить цвет или стиль кнопок в группе для согласования с общей темой или брендингом их приложения или веб-сайта                      |

-   Boolean

представляет данные, которые имеют два возможных состояния: true или false

-   Изображение

заполните столбец в таблице с данными изображения

| Свойства     | Описание                                                       |
| ------------ | -------------------------------------------------------------- |
| Тип масштаба | как изображение масштабируется или изменяется в ячейке таблицы |

-   Группа иконок

специализированная колонка, позволяющая пользователям отображать иконки внутри группы или коллекции

Для каждой кнопки в столбцах можно настроить её свойства.

| Свойства           | Описание                                                                                                                           |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| Иконка             | позволяет пользователям выбрать или указать значок, который будет отображаться в колонке IconGroup                                 |
| Цвет темы          | определяет цветовую схему или тему, применяемую к значку внутри столбца IconGroup                                                  |
| Обработчик события | позволяет пользователям определять событие или действие при взаимодействии с значком в колонке IconGroup                           |
| Отключено          | состояние столбца IconGroup, определяющее отключено или включено для взаимодействия пользователя                                   |

-   Рейтинг

представляет рейтинги или обратную связь с товарами или сущностями

-   Markdown

возможность визуализировать отформатированный текст с помощью синтаксиса Markdown в ячейках столбца

-   Валюта

обрабатывать и форматировать значения валют.

| Свойства                     | Описание                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------ |
| Десятичные знаки             | количество знаков после запятой в столбце                                      |
| Код валюты                   | определяет код или аббревиатуру валюты используемой в столбце                  |
| Показать тысячи разделителей | отображается ли разделитель тысяч для больших значений валют                   |

### Метод

Вы можете использовать другие компоненты для управления компонентом. Мы поддерживаем следующие два метода:

-   **selectPage**

выбрать все строки на конкретной странице в страничной таблице

| Свойства           | Описание                                         |
| ------------------ | ------------------------------------------------ |
| Конечное значение  | номер страницы или индекс выбранной страницы     |

-   **selectRow**

выберите конкретную строку в таблице

| Свойства                      | Описание                                                                                                                                                                                   |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Выбранная строка по умолчанию | по умолчанию, выбранное значение строки, может быть индексом строки, уникальным идентификатором или любым другим свойством, которое уникально идентифицирует строку, которая будет выбрана |

-   **clearSelection**

очистить текущий выбор в таблице

-   **setFilters**

применять фильтры к данным таблицы

| Свойства      | Описание                              |
| ------------- | ------------------------------------- |
| Фильтры       | Условия или критерии фильтрации       |
| Режим фильтра | определяет как применяются фильтры    |

-   **ClearFilters**

очищает все применяемые фильтры в таблице

-   **setSort**

задать сортировку для таблицы

| Свойства     | Описание                                                                |
| ------------ | ----------------------------------------------------------------------- |
| Столбец      | столбец, в котором должна быть применена сортировка                     |
| Направление  | направление типа, которое может быть "по возрастанию" или "по убыванию" |

### Данные

Компонент имеет несколько распространенных данных, которые можно вызвать через **`{{componentName.propertyName}}`** в приложении.

| Название свойства                          | Описание                                                                                                             |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| clickOutsideToResetSelection               | если щелчок за пределами элемента таблицы должен сбросить текущий выбор                                              |
| columnMapper                               | объект, который сопоставляет поля данных или ключи источника данных таблицы с соответствующими названиями столбцов   |
| индексы имен столбцов                      | объект, который хранит индексы названий столбцов                                                                     |
| колонки                                    | массив, который определяет конфигурацию и свойства каждого столбца в таблице                                         |
| видимость столбцов                         | объект, который сохраняет статус видимости каждого столбца в таблице                                                 |
| customColumnIndices                        | объект, который хранит индексы пользовательских столбцов в таблице                                                   |
| источник данных                            | источник данных для таблицы                                                                                          |
| dataSourceJS                               | JavaScript-представление источника данных                                                                            |
| dataSourceMode                             | способ или тип источника данных.                                                                                     |
| ключ сортировки по умолчанию               | клавиша по умолчанию для сортировки таблицы                                                                          |
| defaultSortOrder                           | порядок сортировки по умолчанию для таблицы                                                                          |
| отключено                                  | является ли компонент таблицы отключенным или нет                                                                    |
| отобразить данные                          | отображаемые данные в таблице                                                                                        |
| отображать индексы данных                  | индексы отображаемых строк в таблице                                                                                 |
| отображаемое имя                           | определяет имя или метку компонента таблицы                                                                          |
| скачать                                    | позволяет загружать данные таблицы                                                                                   |
| скачивать данные RawData                   | загрузить ли необработанные данные или отображаемые данные таблицы                                                   |
| пустой штат                                | содержимое или сообщение, отображаемое когда таблица пуста                                                           |
| включить SidePagination на сервере         | Если выбрано значение true, свойство enableServerSidePagination разрешает пагинацию на стороне сервера для таблицы   |
| включить выбор SingleCellSelection         | позволяет выбрать одну ячейку в таблице                                                                              |
| фильтр                                     | применяемый фильтр или условия фильтрации в таблице                                                                  |
| hasNextPage                                | доступна ли следующая страница в странице                                                                            |
| загрузка                                   | находится ли компонент таблицы в состоянии загрузки                                                                  |
| множественный выбор                        | позволяет выбирать несколько строк в таблице                                                                         |
| nextAfterCursor                            | курсор или идентификатор для получения следующего набора данных в странице на стороне сервера                        |
| следующий курсор                           | курсор для получения предыдущего набора данных в пагинации на стороне сервера                                        |
| переполнение                               | поведение таблицы, когда содержимое превышает доступное место                                                        |
| pageIndex                                  | индекс текущей страницы в страничной таблице                                                                         |
| размер страницы                            | определяет количество строк для отображения на странице в странице                                                   |
| paginationOffset                           | количество страниц для смещения в пагинации                                                                          |
| обновить                                   | вызывает обновление данных таблицы                                                                                   |
| переименовать имена столбцов               | сохраняет переименование столбцов в таблице                                                                          |
| события рядов                              | позволяет привязать пользовательские события или действия к определенным строкам в таблице                           |
| Всего RowCount                             | общее количество строк в таблице                                                                                     |

Пример: {{table1.disabled}}

## Вариант использования

Далее мы покажем, как сопоставить данные из `**Upstash**` в `**table**`.

#### Шаг 1 Добавить действие

Давайте создадим в Upstash таблицу под названием **`employee_profile`,** в том числе 4 колонки `name`, `level`, `age` и `married`.

Затем мы можем создать новое действие для Upstash из списка действий в ILLA Cloud и назвать upstash2.

To list all data in employee_profile put the code snippet below in the Redis query section.

```sql
Получить профиль employee_profile
```

Поскольку выходные данные являются списком пар словарей, мы хотим преобразовать их в массив. В Transformer, включите его и поместите код ниже.

```jsx
const jsonString = data[0].result;

// Parse the JSON string into a JavaScript object
const jsonData = JSON.parse(jsonString);

// Access the "employees" property and map it to a new array
const mappedArray = jsonData.employees.map((employee) => ({
  name: employee.name,
  level: employee.level,
  age: employee.age,
  married: employee.married,
}));

return mappedArray;
```

![up_code](https://cdn.illacloud.com/official-website/img/docs/up_code.jpg)

Нажмите **`Сохранить`** и **`Выполнить`** для активации этого действия.

![up_data](https://cdn.illacloud.com/official-website/img/docs/up_data.jpg)

#### Шаг 2 Добавление компонентов

Далее мы можем добавить в холст **`table`** компонент.

В поле **`Источник данных`** на странице Инспекции после нажатия на компонент, поместите `{{upstash2.data}}`, чтобы получить данные из Upstash.

#### Шаг 3 Тест

Окончательный вид должен быть таким, как показано.

![up_test](https://cdn.illacloud.com/official-website/img/docs/up_test.jpg)
