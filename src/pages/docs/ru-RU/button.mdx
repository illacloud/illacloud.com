---
title: Кнопка
metaTitle: Кнопка Компонент | ILLA Cloud
desc: Узнайте, как использовать компонент кнопок в ILLA Cloud, низкокодовой платформе для инструментов строительства. Кнопка позволяет пользователям срабатывать действие или выполнять функцию, кликнув мышью.
crowdinRepo: https://crowdin.com/multilingual/illacloud-website/303?languages=ru&filter=basic&value=0
tagCategory: doc_menu_button_click
categoryName: 🧬 <unk> ble компоненты
categoryPriority: 6
postPriority: 0
---

## <h2 hidden>Что такое кнопка?</h2>

Компонент кнопки позволяет пользователям срабатывать действие или выполнять функцию, нажав на кнопку. 

### Свойства

| Свойства           | Описание                                                                                                                        |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| Текст              | Текст, отображаемый для кнопки                                                                                                  |
| Обработчик событий | Триггеры запросов, управляющих компонентов или вызвать другие API в ответ на события компонента.                                |
| Загрузка           | Должен ли компонент показывать индикатор загрузки.                                                                              |
| Отключено          | Контролировать состояние отключения компонента. Компонент не может быть изменен или сфокусирован, если он отключен.             |
| Подсказка          | Здесь пользователь может ввести подсказку компонента. Подсказка будет показана при фокусировке. Поддерживается формат Markdown. |
| Hidden             | Динамическое управление скрытием компонента. Вы можете изменить скрытый статус через динамическое логическое значение.          |
| Вариант            | Разрешить пользователям выбирать между двумя разными стилями кнопки: заполнить или очертить.                                    |
| Цвет темы          | Позволяет пользователям указывать цвет фона и прозрачность кнопки                                                               |

### Обработчик события

| Событие | Описание                                                                                   |
| ------- | ------------------------------------------------------------------------------------------ |
| Click   | Когда пользователь нажимает на эту кнопку, выполните действия, настроенные пользователями. |

### Данные

Компонент имеет несколько распространенных данных, которые можно вызвать через **`{{componentName.propertyName}}`** в приложении.

| Название свойства | Описание                                                            |
| ----------------- | ------------------------------------------------------------------- |
| цветовая схема    | цвет фона кнопки                                                    |
| отключено         | логическое значение указывает на отключенный статус                 |
| отображаемое имя  | название этого компонента (например кнопка)                         |
| hidden            | логическое значение указывает на скрытый статус кнопки              |
| скрытый           | логическое значение указывает на скрытый динамический статус кнопки |
| загрузка          | стоимость загрузки                                                  |
| текст             | текстовое значение, отображаемое на кнопке                          |
| tooltipТекст      | значение текста подсказки                                           |
| вариант           | стиль варианта кнопки (заполнить или очертить)                      |

### Вариант использования

Далее мы покажем, как использовать кнопку для динамического управления компонентом **`form`**. 

#### Шаг 1 Добавить действие

Мы создали таблицу с именем **`countries`**, включая 5 столбцов: **`num_code`**, **`alpha_2_code`**, **`alpha_3_code`**, **`en_short_name`**, **`nationality`**. Имена стран — это то, что мы хотим использовать в качестве значения параметров, которые хранятся в столбце **`en_short_name`**.

Затем мы можем создать новое действие для поддержки из списка действий с указанием всех данных в \*\*странах и под названием supabasedb1 

```sql
выбрать distinct en_short_name как значение из порядка "Страны" по значению;
```

Вот что у нас есть как результат:

<img src='https://cdn.illacloud.com/official-website/img/docs/countries.png' alt='' />

Нажмите **`Сохранить`** и **`Выполнить`** для активации этого действия.

#### Шаг 2 Добавить компонент

Сначала мы добавим компонент **`Form`** и поместим его как 'Survey'. Затем перетаскиваем в форму входящую составляющую, выделенную компонент и входную числовую составляющую, назвав их как «Имя», «Национальность», ** и **«Возраст».\*\*

Затем мы сопоставляем значения **`страны`** для выбора компонента по 

1.  в указанном режиме, установите источник данных для `{{supabasedb1.data}}` 
2.  в параметре сопоставления, установите значение `{{item.value}} ` \`

<img src='https://cdn.illacloud.com/official-website/img/docs/button1.png' alt='' />

Затем мы можем настроить кнопку **`Submit`** для управления методом **`Send`** компонента **`Form`**. 

1.  В обработчике события Редактирование выберите **`Control component`** в действии, выберите компонент **`form`**, который вы хотите обновить в качестве цели события.
2.  Выберите действие **`Отправить`**. 

<img src='https://cdn.illacloud.com/official-website/img/docs/countries2.png' alt='' />
